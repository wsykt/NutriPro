# 个人健康助手 — 真实实现架构说明（v4.0）

> 版本：v4.0（真实实现版）
> 日期：2026-08-09
> 说明：本文档只记录当前仓库中**真实实现、经验证可用**的模块与能力。规划中未落地的功能（异步任务表之外的扩展、Redis 缓存/队列、接口限流/黑名单、五库物理隔离、知识库快照、素材预筛选、文献预检、路由日志、营养素计算器、Ollama 常驻监控/故障重启、爬虫队列/三档速率、多设备分布式部署等）一律不收录。
> 所有内容均可在代码仓库中找到对应文件佐证。

---

## 一、总体架构（真实运行拓扑）

```
┌──────────────────────────────────────────────────────────────┐
│                 Vue 3 前端 (5173)                             │
│  登录/注册 · 健康档案 · 饮食记录 · 营养报告 · 菜谱库 · 文章    │
│  AI 咨询（对话/流式） · 运动方案 · 指标历史 · 家庭关系         │
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTP REST + SSE (JWT 鉴权)
┌───────────────────────────▼──────────────────────────────────┐
│              Spring Boot 后端 (8082)                          │
│  14 个 Controller · SQLite · JWT · SseEmitter 流式转发        │
│  AiConsultController 统一转发 AI 服务（对话/流式/四功能/语音） │
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTP REST（AI 服务地址 localhost:8002）
┌───────────────────────────▼──────────────────────────────────┐
│              Python AI 服务 (8002)  FastAPI v2.0.0           │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ API 层 main.py（39 个端点）                              │ │
│  └───────────────────────────┬─────────────────────────────┘ │
│  ┌───────────────────────────▼─────────────────────────────┐ │
│  │ 编排层 agent/orchestrator.py                             │ │
│  │  健康快照构建(七模块) → 检索判定 → 模式路由 → Agent 分发  │ │
│  └───────────────────────────┬─────────────────────────────┘ │
│  ┌───────────────────────────▼─────────────────────────────┐ │
│  │ 服务层 services/（11 个组件）                            │ │
│  │  mode_router 路由 · validation_service 校验 ·            │ │
│  │  template_store 模板镜像 · retrieval_service 检索 ·      │ │
│  │  kb_cluster/literature_merge/dispute_detect/kb_dedup    │ │
│  │  async_task_service 异步 · ollama_manager 管理           │ │
│  └───────────────────────────┬─────────────────────────────┘ │
│  ┌───────────────────────────▼─────────────────────────────┐ │
│  │ Agent 层 agent/（13 文件：11 业务 Agent + 编排 + 基类）  │ │
│  │  validators/（4 校验器）                                 │ │
│  └───────────────────────────┬─────────────────────────────┘ │
│  ┌───────────────────────────▼─────────────────────────────┐ │
│  │ 流水线与兜底                                             │ │
│  │  pipeline_v32.py 文章流水线 · local_fallback_engine 兜底 │ │
│  └─────────────────────────────────────────────────────────┘ │
└───────────────┬──────────────┬──────────────┬───────────────┘
                ▼              ▼              ▼
┌──────────────┐  ┌──────────┐ ┌──────────────────────────┐
│ ChromaDB     │  │ Ollama   │ │ SQLite (data/*.db)        │
│ 向量知识库    │  │ qwen2.5  │ │ templates/food/           │
│ + 嵌入BGE    │  │ 7B 本地   │ │ conversations/token_usage │
└──────────────┘  └──────────┘ │ /user_memory/cache        │
                      ┌────────┴──────────┐                  │
                      │ 云端 DeepSeek API │ ←───────────────┘
                      └───────────────────┘
```

---

## 二、AI 服务分层职责

| 层 | 职责 | 真实模块 |
|----|------|----------|
| **API 层** | 暴露 39 个 REST 端点，参数校验、`success_response`/`error_response` 统一包装、SSE 流式 | `main.py` |
| **编排层** | 健康快照构建（七模块）、检索判定、模式路由（A/C/高性能）、任务分发、异步线程池卸载 | `agent/orchestrator.py`、`services/mode_router.py`、`services/async_task_service.py` |
| **服务层** | 模板存储与对账、统一检索、模式路由、统一校验入口、双层聚类、文献合并、争议识别、去重、Ollama 管理、免责声明 | `services/*`（11 文件） |
| **Agent 层** | 11 个业务智能体，各自专注业务；LLM/检索/校验从服务层注入 | `agent/*`（13 py）+ `agent/validators/`（4 校验器） |
| **基础设施层** | LLM 双链路路由（本地 Ollama + 云端 DeepSeek）、BGE 嵌入、ChromaDB 混合检索、SQLite 统一连接 | `llm/router.py`、`vector/embedder.py`、`vector/retriever.py`、`utils/sqlite_utils.py` |

---

## 三、API 端点面（main.py，39 个端点，实测清单）

### 3.1 核心咨询功能（后端 AiConsultController 转发）

| 端点 | 功能 |
|------|------|
| `POST /api/v1/chat` | 健康问答（成功响应走 `success_response` 包装） |
| `POST /api/v1/chat/stream` | SSE 流式对话（事件：thinking/delta/done/error，UTF-8 显式编码） |
| `POST /api/v1/diet/plan` | 一日饮食方案（直接返回结果对象，含 `_meta`） |
| `POST /api/v1/food/recommend` | 食材菜谱推荐 |
| `POST /api/v1/exercise/advice` | 个性化运动方案 |
| `POST /api/v1/meal/parse` | 自然语言饮食解析 |
| `POST /api/v1/nutrition/analyze` | 营养分析（BMR/宏量微量/建议） |
| `POST /api/v1/food/audit` | 食材审核 |
| `POST /api/v1/voice/parse` | 语音语义解析 |
| `POST /api/v1/report/weekly-summary` | 周报生成 |
| `POST /api/v1/health/reflection`、`/reflection` | 健康反思 |

> **接口契约差异（实测确认）**：`/api/v1/chat` 返回 `{success, data}` 包装；`diet/food/exercise` 等 JSON 端点直接返回结果对象（含 `_meta.mode` 与 `_meta.route`）。

### 3.2 文章生成与质量

| 端点 | 功能 |
|------|------|
| `POST /api/v1/article/generate` | 科普文章生成（速读/深度/综述三梯度，B 流水线） |
| `POST /api/v1/quality/score`、`GET /quality/stats` | 文章质量评分与分析 |
| `POST /api/v1/agent/fact-check` | 文献事实校验（PMID 真实性 + 数据一致性） |
| `POST /api/v1/agent/search-materials` | 多源素材搜集（PubMed/官方指南/权威报告） |

### 3.3 知识库与任务

| 端点 | 功能 |
|------|------|
| `POST /api/v1/knowledge/ingest` | 文档摄入（自动分块入库 + 双写） |
| `GET /api/v1/knowledge/list`、`GET /knowledge/stats`、`GET /knowledge/hot-stat` | 知识库列表/统计/热主题 |
| `POST /api/v1/knowledge/acquire` | 云端结果自动回流（相似度 0.8 去重后入库，自增长闭环） |
| `POST /api/v1/knowledge/log-usage` | 检索用量记录 |
| `GET /api/v1/tasks`、`POST /api/v1/tasks/submit`、`GET /api/v1/tasks/{task_id}` | 异步任务提交与轮询（PENDING→RUNNING→SUCCESS/FAILED） |
| `GET /api/v1/agent/stats`、`GET /api/v1/agent/stats/export` | Agent 调用统计 |

### 3.4 Ollama 管理与看板

| 端点 | 功能 |
|------|------|
| `GET /api/v1/ollama/status`、`/models`、`/context-window` | Ollama 健康/模型列表/上下文窗口查询 |
| `POST /api/v1/ollama/models/load`、`/unload`、`/pull`、`/context-window` | 模型加载/卸载/拉取/num_ctx 运行时调整 |
| `GET /health` | AI 服务健康检查 |
| `GET /dashboard`、`/dashboard/{path}` | 监控看板（静态页） |

---

## 四、服务层组件（services/，11 个真实文件）

| 组件 | 文件 | 职责 |
|------|------|------|
| 模式路由 | `mode_router.py` | A/C 方案路由（含提示词模板，见测试方案第四章）；`route()` 统一入口 |
| 校验统一入口 | `validation_service.py` | `validate_response`（委托三组件链）/ `validate_article`（文稿校验） |
| 模板权威镜像 | `template_store.py` | `ai_templates` 表 upsert/get/list/reconcile（与 ChromaDB 对账） |
| 统一检索 | `retrieval_service.py` | 混合检索（BM25+向量）、上下文构建、文档摄入、统计（Agent 与 API 统一入口） |
| 双层聚类 | `kb_cluster.py` | BGE 粗筛 → 本地 LLM 语义终审 → 三层分级 |
| 文献合并 | `literature_merge.py` | 同主题同结论 → 500 字复合权威文献卡片 |
| 学术争议识别 | `dispute_detect.py` | 结论相悖文献归类打标（dispute），注入综述 |
| 去重服务 | `kb_dedup_service.py` | ID 硬去重 + 标题相似度 + 结论一致性判断 |
| 异步任务 | `async_task_service.py` | 线程池卸载长任务（MAX_LLM_CONCURRENCY=5），task_id 轮询状态 |
| Ollama 管理 | `ollama_manager.py` | 模型列表/加载/卸载/num_ctx 运行时调整/拉取 |
| 免责声明 | `disclaimer.py` | 回复免责声明文案注入 |

---

## 五、Agent 层（agent/，13 个 py 文件）

### 5.1 业务 Agent（11 个）

| 领域 | Agent | 职责 |
|------|-------|------|
| **健康咨询域** | `retrieve_judge.py` | 检索判定（是否需要检索知识库） |
| | `health_reflection.py` | 结合画像/趋势/饮食/运动的多轮反思 |
| | `weekly_report.py` | 周期健康周报 |
| **饮食管理域** | `nlu_parser.py` | 自然语言/语音 → 结构化营养数据（倒装修正、多食材拆分） |
| | `nutrition_analysis.py` | BMR 对比、宏量/微量评估 |
| | `food_audit.py` | 食材热量评估、禁忌替换 |
| | `diet_plan.py` | 一日饮食方案（模板召回 + 本地改写） |
| | `food_recommend.py` | 菜谱推荐（按标签/人群匹配） |
| **运动管理域** | `exercise_advice.py` | 结合体测与饮食的运动方案 |
| **内容生成域** | `article_generate.py` | 科普文章（B 流水线：本地骨架 + 云端正文） |
| **辅助工具** | `voice_text_parse.py` | Web Speech 文本 → Qwen 语义解析 |
| 基类 | `base.py` | Agent 基类 |
| 编排 | `orchestrator.py` | 编排层（健康快照七模块 + 路由 + 分发） |

### 5.2 校验器（agent/validators/，4 个真实文件）

| 校验器 | 职责 |
|--------|------|
| `fact_checker.py` | 事实校验（PMID 真实性 + 数据一致性） |
| `numeric_checker.py` | 数值校验（营养素 vs 膳食指南 2022） |
| `safety_interceptor.py` | 安全拦截（输出提示咨询医师等） |
| `validator.py` | 校验器基类/统一接口 |

---

## 六、模型路由体系（实测行为）

### 6.1 路由方案

| 场景 | 路由 | 说明 |
|------|:----:|------|
| 模板命中（normal） | **A_template_local** | 模板召回 → 本地 Qwen 改写，平均约 2.1s |
| 模板未命中（normal） | **C_fallback** | 云端 DeepSeek 生成 + 本地格式校验 |
| 高性能演示 | **C_direct** | `high_performance=true` 直连云端，跳过本地校验（LLM_TIMEOUT_HIGH_PERF=45） |
| 离线/断网 | **A 纯本地** | 本地 Qwen + 规则引擎兜底（`local_fallback_engine.py`） |

### 6.2 实测路由分布（2026-08-09 单用户测试）

| 测试线 | 路由分布 |
|--------|----------|
| 脚本测试（12 用例） | A×6 + C_fallback×2 + C_direct×4 |
| 真实环境（8 用例） | A×3 + C_fallback×1 + C_direct×4 |

### 6.3 健康快照（orchestrator 七模块）

system prompt 按需组装：用户画像、当前体测、7 日趋势、今日饮食、7 日运动、知识库参考、对话意图。前端无需用户重复描述，系统自动推导。

---

## 七、知识库与向量检索

### 7.1 存储与双写

```
ChromaDB（knowledge/chroma_db_storage，单物理库 + 元数据分类标签）
  └── retriever.add(卡片)
        ├─ ChromaDB 向量入库（检索主库）
        └─ SQLite ai_templates 双写（data/templates.db 权威镜像）
启动对账 reconcile：已验 1400 条双侧一致
```

### 7.2 模板矩阵（1400 条）

```
7 人群（普通人/孕妇/健身人群/老年人/青少年/糖尿病患者/通用）
  × 5 档 BMI（过低/偏低/正常/偏高/超高）
  × 4 功能（qa/diet_plan/food_recommend/exercise）
  × 10 模板/组合
  = 1400 条知识库模板卡片（150-250 字，metadata 含人群/BMI/方向/功能）
```

生成脚本 `generate_kb_templates.py` 支持 7 人群并行进程 + checkpoint 断点续跑（产物：`kb_out_*.json`、`ckp_*.json`）。

### 7.3 检索与回流闭环

```
查询 → retriever.hybrid_retrieve（BM25 + 向量，按 target_crowd 过滤）
  → 模板命中 → A 方案本地改写
  → 未命中/高性能 → 云端生成
  → 结果经 kb_dedup（相似度阈值 0.8）自动回流入库（knowledge/acquire）
```

### 7.4 文献与素材加工

- **双层聚类** `kb_cluster.py`：BGE 粗筛 → 三层分级（低分过滤/灰色区间本地 LLM 终审/高分合并）
- **文献合并** `literature_merge.py`：同主题同结论 → 500 字复合权威卡片（附全部 PMID）
- **争议识别** `dispute_detect.py`：结论相悖 → dispute 标签 + 双方立场，注入综述
- **去重** `kb_dedup_service.py`：ID 硬去重 + 标题相似度 + 结论一致性
- 知识库原始素材：`knowledge_base/`（full_knowledge_base.json、enriched_cards_v2.json、knowledge_base_channels.json）+ `knowledge/pdfs/`（膳食指南等权威 PDF）

---

## 八、文章生成流水线（pipeline_v32.py）

### 8.1 B 流水线（真实实现并实测）

```
主题 + 人群 + 三文稿类型
  → ① 素材检索召回（知识库模板 + 权威文献）
  → ② 本地模型生成骨架（仅结构标记，不写正文）
  → ③ 云端按骨架分块填充正文
  → ④ 本地格式校验（标签完整/字数/层级顺序）
  → 输出：速读卡 / 深度文 / 综述文 + 参考文献（真实 PMID）
```

### 8.2 实测对比（2026-08-07，article_3plans）

| 方案 | 字数 | 参考文献 | 云端 token | 耗时 |
|------|:----:|:-------:|:---------:|:----:|
| B 混合（本地骨架+云端正文） | 4983 | 11（真实 PMID） | 5004 | 98.9s |
| C 纯云端 | 5182 | 11（真实 PMID） | 4456 | 34.9s |

结论：B 与 C 质量相当（字数差 4%），C 更快更省 token；B 保留本地离线能力。

### 8.3 三梯度科普体系

| 梯度 | 定位 | 结构 |
|:----:|------|------|
| 速读层 | 通俗、轻量化 | 图文卡片式 |
| 深度层 | 原理 + 营养数据 + 标准依据 | 完整章节，引用官方指南数值 |
| 综述层 | 多文献整合 + 学术争议 + 研究对比 | 融合争议识别结果 |

前端展示：首页主题列表卡片 → 三篇幅选项卡片 → 文章详情页（目录提取/术语标注）。

---

## 九、异步任务与流式输出

### 9.1 异步任务（services/async_task_service.py，真实实现）

```
POST /api/v1/tasks/submit → 立即返回 task_id
GET  /api/v1/tasks/{task_id} → 轮询进度与结果
状态机：PENDING → RUNNING → SUCCESS / FAILED
线程池：ThreadPoolExecutor(max_workers=MAX_LLM_CONCURRENCY=5)
  └─ 长任务端点统一 run_in_thread 卸载，防止阻塞事件循环
```

### 9.2 SSE 流式（真实实现）

- AI 服务：`POST /api/v1/chat/stream` 标准 SSE（事件 thinking/delta/done/error），中文显式 UTF-8 编码防乱码
- 后端：`AiConsultController /consult/stream` 用 `SseEmitter(120s)` 转发
- 说明：当前流式覆盖对话链路；文章生成采用同步 + 异步任务两种方式，无分章流式

---

## 十、爬虫与知识库构建脚本（crawler/ + 顶层脚本）

### 10.1 爬虫（crawler/，真实实现）

| 脚本 | 职责 |
|------|------|
| `food_crawler.py` | 食物营养数据采集 |
| `literature_enrich.py` / `literature_enrich_v2.py` | 科研文献丰富（PubMed） |
| `merge_foods.py` | 相似食物合并（如大米→普通大米） |
| `ingest_to_sqlite.py` | 数据入库 SQLite |
| `ingest_to_vector_kb.py` | 数据入库向量库 |
| `compliance.py` | 合规约束（白名单/robots/限频） |
| `verify.py` | 数据校验 |
| `e2e_test.py` / `benchmark.py` | 端到端测试 / 性能基准 |
| `raw_store.py` / `parsers.py` / `config.py` | 原始存储 / 解析 / 配置 |

采集产出：`crawler/exported_food_data/`（9 类食物 CSV/MD：主食/肉蛋类/水产/蔬菜/水果/豆制品/奶类/油脂类/调味品）、`merged_foods.json`。

### 10.2 知识库构建（顶层脚本，真实实现）

| 脚本 | 能力 |
|------|------|
| `generate_kb_templates.py` | 模板批量生成：`--workers N` 多进程（按人群切分）· `--no-cloud` 离线 · `--resume` 断点 · `--crowd/--bmi/--func/--per-group` 定向 · `--dry-run` |
| `knowledge_builder_channels.py` | 多渠道采集参数化：`--topics-file` 自定义主题 JSON · `--max-results/--max-topics` 限量 · `--channels` 选渠道 · `--output` |
| `build_knowledge_base_full.py` | 全量知识库构建 |
| `import_cards_to_chromadb.py` | 卡片导入 ChromaDB |
| `augment_knowledge_base.py` / `kb_extend.py` | 知识库扩充 |
| `merge_kb_channels.py` | 多渠道结果合并 |
| `process_and_ingest.py` | 处理并入库 |
| `source_verifier.py` | 来源校验 |
| `manage_articles.py` | 文章管理 |

---

## 十一、单用户测试体系（test_output/single_user_test/）

### 11.1 双线并行测试

```
脚本测试 ─► run_single_user_script_test.py（直调 ModeRouter.route，12 用例，离线可跑）
真实环境 ─► run_single_user_real_test.py（HTTP 调 4 个 API，8 个代表用例）
两条线互不依赖，输出独立 JSON 后汇总。
```

### 11.2 结果（2026-08-09 13:00-13:12）

| 测试线 | 用例数 | 通过 | 路由分布 |
|--------|:-----:|:----:|----------|
| 脚本测试 | 12 | 8/8 校验通过，0 异常 | A×6 + C_fallback×2 + C_direct×4 |
| 真实环境 | 8 | **8/8 全部通过** | A×3 + C_fallback×1 + C_direct×4 |

- 正常模式模板命中平均 **2.1s**；高性能模式云端直出平均约 43s
- 发现的改进项：qa 未命中模板时 C_fallback 双调用耗时 110s（建议直走 C_direct）；R01 触发安全拦截属守卫正常行为

### 11.3 提示词模板固化

每个功能仅保留一份 A/C 方案模板（`services/mode_router.py`），测试时只替换输入参数。4 个功能的完整模板与 HTTP 请求格式见 [测试方案.md](file:///c:/Users/13425/Desktop/个人健康助手/health/ai_service/test_output/single_user_test/测试方案.md)。

### 11.4 其他测试产物

- `test_output/ai_3plans/`（chat/diet/food_recommend/exercise 三方案对比 + comparison_report.md）
- `test_output/article_3plans/`（B/C 方案文章生成对比 + comparison_3plans.md）
- `test_output/llm_compare/`（本地 vs 云端对比）
- 单元测试：`tests/unit/`（test_arch_services / test_local_fallback / test_nlu_parser / test_quality_scorer）+ `test_mode_router.py` / `test_kb_dedup.py` / `test_validators.py` / `test_dynamic_topk.py` / `test_ollama_model.py`（17 项聚类/合并/争议单测通过记录见 `test_arch_services.py`）

---

## 十二、后端架构（Spring Boot 2.7，8082）

### 12.1 14 个 Controller（真实清单）

| Controller | 职责 |
|-----------|------|
| `AuthController` | 注册/登录/JWT |
| `ProfileController` | 用户健康档案（gender/age/height/weight/crowd_type 等约束校验） |
| `DietController` | 饮食记录 |
| `FoodController` | 食物库（9 大类，生/熟标注，visibility 字段控显示） |
| `RecipeController` / `RecipeUserController` | 菜谱库 / 用户菜谱 |
| `ArticleController` | 科普文章（三梯度） |
| `AdminController` | 管理后台（AI 评分仅存库，不在用户界面显示） |
| `CommunityController` | 社区 |
| `ExerciseRecordController` | 运动记录 |
| `NutritionReportController` | 营养报告（BMR/宏量微量/建议） |
| `BodyMetricsHistoryController` | 体测历史 |
| `FamilyRelationController` | 家庭关系 |
| `AiConsultController` | AI 服务转发：/consult、/consult/stream（SseEmitter）、/voice/parse、/nutrition/analyze、/food/audit、/report/weekly、/article/generate、/meal/parse、/diet/plan、/generate-recipe、/recipe/recommend、/exercise/advice |

### 12.2 技术要点

- SQLite + Hibernate（`SQLiteDialect`，ddl-auto: update 自动建表，时间戳用空格分隔无毫秒格式）
- JWT 鉴权，RestTemplate 调 AI 服务（5s 连接 / 30s 读取超时 + 断路器：3 次失败 → 60s 冷却）

---

## 十三、前端架构（Vue 3 + Vite，5173）

### 13.1 页面（29 个 .vue）

- 入口：`App.vue` / `Login.vue` / `Register.vue` / `ForgotPassword.vue` / `Home.vue` / `Dashboard.vue` / `Admin.vue`
- 功能页（`views/dashboard/`）：AiConsult（AI 咨询，含流式渲染/模式标识）、DietaryProfile、FoodInput、FoodSearch、FoodAdd、Nutrition、HealthReport、HealthHistory、MetricsHistory、MuscleChart、RecipeLibrary、RecipeDetail、TrainingPlan、Articles、ArticleDetail、Profile、NearGym、FamilyRelation、FamilyFoodInput、IconGallery
- 组件：`HealthHeatmap.vue`（健康热力图）、`FeatureSlider.vue`

### 13.2 关键交互

- AI 咨询：对话 + SSE 流式渲染 + `_meta.mode/route` 模式标识展示
- 科普文章：主题卡片 → 三篇幅选项 → 详情页（TOC 提取、术语标注、目录编号规范）

---

## 十四、一键启动与工具链

### 14.1 start_all.ps1（真实实现）

一键启动三服务（AI 8002 / 后端 8082 / 前端 5173），含本地 Ollama + 云端 DeepSeek 双链路自检与健康检查。

### 14.2 其他工具

- `setup_ollama_model.py`：Ollama 模型准备（qwen2.5:7b-instruct-q4_K_M，`Modelfile.health_framework` 自定义模型）
- `ollama_service.py`：Ollama 服务封装
- `dashboard/index.html`：监控看板（知识库/调用统计）
- `utils/`：sqlite_utils（WAL + busy_timeout）、cache_utils、json_utils、log_config、quality_scorer、response_utils、retrieval_utils、retry_utils

---

## 十五、数据存储

| 库 | 用途 |
|----|------|
| `data/templates.db` | ai_templates 权威镜像（1400 模板双写对账） |
| `data/food.db` | 食物营养数据 |
| `data/conversations.db` | 对话存储 |
| `data/token_usage.db` | token 用量记录 |
| `data/user_memory.db` | 用户记忆 |
| `data/cache.db` | 缓存 |
| `knowledge/chroma_db_storage` | ChromaDB 向量库 |
| `backend-health/data/health.db` | 后端业务库（用户/饮食/运动/营养报告/文章） |

---

## 十六、关键设计决策与约束（真实执行中）

| 约束 | 说明 |
|------|------|
| 双模型边界 | B 流水线仅用于带固定标签的长篇科普文章（本地只做骨架）；结构化 JSON 与短问答走模板召回→本地改写或 C 方案 |
| 高性能模式 | `high_performance=true` 直连云端 C 方案，跳过本地校验（LLM_TIMEOUT_HIGH_PERF=45） |
| 知识库纯净化 | 向量库只存权威原始资料与生成模板；云端结果回流需相似度 ≥0.8 去重 |
| 文献溯源 | 只存真实 PMID（事实校验器验证），争议文献打标不删除 |
| SQLite 规范 | 统一 `utils/sqlite_utils.get_conn`（WAL + busy_timeout=5000），JSON 属性名与 Java 字段一致 |
| 建表方式 | 保持自动建表（ddl-auto: update） |
| 检索口径 | 统一走 `services/retrieval_service.py`，Agent 不再自行检索 |
| 密钥隔离 | DeepSeek API 密钥存 `.env`，禁止上传代码 |
| 爬虫红线 | 白名单域名、robots 协议、限速、只存权威公开摘要 |
| 数值校验 | 营养素对照《中国居民膳食指南 2022》超标拦截修正 |

---

## 十七、与既有文档的衔接

| 文档 | 关系 |
|------|------|
| [项目当前架构说明.md](项目当前架构说明.md) | 当前代码实况架构（与本文档目标架构互为对照） |
| [AI模块架构设计文档.md](AI模块架构设计文档.md) | AICore 设计 |
| [后端系统架构设计文档.md](后端系统架构设计文档.md) | 后端设计 |
| [前端应用架构设计文档.md](前端应用架构设计文档.md) | 前端设计 |
| [数据库设计说明.md](数据库设计说明.md) | 数据模型 |
| [SQL建表演进报告.md](backend-health/SQL建表演进报告.md) | 建表演进 |
| [启动说明.md](启动说明.md) | 启动流程与环境要求 |

---

## 十八、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-08 | v1.0-v3.1 | 目标架构演进（规划态，含 ⭐ 未落地/◎ 预埋标注） |
| 2026-08-09 | **v4.0** | **重写为真实实现版**：删除全部规划/预埋未落地内容；按实际代码盘点收录 39 端点、11 服务组件、13 Agent 文件 + 4 校验器、14 后端 Controller、29 前端页面；新增多进程模板生成（`--workers`/`--no-cloud`）、知识库采集参数化（`--topics-file`/`--max-results`）、异步任务服务、SSE 流式、Ollama 管理接口、单用户测试体系（脚本测试 + 真实环境测试双线 12+8 用例）及实测数据 |
