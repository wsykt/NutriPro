# 个人健康助手 — 架构与功能实现说明

> 最后更新：2026-08-05 | 基于项目实际代码统计

## 一、项目结构

```
个人健康助手/
├── health/                          # 主项目
│   ├── backend-health/              # Spring Boot 后端（Java 8）
│   │   ├── src/main/java/com/health/
│   │   │   ├── controller/          # 14 个 REST 控制器（~134 个端点）
│   │   │   ├── service/             # 13 个业务服务
│   │   │   ├── entity/              # 17 个 JPA 实体
│   │   │   ├── repository/          # 17 个数据访问接口
│   │   │   ├── dto/                 # 数据传输对象
│   │   │   ├── config/              # Security / 异常处理 / SQLite 方言
│   │   │   └── security/            # JWT 认证（Filter + Util）
│   │   └── src/test/                # JUnit 5 单元测试（27 个用例）
│   │
│   ├── frontend-health/             # Vue 3 前端（TypeScript）
│   │   └── src/
│   │       ├── views/               # 37 个页面视图（31 个注册路由 + 6 个旧页面）
│   │       │   ├── dashboard/       # 26 个仪表盘子页面
│   │       │   └── *.vue            # 11 个顶级页面
│   │       ├── api/index.ts         # 统一 API 封装（12 个模块，96 个方法）
│   │       ├── router/index.ts      # Hash 路由 + JWT 守卫（31 条路由于）
│   │       └── stores/user.ts       # Pinia 用户状态管理
│   │
│   └── ai_service/                  # Python AI 服务（FastAPI）
│       ├── agent/                   # 13 个 AI Agent
│       ├── vector/                  # BGE 向量嵌入 + ChromaDB 检索
│       ├── llm/router.py            # DeepSeek / Qwen LLM 路由
│       ├── knowledge/               # 权威医学 PDF 文献
│       ├── conversation/            # 对话记忆 + 上下文管理
│       ├── services/                # 免责声明 + 检索服务
│       ├── utils/                   # 缓存 / 日志 / 质量评分 / 重试
│       ├── config/settings.py       # 全局配置
│       └── main.py                  # FastAPI 入口（28 个 REST 端点）
│
├── health-demo/                     # 前端 Demo（独立展示用）
└── 个人健康助手1/                    # 旧版 Demo（保留参考）
```

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                       Vue 3 前端 (5173)                       │
│  Axios → JWT Token → 调用后端 API                             │
└──────────────────────┬───────────────────────────────────────┘
                       │ HTTP REST
┌──────────────────────▼───────────────────────────────────────┐
│              Spring Boot 后端 (8082)                          │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │ Controller  │→│   Service     │→│   Repository      │    │
│  │ (JWT 鉴权)   │  │ (业务逻辑)    │  │ (SQLite JPA)      │    │
│  └─────────────┘  └──────┬───────┘  └──────────────────┘    │
│                          │                                    │
│                AiChatService                                  │
│                (调用 Python AI 服务)                           │
│                RestTemplate 5s连接/30s读取                     │
│                断路器: 3次失败→60s熔断                          │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP REST
┌──────────────────────────▼───────────────────────────────────┐
│              Python AI 服务 (8002)                            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              AgentOrchestrator（智能体编排器）             │ │
│  │  ┌──────────┬──────────┬──────────┬──────────────────┐  │ │
│  │  │营养分析   │食物推荐   │膳食计划   │运动建议          │  │ │
│  │  ├──────────┼──────────┼──────────┼──────────────────┤  │ │
│  │  │食物审核   │周报生成   │文章生成   │NLU 饮食解析       │  │ │
│  │  ├──────────┼──────────┼──────────┼──────────────────┤  │ │
│  │  │健康反思   │语音解析   │检索判断   │问答对话          │  │ │
│  │  ├──────────┼──────────┼──────────┼──────────────────┤  │ │
│  │  │资料搜集   │事实校验   │          │                  │  │ │
│  │  └──────────┴──────────┴──────────┴──────────────────┘  │ │
│  └─────────────────────────────────────────────────────────┘ │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐ │
│  │ 向量检索引擎  │ │  LLM 路由器   │ │  对话记忆存储        │ │
│  │ BGE + Chroma │ │ DeepSeek/Qwen│ │  SQLite              │ │
│  └──────────────┘ └──────────────┘ └──────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 三、后端架构（Spring Boot）

### 技术栈
- Spring Boot 2.7.18 + JPA + SQLite
- Spring Security + JWT 无状态认证
- Java 8（JDK 1.8.0_341）

### 核心 Controller 一览（14 个，~134 个端点）

| Controller | 路径前缀 | 端点数 | 功能 |
|------------|----------|:------:|------|
| `AuthController` | `/api/auth` | 3 | 登录、注册、重置密码，返回 JWT Token |
| `DietController` | `/api/diet` | 4 | 三餐饮食记录 CRUD、营养计算 |
| `FoodController` | `/api/food` | 9 | 食物搜索、分类筛选、用户提交/审核 |
| `RecipeController` | `/api/recipes` | 7 | 食谱搜索、详情、食材查询、CRUD |
| `RecipeUserController` | `/api/recipe` | 8 | 用户视角菜谱：浏览、搜索、标签、收藏管理 |
| `ArticleController` | `/api/articles` | 26 | 科普文章：搜索、浏览、生成、知识库管理、自校正 |
| `AiConsultController` | `/api/ai` | 11 | AI 咨询入口：对话、食谱生成、营养分析、膳食计划等 |
| `ProfileController` | `/api/profile` | 4 | 用户档案：身高体重、过敏原、饮食偏好、快照 |
| `NutritionReportController` | `/api/report` | 5 | 营养报告：BMR、营养评估、建议、范围查询 |
| `FamilyRelationController` | `/api/relation` | 7 | 亲友关系：添加、搜索、确认/拒绝、代理操作 |
| `CommunityController` | `/api/community` | 13 | 社区：帖子、评论、点赞、标签 |
| `ExerciseRecordController` | `/api/exercise` | 9 | 运动记录 CRUD、今日统计、周统计、运动类型 |
| `BodyMetricsHistoryController` | `/api/metrics` | 4 | 身体指标历史记录、范围查询、保存/删除 |
| `AdminController` | `/api/admin` | 41 | 管理后台：用户管理、食物审核、菜谱管理、文章分析、社区审核、运动审核 |

### 业务 Service 一览（13 个）

| Service | 职责 |
|---------|------|
| `AuthService` | 用户注册、登录、密码重置 |
| `ProfileService` | 用户档案、饮食偏好管理 |
| `FoodService` | 食物数据库搜索、分类、审核 |
| `DietService` | 饮食记录、营养汇总计算 |
| `RecipeService` | 菜谱搜索、食材智能替换引擎 |
| `ExerciseRecordService` | 运动记录 CRUD、统计 |
| `NutritionReportService` | 营养报告生成、评估 |
| `BodyMetricsHistoryService` | 身体指标历史管理 |
| `FamilyRelationService` | 亲属关系绑定、代理操作验证 |
| `ArticleService` | 科普文章管理、三版拆分、知识库 |
| `ArticleAnalysisService` | 文章质量分析：评分、问题识别、优化闭环 |
| `CommunityService` | 社区帖子、评论、点赞管理 |
| `AiChatService` | AI 对话编排：调用 Python AI 服务、断路保护 |

### AI 集成关键：AiChatService

```
前端 AiConsult.vue
  → api.ai.consult(question)
    → POST /api/ai/consult
      → AiConsultController.consult()
        → AiChatService.consult(userId, question)
          → 构建健康快照（用户档案 + 今日饮食 + 运动记录 + 7日趋势）
          → POST http://localhost:8002/api/v1/chat
            → 断路器检查（3次失败→60秒熔断）
            → 5秒连接超时 / 30秒读取超时
            → AgentOrchestrator.chat() → DeepSeek LLM
```

### 数据库实体（17 个）

| # | 实体 | 表名 | 说明 |
|---|------|------|------|
| 1 | `User` | `user` | 用户核心：认证、身体指标、饮食偏好 |
| 2 | `Food` | `food` | 食物营养成分库（1500+ 记录） |
| 3 | `DietMeal` | `diet_meal` | 饮食餐食记录 |
| 4 | `DietItem` | `diet_item` | 饮食条目（关联食物与重量） |
| 5 | `ExerciseRecord` | `exercise_record` | 运动记录 |
| 6 | `NutritionReport` | `nutrition_report` | 每日营养评估报告（含快照） |
| 7 | `BodyMetricsHistory` | `body_metrics_history` | 身体指标历史 |
| 8 | `FamilyRelation` | `family_relation` | 亲属关系（监护人-被监护人） |
| 9 | `Recipe` | `recipes` | 菜谱库 |
| 10 | `RecipeIngredient` | `recipe_ingredients` | 菜谱食材清单 |
| 11 | `SavedRecipe` | `saved_recipe` | 用户收藏菜谱 |
| 12 | `Article` | `article` | 科普文章（三版：速读/深度/综述） |
| 13 | `ArticleAnalysis` | `article_analysis` | 文章质量分析记录 |
| 14 | `Post` | `post` | 社区帖子 |
| 15 | `Comment` | `comment` | 帖子评论 |
| 16 | `PostLike` | `post_like` | 帖子点赞 |
| 17 | `AiConversationRecord` | `ai_conversation_record` | AI 对话记录 |

### 食谱智能替换引擎

`RecipeService.java` 实现两级替换：

1. **规则基替换**（基于用户画像）：过敏原匹配 → 饮食限制匹配 → 口味偏好匹配
2. **食物数据库替换**（基于营养成分）：高脂食材（脂肪 >20g/100g）→ 低脂替代、高GI食材（GI >70）→ 低GI替代、高热量食材（热量 >250kcal）→ 低热量替代

食材匹配使用 5 级智能匹配：同义词映射 → 精确匹配 → 去括号匹配 → 包含匹配 → 核心名匹配（如 `姜片→姜`、`生抽→酱油`、`鸡胸肉→鸡胸肉(生)`）

## 四、AI 服务架构（FastAPI）

### Agent 编排器（orchestrator.py）

`AgentOrchestrator` 是 AI 服务的核心调度中心，职责包括：

- **意图识别**：通过 NLU Parser 分析用户输入的意图分类
- **Agent 调度**：根据意图将请求分发给对应的子 Agent
- **知识库检索**：从 ChromaDB 检索相关医学文献片段注入上下文
- **LLM 路由**：根据任务类型选择 DeepSeek 或 Qwen 模型
- **记忆管理**：维护用户对话历史和多轮记忆
- **故障切换**：LLM 不可用时自动降级到本地回退引擎
- **缓存管理**：向量检索与 LLM 响应缓存加速

### Agent 清单（13 个 + 2 个辅助）

| Agent | 触发场景 | 核心能力 |
|-------|---------|---------|
| `question_answer` | 通用健康问答 | 知识库检索 + LLM 生成 |
| `nutrition_analysis` | 营养分析请求 | 对比 DRI 标准，分析热量/蛋白质/脂肪/碳水/纤维/钙/叶酸/DHA |
| `diet_plan` | 膳食计划请求 | 基于用户画像生成结构化三餐方案 |
| `food_recommend` | 菜谱推荐请求 | 根据食材 + 人群 + 目标推荐菜谱 |
| `food_audit` | 食物审核请求 | 审核食物营养标签，检测高糖/高脂/高盐 |
| `exercise_advice` | 运动建议请求 | 基于 BMI + 目标 + 慢性病生成周运动计划 |
| `weekly_report` | 周报生成请求 | 汇总一周饮食运动数据，生成健康周报 |
| `article_generate` | 文章生成请求 | 生成针对特定人群的健康科普文章 |
| `health_reflection` | 健康反思请求 | 分析健康指标，给出改善建议（含风险等级） |
| `nlu_parser` | 饮食描述解析 | 自然语言→结构化营养数据 |
| `voice_text_parse` | 语音录入解析 | 语音转录文本→食物条目标注 |
| `retrieve_judge` | 检索判断 | 评估是否需要检索知识库 |
| `orchestrator` | 编排调度 | 统一入口，协调各 Agent 协作 |
| `search_materials` | 资料搜集（辅助） | LLM 分析缺失角度并联网搜索权威资料 |
| `fact_check` | 事实校验（辅助） | 核查科普母稿中引用是否有素材支撑 |

### 知识库架构

- **向量模型**：`BGE-base-zh-v1.5`（768维中文嵌入，本地运行）
- **向量数据库**：ChromaDB（本地嵌入式部署）
- **知识来源**：PubMed 学术文献、中国膳食指南、糖尿病食养指南、孕产期营养等权威资料
- **混合检索**：向量相似度 + 关键词 BM25 混合检索
- **食物数据库**：中国食物成分表（1500+ 食物记录，覆盖 8 大类）
- **知识管理**：仅存储原始权威材料，AI 生成内容禁止入向量库；来源标注 PubMed/官方指南/权威报告

### 文章生成流水线（双模型）

```
本地 Ollama (qwen2.5:7b)
  → Stage 1: 从知识库提取素材 → 构建文章标签框架
  → Stage 3: 格式校验（标签完整性/字数/顺序）

云端 API (DeepSeek)
  → Stage 2: 基于框架扩充内容 → 外部数据展开
  → 事实校验: 随机抽检（2/10篇）验证数据准确性
```

### API 端点一览（共 28 个）

#### GET 端点（9 个）

| 端点 | 功能 |
|------|------|
| `/health` | 健康检查（服务状态、版本、Agent 统计） |
| `/api/v1/agent/stats` | Agent 运行统计（调用次数、成功率、耗时） |
| `/api/v1/agent/stats/export` | 统计完整导出（JSON） |
| `/api/v1/knowledge/stats` | 知识库统计（文档数、分类分布、来源分布） |
| `/api/v1/knowledge/list` | 知识库文档列表（含内容预览） |
| `/api/v1/knowledge/hot-stat` | RAG 热度统计（热门词、场景分布、知识缺口） |
| `/api/v1/quality/stats` | 回答质量统计概览 |
| `/dashboard` | 可视化面板入口 |
| `/dashboard/{path}` | 可视化面板静态文件服务 |

#### POST 端点（19 个）

| 端点 | 功能 |
|------|------|
| `/api/v1/chat` | AI 对话（核心端点，多轮对话+知识检索+NLU） |
| `/api/v1/meal/parse` | NLU 饮食描述解析 |
| `/api/v1/retrieve` | 知识库混合检索 |
| `/api/v1/nutrition/analyze` | 营养分析 |
| `/api/v1/food/audit` | 食物审核 |
| `/api/v1/voice/parse` | 语音文本解析 |
| `/api/v1/report/weekly-summary` | 周报生成 |
| `/api/v1/article/generate` | 科普文章生成 |
| `/api/v1/diet/plan` | 膳食计划 |
| `/api/v1/food/recommend` | 菜谱推荐 |
| `/api/v1/exercise/advice` | 运动建议 |
| `/api/v1/reflection` | 回答质量反思 |
| `/api/v1/health/reflection` | 健康反思 |
| `/api/v1/quality/score` | 回答质量评分 |
| `/api/v1/knowledge/ingest` | 知识库文档摄入 |
| `/api/v1/knowledge/log-usage` | RAG 使用记录（热度统计） |
| `/api/v1/knowledge/acquire` | 知识库自学习（DuckDuckGo + BeautifulSoup） |
| `/api/v1/agent/search-materials` | 资料搜集 Agent（联网补充） |
| `/api/v1/agent/fact-check` | 事实校验 Agent |

### 容错与降级

- 所有 Agent 均配置离线兜底处理器（`LocalFallbackEngine`）
- LLM 不可用时自动切换本地规则引擎
- 中间件层：请求校验 + 超长文本截断 + TraceID 追踪
- POST 最大文本长度限制：500 字符

## 五、前端架构（Vue 3）

### 技术栈
- Vue 3 + TypeScript + Vite
- Vue Router 4（Hash 模式）+ Pinia 状态管理
- TailwindCSS + ECharts 可视化
- Axios HTTP 客户端（12 个 API 模块，96 个方法）

### 路由结构（31 条路由）

#### 顶级路由（6 条）

```
/                          → Home.vue（品牌介绍落地页）
/login                     → Login.vue
/register                  → Register.vue
/forgot-password           → ForgotPassword.vue
/admin                     → Admin.vue（需认证）
/dashboard                 → Dashboard.vue（主框架，需认证，重定向至 profile）
```

#### Dashboard 子路由 — 功能页面（18 条）

| 路径 | 页面 | 说明 |
|------|------|------|
| `/dashboard/profile` | Profile.vue | 个人档案 |
| `/dashboard/food-input` | FoodInput.vue | 饮食录入 |
| `/dashboard/food-add` | FoodAdd.vue | 添加食物 |
| `/dashboard/nutrition` | Nutrition.vue | 营养分析 |
| `/dashboard/food-search` | FoodSearch.vue | 食物搜索 |
| `/dashboard/family-input` | FamilyFoodInput.vue | 亲友饮食录入 |
| `/dashboard/ai-consult` | AiConsult.vue | AI 健康咨询（含快捷面板） |
| `/dashboard/training-plan` | TrainingPlan.vue | 训练计划 |
| `/dashboard/gym` | NearGym.vue | 附近健身 |
| `/dashboard/family-relation` | FamilyRelation.vue | 亲友关系管理 |
| `/dashboard/metrics-history` | MetricsHistory.vue | 身体指标历史 |
| `/dashboard/health-history` | HealthHistory.vue | 健康历史 |
| `/dashboard/recipe-library` | RecipeLibrary.vue | 菜谱库 |
| `/dashboard/recipe-detail/:id` | RecipeDetail.vue | 菜谱详情+智能替换 |
| `/dashboard/dietary-profile` | DietaryProfile.vue | 饮食画像 |
| `/dashboard/health-report` | HealthReport.vue | 健康报告 |
| `/dashboard/muscle-chart` | MuscleChart.vue | 肌肉分析图 |
| `/dashboard/articles` | Articles.vue | 科普文章 |

#### Dashboard 子路由 — 文章详情（1 条参数路由）

| 路径 | 页面 |
|------|------|
| `/dashboard/article-detail/:id` | ArticleDetail.vue |

#### Dashboard 子路由 — 重定向路由（5 条）

| 路径 | 重定向到 | 说明 |
|------|----------|------|
| `/dashboard/ai-chat` | `/dashboard/ai-consult` | AI 对话已合并 |
| `/dashboard/feature-hub` | `/dashboard/profile` | 功能中心已合并 |
| `/dashboard/health-education` | `/dashboard/articles` | 健康教育已合并 |
| `/dashboard/recipe-search` | `/dashboard/recipe-library` | 菜谱搜索已合并 |
| `/dashboard/community` | `/dashboard/profile` | 社区交流已合并至个人中心 |

#### Dashboard 子路由 — 开发辅助（1 条，不在导航菜单显示）

| 路径 | 页面 |
|------|------|
| `/dashboard/icon-gallery` | IconGallery.vue |

### API 封装（api/index.ts，12 个模块，96 个方法）

| 模块 | 方法数 | 主要功能 |
|------|:------:|----------|
| `auth` | 3 | 登录、注册、重置密码 |
| `food` | 8 | 食物搜索、分类、添加、审核、批量查询 |
| `diet` | 4 | 饮食记录增删查、营养分析 |
| `profile` | 4 | 个人档案、饮食偏好、快照 |
| `admin` | 10 | 用户管理、食物审核、统计 |
| `report` | 5 | 营养报告保存、查询、范围查询 |
| `relation` | 7 | 亲属关系：添加、确认/拒绝、监护人/被监护人列表 |
| `metrics` | 4 | 身体指标历史、范围查询、保存/删除 |
| `ai` | 13 | AI 对话、营养分析、膳食计划、菜谱推荐、运动建议、语音解析、周报、文章、NLU 解析 |
| `recipe` | 10 | 菜谱搜索、详情、收藏管理、CRUD |
| `article` | 14 | 文章浏览、搜索、生成、点赞、三版查询、主题分组 |
| `community` | 12 | 帖子发布/删除、评论、点赞、标签、用户动态/点赞/评论列表 |

### Axios 拦截器机制

| 拦截器 | 功能 |
|--------|------|
| 请求拦截 | 自动注入 Bearer Token；亲属代理模式自动附加 `targetUserId` 参数 |
| 响应拦截（成功） | 剥离 `ApiResponse.data` 包装；检查 `body.code !== 200` 防止业务错误静默吞掉 |
| 响应拦截（错误） | 401/403 自动清除登录态并跳转登录页（开发模式下 `/profile/info` 的 403 豁免） |

### AI 咨询页快捷面板

`AiConsult.vue` 右侧栏提供一键 Agent 功能：

- 营养分析 → `/ai/nutrition/analyze`
- 膳食计划 → `/ai/diet/plan`
- 周报生成 → `/ai/report/weekly`
- 运动建议 → `/ai/exercise/advice`

点击后自动组织对话消息 → 后端 → AI 编排器 → Markdown 格式结果展示。

## 六、关键功能实现流程

### 1. AI 健康咨询

```
用户输入问题
  → 前端 POST /api/ai/consult {question}
  → 后端构建健康快照（用户档案、今日饮食、运动记录、BMI、7日趋势）
  → AiChatService 调用 AI 服务 /api/v1/chat
  → AgentOrchestrator.chat()
      → NLU 意图识别
      → 知识库混合检索（BGE 向量 + BM25）
      → 对话记忆提取（最近 5 轮）
      → LLM 生成回答（DeepSeek）
      → 质量评分 + 免责声明追加
  → 回答 + 会话ID 返回前端
  → 前端 Markdown 渲染展示
```

### 2. 食谱智能替换

```
用户查看食谱详情
  → 前端调用 /api/recipes/{id}/detail
  → 后端 analyzeIngredients() 分析每个食材
      → 规则引擎匹配（过敏原/饮食限制/口味）
      → 食物数据库匹配（高脂/高GI/高热量检出）
      → findBestFoodMatch() 5级匹配
  → 返回食材清单 + 替换建议（含营养成分对比）
  → 前端展示替换前/后营养值（总值 + 每100g值）
  → 用户确认替换 → 前端重新计算营养
```

### 3. 饮食记录与营养分析

```
用户录入饮食（手动/语音/NLU解析）
  → 前端 POST /api/diet/add {mealType, foods[]}
  → 后端计算各餐营养汇总（calorie/protein/fat/carb/fiber/ca/dha/folate）
  → 实时更新 BMR + BMI
  → 对比 DRI 推荐摄入量
  → 生成营养缺口分析
  → 前端 ECharts 雷达图展示
```

### 4. 亲友代管

```
用户添加亲友关系
  → POST /api/relation/add {wardUsername}
  → 被添加方确认 → 关系建立
  → 用户可在饮食录入/健康咨询时切换为目标用户（targetUserId 参数）
  → 后端验证亲属关系后代理操作
  → 前端自动在 localStorage 存储 actAsUserId
```

### 5. 食物搜索与优先级

```
用户搜索食物
  → 后端模糊匹配 foodName + foodCategory
  → 按 4 级优先级排序：
      顶级常用(11) > 常用食物(10) > 一般(5-9) > 冷门(0)
  → 同优先级按分类排序
  → 返回结果含营养显示标记（showGi/showFolicAcid/showDha）
```

### 6. 科普文章生成（A-2 双模型流水线）

```
管理员选择主题与人群
  → 本地 Ollama 构建文章标签框架（Stage 1）
  → 云 API 扩充前沿内容与分层优化（Stage 2）
  → 本地 Ollama 格式校验（Stage 3）
  → 事实校验 Agent 抽检引用准确性
  → 三版拆分：速读卡（500字）/ 深度文（2000字）/ 综述文（4000字）
  → 文章质量分析（5维度：结构/证据/可读性/准确性/受众适配）
  → 生成分析记录 → 管理员审核 → 应用优化（闭环）
  → 前端三层导航：主题卡片 → 篇幅选择 → 文章详情
```

### 7. 安全鉴权流程

```
前端路由守卫（router/index.ts）
  → 开发模式：import.meta.env.DEV → 自动注入 mock token
  → 生产模式：检查 localStorage token

后端 SecurityConfig
  → /api/auth/**、/api/food/list、/api/food/search：公开
  → /api/recipes/** GET：公开，POST/PUT/DELETE：需认证
  → /api/articles/** GET：公开，POST/PUT/DELETE：需认证
  → /api/exercise/types：公开
  → 其余全部接口：需认证
  → /api/admin/**：需 ADMIN 角色
  → 无权限访问返回 403

前端响应拦截器
  → 401/403：清除 token，跳转 /login
  → 开发模式下 /profile/info 的 403 豁免（避免无法预览）
```

## 七、数据库

- **SQLite** 数据库文件：`backend-health/data/health.db`（17 张表）
- **JPA 策略**：`ddl-auto: update`（实体自动建表/更新，无手动 SQL schema）
- **AI 服务辅助数据库**（`ai_service/data/`）：
  - `conversations.db` — 对话记录
  - `user_memory.db` — 用户长期记忆
  - `food.db` — 食物数据副本
  - `cache.db` — 向量检索与 LLM 响应缓存

详细数据库设计见 [数据库设计说明.md](数据库设计说明.md)。

## 八、启动方式

### 一键启动（Windows PowerShell）
```powershell
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1"
```

### 分别启动

**1. 启动 AI 服务（端口 8002）**
```bash
cd health/ai_service
pip install -r requirements.txt
python main.py
```

**2. 启动后端（端口 8082）**
```bash
# 设置 Java 环境 + 使用 IntelliJ IDEA 内置 Maven
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_341"
& "C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.3\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run
```

**3. 启动前端（端口 5173）**
```bash
cd health/frontend-health
npm install
npm run dev
```

## 九、测试

- **Java 单元测试**：`backend-health/src/test/`，27 个测试用例
  - `RecipeServiceTest`（20 个）— 食材匹配 5 级算法
  - `AiChatServiceTest`（7 个）— 断路器、用户校验、边界条件
- **Python AI 测试**：`ai_service/tests/`（pytest）+ `scripts/`（50+ 测试脚本）
- **运行测试**：
```bash
# Java 测试
cd health/backend-health
mvn test

# Python 测试
cd health/ai_service
python scripts/test_all_agents.py
```

## 十、AI 咨询双模式路由与模板知识库流水线（2026-08-07 新增）

> 针对 4 个 AI 咨询功能（健康问答 / 一日饮食方案 / 菜谱推荐 / 个性化运动）新增**正常模式 vs 高性能模式**双路由，并构建完整的**模板批量生成 → 本地草稿 → 云端润色 → 逐条入库 → 自增长闭环**流水线。

### 10.1 双模式路由（ModeRouter）

入口：`ai_service/services/mode_router.py` → `ModeRouter.route(func_type, high_performance, **kwargs)`

| 模式 | `high_performance` | 流水线 | 适用场景 |
|------|---------------------|--------|----------|
| **正常模式** | `false`（默认） | **A 方案优先**：向量召回匹配人群+BMI+方向的模板 → 本地 Ollama (qwen2.5-7b) 改写 → 领域规则校验；<br>**校验不通过则自动回退 C 方案**：云端 DeepSeek 重生成 | 真实用户使用：省钱、响应可接受 |
| **高性能模式** | `true` | **直接 C 方案**：云端 DeepSeek 生成，跳过本地校验 + 模板召回 | 演示/对速度敏感 |

正常模式路径：
```
用户请求 (func_type + user_profile + goal...)
    │
    ├─ [Stage 1] 向量库召回 top_k=3 模板（按 target_crowd + bmi_id 预过滤）
    │       有模板 → A 方案 本地改写
    │       无模板 → 走本地兜底规则引擎
    │
    ├─ [Stage 2] 领域规则校验（diet_plan/food_recommend/exercise/qa 独立 Validator）
    │       通过  → 返回 (route=A_template_local)
    │       不通过 → C 方案云端重生成 (route=C_fallback)
    │
    └─ [Stage 3] 知识库自增长闭环（见 10.3）
             C_direct / C_fallback 结果都回写 ChromaDB
             相似主题用高性能模式云端合并；新主题直接新增
```

### 10.2 模板矩阵与批量生成脚本

**矩阵规模**（默认配置）：

```
7 人群 × 5 BMI 档 × 4 功能 × 10 模板/格 = 1400 条
├─ 人群：普通人 / 孕妇 / 健身人群 / 老年人 / 青少年 / 糖尿病患者 / 通用
├─ BMI：very_low(<18.5) / low(18.5-20) / normal(18.5-24) / high(24-28) / very_high(≥28)
├─ 功能：qa / diet_plan / food_recommend / exercise
└─ 方向：增重 / 减脂 / 控糖 / 增肌 / 保持健康 / 改善代谢 / 关节友好 …（每类轮选 8 个主诉）
```

每条模板的 ChromaDB metadata 完整标注（检索过滤用）：
```json
{
  "template_type": "ai_template",
  "func_type": "qa / diet_plan / food_recommend / exercise",
  "target_crowd": "老年人",
  "crowd": "老年人",
  "bmi_id":   "very_high",
  "bmi_cn":   "超高",
  "direction":"减脂",
  "category": "nutrition_standard（兼容旧过滤）",
  "topic":    "老年人-BMI超高-减脂",
  "source_channel": "ai_template_generator_v1",
  "card_id":  "tpl_老年人_very_high_diet_plan_05_a1b2c3d4",
  "seed_index": "05",
  "ingest_time": "2026-08-07 22:19:15"
}
```

**脚本入口**：`ai_service/generate_kb_templates.py`

```bash
# 1) 仅生成任务清单（预演）
python generate_kb_templates.py --dry-run \
    --crowd 普通人 --bmi normal,very_high --func qa,diet_plan --per-group 2

# 2) 真实生成（多进程分人群跑；每人群独立 checkpoint）
python generate_kb_templates.py --crowd 普通人 --per-group 10 \
    --resume ./ckp_普通人.json --output ./kb_out_普通人.json --no-ingest

# 3) 合并 7 个 kb_out_*.json → 总文件 kb_templates_1400_merged.json
```

**两步生成质量保证**：
1. **本地 Ollama qwen2.5-7b 草稿**：按 Prompt 填结构化 JSON（含 `final_content` 字段）
2. **云端 DeepSeek 完善**：对草稿做数值合理性、营养范围、人群专属建议补全，标记 `polished_by=cloud_polished`

### 10.3 模板知识库逐条入库流水线（用户要求的"一个一个塞进去"）

脚本参数：`generate_kb_templates.py --ingest-only` 新增能力

| 参数 | 默认 | 作用 |
|------|------|------|
| `--ingest-only <file.json>` | - | 只执行入库（不再跑模型），文件格式 `{"templates":[{task, final_content, ...}, ...]}` 或 list |
| `--ingest-batch-size N` | **1**（逐条） | N=1：每条外层 try/except 单条隔离；N>1：批量失败自动回退逐条重试 |
| `--ingest-resume <ckp.json>` | 自动 `*_ingest_ckp.json` | 断点续跑：读取 completed_ids set 跳过已入库，成功每 10 条 flush 到磁盘 |
| checkpoint 文件 | `*.ingest_ckp.json` | 保存 `{completed_ids:[...], failed_ids:[{id,error}]}`，下次续跑自动跳过成功 |

```
单条模式关键边界（N=1）：
  for each template:
    build_document_and_metadata → (doc, meta, card_id)
    try:
        retriever.add(documents=[doc], metadatas=[meta], ids=[card_id])
        → 成功：completed_ids.add(id)
        → 失败：failed_ids.append({id, error[:500]})，日志打 WARN 不阻塞后续
    flush checkpoint every 10 ok / end of batch
```

**典型命令**：
```bash
# 推荐（逐条 + 自动续跑）
python generate_kb_templates.py \
    --ingest-only ./kb_templates_1400_merged.json \
    --ingest-batch-size 1

# 中途断了，下次自动续：
python generate_kb_templates.py \
    --ingest-only ./kb_templates_1400_merged.json \
    --ingest-resume ./kb_templates_1400_merged_ingest_ckp.json
```

### 10.4 知识库自增长（C 方案结果自动入库）

触发点：`ModeRouter.route` 两个 C 方案返回分支（`C_direct` / `C_fallback`）

入口：`_ingest_c_result(func_type, result, trigger_route, **kwargs)`

流程：
```
拿到 C 方案生成的结构化结果
  │
  ├─ 1) 空值/缺字段过滤（直接跳过）
  ├─ 2) 从 kwargs 提取 target_crowd、BMI 等级，抽取主诉方向
  ├─ 3) 构造 Match Query（人群+BMI+方向关键词），向量检索 top_k=1
  │      │
  │      ├─ similarity ≥ DUP_SIMILARITY_THRESHOLD (0.8)
  │      │      → 主题重复：再调一次高性能模式云端
  │      │          把「库内旧模板内容 + 新C方案结果」合并去重
  │      │          产出 [整合版] 新文档入库，metadata 标注 merged_from=<old_card_id>
  │      │
  │      └─ similarity < 0.8 → 全新主题
  │             → 按标准模板格式构造 (document + 完整 metadata) 直接入库
  │
  └─ 4) 全部外层 try/except：入库异常吞掉写 WARN 日志，**绝不阻塞用户响应**
```

相关环境变量：
- `KB_AUTO_INGEST_C_RESULTS=true`（默认开）— 总开关
- `KB_DUP_SIMILARITY_THRESHOLD=0.8` — 相似度阈值，可调

### 10.5 API 层对外暴露

`main.py` 4 个咨询端点 + 对话端点全部新增 `high_performance: bool = False` Query 参数：

```
POST /api/v1/chat?high_performance=true           # AI 对话
POST /api/v1/diet/plan?high_performance=false     # 一日饮食方案
POST /api/v1/food/recommend?high_performance=true # 菜谱推荐
POST /api/v1/exercise/advice?high_performance=... # 个性化运动方案
```

`AgentOrchestrator.process(...)` 已透传：`high_performance` 不参与缓存键，同一输入在两种模式下分别执行。

### 10.6 新增/改动文件清单（2026-08-07 本轮）

| 文件 | 变更 |
|------|------|
| `ai_service/generate_kb_templates.py` | 模板生成主脚本；新增 `--ingest-batch-size` / `--ingest-resume`；`ingest_to_chroma` 改为逐条 try/except + checkpoint |
| `ai_service/services/mode_router.py` | ModeRouter：新增 `route` 双模式、`_ingest_c_result` 自增长、4 个 Validator |
| `ai_service/agent/orchestrator.py` | 注入 mode_router；透传 `high_performance`；缓存键排除该参数 |
| `ai_service/config/settings.py` | 新增 `KB_AUTO_INGEST_C_RESULTS` / `KB_DUP_SIMILARITY_THRESHOLD` 环境变量 |
| `ai_service/llm/router.py` | 修复 JSON 字符串内嵌 `\n` 导致解析失败：新增 `_escape_newlines_in_json_strings` 逐字符状态机 |
| `ai_service/vector/retriever.py` | ChromaRetriever：BGE 嵌入 + BM25；`target_crowd` 预过滤 where 子句 |
| `ai_service/test_mode_router.py` | 10 个模式路由单测（PASS 10/10） |

### 10.7 知识库去重与存储优化（2026-08-08 本轮）

> 针对上一轮自增长逻辑暴露的问题（主题堆积、内容膨胀、过度精简导致大模型歧义）进行系统性优化，核心是 **四层去重漏斗 + 双层存储 + 本地相关性校验 + 单篇扩充至 ~500 字**。

#### 10.7.1 四层去重漏斗（services/kb_dedup_service.py 新增）

```
新C方案结果
  │
  ├─ 层1 元数据硬过滤
  │      func_type + 人群(KB长名) + BMI档位 全部一致才视为候选
  │      （不一致 → 直接走「新增」）
  │
  ├─ 层2 语义相似度判定
  │      BGE 向量余弦相似度（替代旧 Jaccard 字符集方法）
  │      检索 top_k=5 候选，取最高相似度
  │
  ├─ 层3 分级合并策略
  │      sim ≥ 0.95   → REJECT       完全重复：跳过入库，仅更新命中
  │      sim ≥ 0.75   → CLOUD_MERGE  高相似：调云端把新旧整合为 [整合版]
  │      sim ≥ 0.50   → MARK_VARIANT 中相似：新增但标记 variant_of=原主题
  │      其余          → INGEST_NEW  低相似：按标准格式直接新增
  │
  └─ 层4 双层存储入库（retriever.add_with_backup）
         精简展示版（≤200字，用于向量检索）
         完整备份版（全文存入 metadata.full_content，供改写/分析保留核心结论原文）
```

设计原则：
- **比赛阶段不删除冷数据**（`KB_AUTO_COLD_CLEANUP=false` 默认关），仅保留清理接口，保证知识库完善性
- **核心结论保留原文**：数据性/结论性语句（推荐量、安全阈值、禁忌事项）不做模糊改写，降低下游大模型二次推断的歧义风险
- 原 `_ingest_c_result` 逻辑降级保留为 `_ingest_c_result_legacy`，去重服务异常时自动回退

#### 10.7.2 本地大模型相关性校验

- 开关：`KB_LOCAL_RELEVANCE_CHECK=true`（默认开）
- 位置：`ModeRouter.route` 三个返回分支（C_direct / A_template_local / C_fallback）统一调用 `_apply_relevance_check`
- 机制：用本地 Ollama（`check_relevance_with_local_llm`）判断「用户问题 ↔ 生成回答」是否相关，返回 `{relevant, confidence, reason}`
- 结果注入 `resp.validation.relevance`，供前端展示；判定不相关仅记 WARN 日志，**绝不阻断用户响应**
- 校验异常/无 LLM/问题过短 → 默认通过（安全降级）

#### 10.7.3 知识库内容长度调整

| 配置项 | 旧值 | 新值 | 说明 |
|--------|------|------|------|
| `KB_CONTENT_MAX_LENGTH` | - | 500 | 单篇内容上限（字） |
| `KB_CONTENT_DISPLAY_LENGTH` | - | 200 | 精简展示版长度 |
| 模板生成 prompt | 150-400字 | 200-400字（本地初稿） | 本地 Ollama 初稿 |
| 云端完善 prompt | 250-500字 | 400-600字（目标~500字） | DeepSeek 润色，且要求核心结论保留原文表述 |

#### 10.7.4 新增/修改文件清单（2026-08-08 本轮）

| 文件 | 变更 |
|------|------|
| `ai_service/services/kb_dedup_service.py` | **新增**：四层去重服务（MergeStrategy / DedupResult / KBDedupService），含 `check_and_ingest` / `_find_candidates` / `_split_dual_layer` / `check_relevance_with_local_llm` |
| `ai_service/services/mode_router.py` | 集成去重服务；新增 `_apply_relevance_check` / `_build_user_question_for_relevance`；`_ingest_c_result` 优先走四层去重，异常降级旧逻辑 |
| `ai_service/vector/retriever.py` | 相似度改为 BGE 向量余弦；新增 `add_with_backup` / `get_full_content` 双层存储方法 |
| `ai_service/config/settings.py` | 新增 9 个配置：`KB_DEDUP_ENABLED` / `KB_DEDUP_HIGH_THRESHOLD`(0.75) / `KB_DEDUP_MEDIUM_THRESHOLD`(0.50) / `KB_DEDUP_TOP_K_CANDIDATES`(5) / `KB_CONTENT_MAX_LENGTH`(500) / `KB_CONTENT_DISPLAY_LENGTH`(200) / `KB_DUAL_LAYER_STORAGE` / `KB_AUTO_COLD_CLEANUP`(false) / `KB_LOCAL_RELEVANCE_CHECK` |
| `ai_service/generate_kb_templates.py` | prompt 长度调整 + 核心结论保留原文要求 |
| `ai_service/test_kb_dedup.py` | **新增**：35 个单测（去重策略/分级合并/双层存储/相关性校验/路由集成），全部 PASS |
| `ai_service/test_mode_router.py` | 10 个路由单测保持 PASS（关闭相关性开关，聚焦路由分发） |

> 注：10.4 中旧阈值 `KB_DUP_SIMILARITY_THRESHOLD=0.8` 已被新四层去重替代（硬过滤 + 0.75/0.50 分级）；该环境变量保留用于旧逻辑降级兜底，默认 0.45。

### 10.8 权威文献扩充与 Agent 校验组件拆分（2026-08-08 本轮）

> 四个方向：① 扩充 PubMed/PMC/Trip-Database 权威文献素材；② 模板生成脚本持续扩充（1400 份目标）；③ 四层去重机制完善；④ Agent 职责拆分（事实校验/数值检查/安全拦截独立组件）。

#### 10.8.1 权威文献素材扩充（crawler/literature_enrich_v2.py 改造）

| 数据源 | 实现 | 说明 |
|--------|------|------|
| PubMed | NCBI esearch(db=pubmed) + efetch | 真实 PMID 可追溯，id=`PMID_xxx` |
| EuropePMC | REST API | 既有来源，保留 |
| PMC 直接抓取 | NCBI esearch(db=pmc) + efetch，XML 解析 | **本轮新增**，id=`PMC_xxx`，source_channel="PMC" |
| Trip Database | articles/search API | **本轮新增**，id=`TRIP_xxx`，需 `TRIP_DATABASE_API_KEY`，未配置时优雅降级（仅日志，返回空结果） |
| 官方指南 | 既有抓取 | 保留 |

增强点：
- 全部数据源遵守 **0.5s 请求限速**；新增 `--skip-pmc-direct` / `--skip-trip` 参数可跳过单源
- 入库统一走**文献专用四层去重** `KBDedupService.check_literature_ingest()`：
  - 层1：`source_type=="literature"` + group+topic 精确匹配
  - 层3：sim ≥ high(0.95) → **REJECT**（文献为单一来源可追溯，高相似直接丢弃）；sim ≥ medium(0.75) → 标记 `variant_of`/`variant_similarity` 入库；其余新增
  - 层4：`add_with_backup` 双层存储（展示版 ≤200 字 + 完整版存 metadata.full_content）
- 移除旧的 `title_similarity` / `deduplicate_cards` 简单字符串去重，统一走向量语义去重

#### 10.8.2 Agent 校验组件拆分（agent/validators/ 新增 4 组件）

将原本耦合在 orchestrator 的事实/数值/安全逻辑拆成**独立可复用校验组件**，统一由 `ValidatorPipeline` 串联，供 orchestrator 在后置校验层调用：

```
safety(安全拦截) → numeric(数值检查) → fact(事实校验) → 汇总(issues/level/blocked)
```

| 组件 | 文件 | 职责 |
|------|------|------|
| 安全拦截 | `safety_interceptor.py` | 高危模式 BLOCK（极端断食/禁食/危险药物/医疗越界停药/用X治癌）；人群禁忌（孕妇/糖尿病/高血压/老年/青少年/心血管）；医疗边界 WARN（诊断/处方/开药）；免责声明缺失 INFO |
| 数值检查 | `numeric_checker.py` | 规则表驱动零 LLM：JSON 嵌套路径（含 `*` 通配符）区间校验、中文文本数值抽取（盐/水/蛋白质/运动时长）、宏量营养素供能比（蛋白 10-20% / 碳水 50-65% / 脂肪 20-30%，三项全失衡升级 high） |
| 事实校验 | `fact_checker.py` | 绝对化用语（100%/保证/根治/完全治愈）、无知识库依据的结论性声明（数字共现启发式）、可选 LLM 深度校验（异常降级通过） |
| 统一流水线 | `validator.py` | 三组件串联；仅 `safety level==block` 才阻断（BLOCK 时 orchestrator 替换为安全响应）；数值/事实问题附加 `_meta.validation` 不阻断；仅 info 级问题（缺免责声明）不升级告警级别 |

集成点（orchestrator.py）：
- `_validate_and_guard()`：四个核心健康功能（qa/diet_plan/food_recommend/exercise）后置校验
- `_guard_qa_response()`：qa 文本 BLOCK 时替换为安全提示
- 开关 `VALIDATOR_ENABLED`（默认 true）、`VALIDATOR_BLOCK_SAFETY`（默认 true）
- 高危人群策略：**孕妇 / 糖尿病患者命中任何禁忌规则直接 BLOCK**（安全优先），其余人群 WARN

#### 10.8.3 模板库与去重验证

- 1400 份模板（7人群 × 5档BMI × 4功能 × 10套）已全部入库验证，无缺口（ChromaDB 当前 4605 条：1400 模板 + 1076 文献 + 其他）
- 生成脚本 `generate_kb_templates.py` 支持断点续跑、失败清单、分人群并行（checkpoint + 日志逐人群维护）

#### 10.8.4 测试覆盖

| 测试文件 | 数量 | 结果 |
|----------|------|------|
| `test_validators.py` | 40 | 全 PASS（事实8 / 数值11 / 安全11 / 流水线6 / orchestrator 集成4） |
| `test_kb_dedup.py` | 39 | 全 PASS（无回归） |

本轮修复的组件问题：`_get_nested` 通配符展开丢失容器身份、中文单位（克/毫升）正则不匹配、宏量营养素全失衡未升级 high、orchestrator 缺 logger、孕妇/糖尿病禁忌未达 BLOCK 级。

### 10.9 SQLite 统一存储与稳定性优化（2026-08-08 本轮）

> 目标：① 为项目内全部 SQLite 库统一 WAL + busy_timeout + 索引，解决并发写入 `database is locked`；② 新建 `ai_templates` 模板表作为 ChromaDB 的**权威镜像副本**，实现双写一致性（ChromaDB 写模板卡片时同步写 SQLite）。

#### 10.9.1 稳定性优化：SQLite 连接统一封装（utils/sqlite_utils.py 新增）

```
get_conn(db_path, busy_timeout_ms=5000)
  ├─ PRAGMA journal_mode=WAL          # 读写并发，避免 database is locked
  ├─ PRAGMA busy_timeout=5000         # 写锁冲突时等待而非直接抛异常
  └─ 自动创建父目录

init_db(db_path, ddl_statements, indexes)  # 幂等建表 + 建索引
```

接入点（4 处 SQLite 库全部统一）：

| 库 | 文件 | 优化 |
|----|------|------|
| `data/cache.db` | `utils/cache_utils.py` | 此前已具备 WAL + busy_timeout + 索引 |
| `data/conversations.db` | `conversation/store.py` | **本轮**：WAL + busy_timeout + `conversation_id` / `user_id` 索引 |
| `data/user_memory.db` | `conversation/memory_extract.py` | **本轮**：WAL + busy_timeout |
| `data/token_usage.db` | `llm/router.py` | **本轮**：WAL + busy_timeout |

#### 10.9.2 SQLite 模板表 + 双写一致性（services/template_store.py 新增）

表结构 `ai_templates`：

| 字段 | 说明 |
|------|------|
| `id` | ChromaDB card_id（主键） |
| `func_type` | qa / diet_plan / food_recommend / exercise |
| `target_crowd` | KB 长名（普通人/孕妇/糖尿病患者/...） |
| `bmi_id` | very_low / low / normal / high / very_high |
| `direction` | 目标方向（增肌/减脂/...） |
| `content` | 精简展示版（与 ChromaDB document 一致） |
| `full_content` | 完整备份版（可能为空） |
| `metadata_json` | 完整 metadata（JSON 序列化） |
| `ingest_time` | 入库时间 |

双写链路：

```
retriever.add(documents, metadatas, ids)
  ├─ 1) ChromaDB 向量入库（原有）
  ├─ 2) BM25 索引重建（原有）
  └─ 3) _dual_write_templates()  ← 新增
         metadata.template_type == "ai_template" 的卡片
         → template_store.upsert(id, content, meta, full_content)
         （失败仅 WARN，绝不影响主流程）
```

- `add_with_backup`（双层存储）已自动纳入双写（内部调用 `add`）
- 启动对账 `reconcile(retriever)`：以 ChromaDB 为准回补 SQLite 缺失的 ai_template 记录（单向回补；SQLite 有而 ChromaDB 无的记录只统计不删除，比赛阶段不删冷数据）
- 支持离线查询：`list(func_type, target_crowd, bmi_id, direction)` 便于管理与审计
- 真实库对账结果：ChromaDB 1400 条模板 → SQLite 回补 1400 条，双侧一致

配置：`TEMPLATES_DB_PATH=./data/templates.db`

#### 10.9.3 稳定性优化：LLM 超时放宽

- `LLM_TIMEOUT_HIGH_PERF=45`（高性能演示模式云端超时从 30s 放宽至 45s），`ModeRouter.route` 高性能分支已接入
- `LLM_TIMEOUT=30`（正常模式）保持不变
- Ollama 本地超时 `OLLAMA_TIMEOUT=180` 保持不变

#### 10.9.4 新增/修改文件清单

| 文件 | 变更 |
|------|------|
| `ai_service/utils/sqlite_utils.py` | **新增**：SQLite 连接统一封装（WAL + busy_timeout + init_db） |
| `ai_service/services/template_store.py` | **新增**：ai_templates 模板表（upsert/get/list/count/delete/reconcile） |
| `ai_service/vector/retriever.py` | `add` 增加 `_dual_write_templates` 双写钩子 |
| `ai_service/conversation/store.py` | 接入 get_conn + 索引 |
| `ai_service/conversation/memory_extract.py` | 接入 get_conn |
| `ai_service/llm/router.py` | TokenTracker 接入 get_conn |
| `ai_service/config/settings.py` | 新增 `TEMPLATES_DB_PATH` |
| `ai_service/main.py` | 启动生命周期增加模板库对账 |

测试：`test_mode_router.py` + `test_kb_dedup.py` 共 49 例全 PASS；模板库双写与对账单独验证通过。

---

## 十一、本次会话修复汇总（2026-08-05）

本轮针对[项目缺陷检查报告](项目缺陷检查报告.md)中识别的 **6 项严重/中等缺陷**进行了代码修复：

| 编号 | 严重度 | 修复内容 | 文件 |
|:----:|:------:|----------|------|
| C1 | 严重 | 收紧 SecurityConfig：recipes/articles 的 GET 公开、写操作需认证 | `SecurityConfig.java` |
| C2 | 严重 | `const DEV_MODE = true` → `import.meta.env.DEV`，生产环境自动禁用模拟登录 | `router/index.ts` |
| C3 | 严重 | 补充前端 `api.community.*` 12 个方法，修复 Community.vue 报错 | `api/index.ts` |
| C4 | 严重 | 响应拦截器增加 `body.code !== 200` 检查，防止业务错误静默吞掉 | `api/index.ts` |
| C5 | 严重 | 新增 `AccessDeniedException` 处理，返回 403 而非 500 | `GlobalExceptionHandler.java` |
| M4 | 中等 | NPE 异常 HTTP 状态 401 → 500（NPE 不是鉴权问题） | `GlobalExceptionHandler.java` |
