# 个人健康助手 — 架构与功能实现说明

## 一、项目结构

```
个人健康助手/
├── health/                          # 主项目
│   ├── backend-health/              # Spring Boot 后端（Java 8）
│   │   ├── src/main/java/com/health/
│   │   │   ├── controller/          # 13 个 REST 控制器
│   │   │   ├── service/             # 11 个业务服务
│   │   │   ├── entity/              # 15 个 JPA 实体
│   │   │   ├── repository/          # 14 个数据访问接口
│   │   │   ├── dto/                 # 6 个数据传输对象
│   │   │   ├── config/              # Security / 异常处理 / SQLite方言
│   │   │   └── security/            # JWT 认证（Filter + Util）
│   │   └── src/test/                # JUnit 5 单元测试（27 个用例）
│   │
│   ├── frontend-health/             # Vue 3 前端（TypeScript）
│   │   └── src/
│   │       ├── views/               # 28 个页面视图
│   │       │   ├── dashboard/       # 17 个仪表盘子页面
│   │       │   └── *.vue            # 11 个独立页面
│   │       ├── api/index.ts         # 统一 API 封装（10 个模块）
│   │       ├── router/index.ts      # Hash 路由 + JWT 守卫
│   │       └── stores/user.ts       # Pinia 用户状态管理
│   │
│   └── ai_service/                  # Python AI 服务（FastAPI）
│       ├── agent/                   # 13 个 AI Agent
│       ├── vector/                  # BGE 向量嵌入 + ChromaDB 检索
│       ├── llm/router.py            # DeepSeek / Qwen LLM 路由
│       ├── knowledge/pdfs/          # 11 份权威医学 PDF 文献
│       ├── conversation/            # 对话记忆 + 上下文管理
│       ├── services/                # 免责声明 + 检索服务
│       ├── utils/                   # 缓存 / 日志 / 质量评分 / 重试
│       ├── config/settings.py       # 全局配置
│       └── main.py                  # FastAPI 入口（25 个 REST 端点）
│
├── health-demo/                     # 前端 Demo（独立展示用）
└── 个人健康助手1/                    # 旧版 Demo（保留参考）
```

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                       Vue 3 前端 (8080)                       │
│  Axios → JWT Token → 调用后端 API                             │
└──────────────────────┬───────────────────────────────────────┘
                       │ HTTP REST
┌──────────────────────▼───────────────────────────────────────┐
│              Spring Boot 后端 (8082)                          │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │ Controller  │→│   Service     │→│   Repository      │    │
│  │ (JWT 鉴权)   │  │ (业务逻辑)    │  │ (SQLite JPA)      │    │
│  └─────────────┘  └──────┬───────┘  └──────────────────┘    │
│                          │                                    │
│                AiChatService                                  │
│                (调用 Python AI 服务)                           │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP REST
┌──────────────────────────▼───────────────────────────────────┐
│              Python AI 服务 (8002)                            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              AgentOrchestrator（智能体编排器）             │ │
│  │  ┌──────────┬──────────┬──────────┬──────────────────┐  │ │
│  │  │营养分析   │食物推荐   │膳食计划   │运动建议          │  │ │
│  │  ├──────────┼──────────┼──────────┼──────────────────┤  │ │
│  │  │食物审核   │周报生成   │文章生成   │NLU 饮食解析       │  │ │
│  │  ├──────────┼──────────┼──────────┼──────────────────┤  │ │
│  │  │健康反思   │语音解析   │检索判断   │问答对话          │  │ │
│  │  └──────────┴──────────┴──────────┴──────────────────┘  │ │
│  └─────────────────────────────────────────────────────────┘ │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐ │
│  │ 向量检索引擎  │ │  LLM 路由器   │ │  对话记忆存储        │ │
│  │ BGE + Chroma │ │ DeepSeek/Qwen│ │  SQLite              │ │
│  └──────────────┘ └──────────────┘ └──────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 三、后端架构（Spring Boot）

### 技术栈
- Spring Boot 2.7.18 + JPA + SQLite
- Spring Security + JWT 无状态认证
- Java 8（JDK 1.8.0_341）

### 核心 Controller 一览

| Controller | 路径 | 功能 |
|------------|------|------|
| `AuthController` | `/api/auth/*` | 登录、注册、重置密码，返回 JWT Token |
| `DietController` | `/api/diet/*` | 三餐饮食记录 CRUD、营养计算 |
| `FoodController` | `/api/food/*` | 食物搜索、分类筛选、优先级排序 |
| `RecipeController` | `/api/recipes/*` | 食谱搜索、详情、智能替换 |
| `AiConsultController` | `/api/ai/*` | AI 咨询入口：对话、食谱生成、营养分析等 9 个端点 |
| `ProfileController` | `/api/profile/*` | 用户档案：身高体重、过敏原、饮食偏好 |
| `NutritionReportController` | `/api/report/*` | 营养报告：BMR、营养评估、建议 |
| `FamilyRelationController` | `/api/family/*` | 亲友关系：添加、搜索、代理操作 |
| `CommunityController` | `/api/community/*` | 社区：发帖、点赞、评论 |
| `ExerciseRecordController` | `/api/exercise/*` | 运动记录 CRUD |
| `MetricsHistoryController` | `/api/metrics/*` | 身体指标历史记录 |

### AI 集成关键：AiChatService

```
前端 AiConsult.vue
  → api.ai.consult(question)
    → POST /api/ai/consult
      → AiConsultController.consult()
        → AiChatService.consult(userId, question)
          → 构建健康快照（用户档案 + 今日饮食 + 运动记录）
          → POST http://localhost:8002/api/v1/chat
            → 断路器检查（3次失败→60秒熔断）
            → 5秒连接超时 / 30秒读取超时
            → AgentOrchestrator.chat() → DeepSeek LLM
```

### 数据库实体

核心 15 个实体：`User`、`Food`、`DietMeal`、`DietItem`、`Recipe`、`RecipeIngredient`、`SavedRecipe`、`NutritionReport`、`BodyMetricsHistory`、`ExerciseRecord`、`FamilyRelation`、`AiConversationRecord`、`Post`、`Comment`、`PostLike`

### 食谱智能替换引擎

`RecipeService.java` 实现两级替换：

1. **规则基替换**（基于用户画像）：过敏原匹配 → 饮食限制匹配 → 口味偏好匹配
2. **食物数据库替换**（基于营养成分）：高脂食材（脂肪 >20g/100g）→ 低脂替代、高GI食材（GI >70）→ 低GI替代、高热量食材（热量 >250kcal）→ 低热量替代

食材匹配使用 5 级智能匹配：同义词映射 → 精确匹配 → 去括号匹配 → 包含匹配 → 核心名匹配（如 `姜片→姜`、`生抽→酱油`、`鸡胸肉→鸡胸肉(生)`）

## 四、AI 服务架构（FastAPI）

### Agent 编排器（orchestrator.py）

`AgentOrchestrator` 是 AI 服务的核心调度中心，职责包括：

- **意图识别**：通过 NLU Parser 分析用户输入的意图分类
- **Agent 调度**：根据意图将请求分发给对应的子 Agent
- **知识库检索**：从 ChromaDB 检索相关医学文献片段注入上下文
- **LLM 路由**：根据任务类型选择 DeepSeek 或 Qwen 模型
- **记忆管理**：维护用户对话历史和多轮记忆
- **故障切换**：LLM 不可用时自动降级到本地回退引擎

### 13 个 Agent 功能

| Agent | 触发场景 | 核心能力 |
|-------|---------|---------|
| `question_answer` | 通用健康问答 | 知识库检索 + LLM 生成 |
| `nutrition_analysis` | 营养分析请求 | 对比 DRI 标准，分析热量/蛋白质/脂肪/碳水 |
| `diet_plan` | 膳食计划请求 | 基于用户画像生成一周膳食计划 |
| `food_recommend` | 菜谱推荐请求 | 根据食材 + 人群 + 目标推荐菜谱 |
| `food_audit` | 食物审核请求 | 审核食物营养标签，检测高糖/高脂/高盐 |
| `exercise_advice` | 运动建议请求 | 基于 BMI + 目标 + 慢性病生成运动方案 |
| `weekly_report` | 周报生成请求 | 汇总一周饮食运动数据，生成健康周报 |
| `article_generate` | 文章生成请求 | 生成针对特定人群的健康科普文章 |
| `health_reflection` | 健康反思请求 | 分析健康指标，给出改善建议 |
| `nlu_parser` | 饮食描述解析 | 自然语言→结构化营养数据 |
| `voice_text_parse` | 语音录入解析 | 语音转录文本→食物条目标注 |
| `retrieve_judge` | 检索判断 | 评估是否需要检索知识库 |
| `orchestrator` | 编排调度 | 统一入口，协调各 Agent 协作 |

### 知识库架构

- **向量模型**：`BGE-base-zh-v1.5`（768维中文嵌入，本地运行）
- **向量数据库**：ChromaDB（本地嵌入式部署）
- **知识来源**：11 份权威 PDF 文献（中国膳食指南、糖尿病食养指南、孕产期营养等）
- **混合检索**：向量相似度 + 关键词 BM25 混合检索
- **食物数据库**：中国食物成分表（1500+ 食物记录，覆盖 8 大类）

### API 端点一览（共 25 个）

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/chat` | POST | AI 对话（核心端点） |
| `/api/v1/meal/parse` | POST | 饮食描述 NLU 解析 |
| `/api/v1/retrieve` | POST | 知识库检索 |
| `/api/v1/nutrition/analyze` | POST | 营养分析 |
| `/api/v1/food/audit` | POST | 食物审核 |
| `/api/v1/voice/parse` | POST | 语音文本解析 |
| `/api/v1/report/weekly-summary` | POST | 周报生成 |
| `/api/v1/article/generate` | POST | 文章生成 |
| `/api/v1/diet/plan` | POST | 膳食计划 |
| `/api/v1/food/recommend` | POST | 菜谱推荐 |
| `/api/v1/exercise/advice` | POST | 运动建议 |
| `/api/v1/reflection` | POST | 回答质量反思 |
| `/api/v1/health/reflection` | POST | 健康反思 |
| `/api/v1/quality/score` | POST | 回答质量评分 |
| `/api/v1/quality/stats` | GET | 质量统计 |
| `/api/v1/knowledge/stats` | GET | 知识库统计 |
| `/api/v1/agent/stats` | GET | Agent 运行统计 |
| `/api/v1/agent/stats/export` | GET | Agent 统计数据导出 |
| `/health` | GET | 健康检查 |

## 五、前端架构（Vue 3）

### 技术栈
- Vue 3 + TypeScript + Vite
- Vue Router 4（Hash 模式）+ Pinia 状态管理
- TailwindCSS + ECharts 可视化
- Axios HTTP 客户端

### 路由结构

```
/                          → Home.vue（品牌介绍落地页）
/login                     → Login.vue
/register                  → Register.vue
/forgot-password           → ForgotPassword.vue
/admin                     → Admin.vue
/dashboard                 → Dashboard.vue（主框架）
  /dashboard/profile       → 个人档案
  /dashboard/food-input    → 饮食录入
  /dashboard/food-add      → 添加食物
  /dashboard/nutrition     → 营养分析
  /dashboard/food-search   → 食物搜索
  /dashboard/ai-consult    → AI 健康咨询（含 Agent 快捷面板）
  /dashboard/recipe-library → 食谱库
  /dashboard/recipe-detail/:id → 食谱详情+智能替换
  /dashboard/recipe-search → 食谱搜索
  /dashboard/dietary-profile → 饮食画像
  /dashboard/health-report → 健康报告
  /dashboard/metrics-history → 身体指标历史
  /dashboard/muscle-chart  → 肌肉分析图
  /dashboard/exercise      → 运动记录
  /dashboard/family-relation → 亲友关系
  /dashboard/community     → 健康社区
  /dashboard/gym           → 附近健身
```

### API 封装（api/index.ts）

10 个 API 模块：`auth`、`food`、`diet`、`profile`、`admin`、`report`、`relation`、`metrics`、`ai`（9 个接口）、`recipe`

### AI 咨询页 Agent 快捷面板

在 `AiConsult.vue` 右侧栏提供 4 个一键 Agent 功能：
- 📊 营养分析 → 调用 `/ai/nutrition/analyze`
- 📋 膳食计划 → 调用 `/ai/diet/plan`
- 📈 周报生成 → 调用 `/ai/report/weekly`
- 🏃 运动建议 → 调用 `/ai/exercise/advice`

点击后自动组织对话消息 → 后端 → AI 编排器 → 格式化结果展示

## 六、关键功能实现流程

### 1. AI 健康咨询

```
用户输入问题
  → 前端 POST /api/ai/consult {question}
  → 后端构建健康快照（用户档案、今日饮食、运动记录、BMI）
  → AiChatService 调用 AI 服务 /api/v1/chat
  → AgentOrchestrator.chat()
      → NLU 意图识别
      → 知识库混合检索（BGE 向量 + BM25）
      → 对话记忆提取（最近 5 轮）
      → LLM 生成回答（DeepSeek V4 Flash）
      → 质量评分 + 免责声明追加
  → 回答 + 会话ID 返回前端
  → 前端 Markdown 渲染展示
```

### 2. 食谱智能替换

```
用户查看食谱详情
  → 前端调用 /api/recipes/{id}/detail
  → 后端 analyzeIngredients() 分析每个食材
      → 规则引擎匹配（过敏原/饮食限制/口味）
      → 食物数据库匹配（高脂/高GI/高热量检出）
      → findBestFoodMatch() 5级匹配（同义词→精确→去括号→包含→核心名）
  → 返回食材清单 + 替换建议（含营养成分对比）
  → 前端展示替换前/后营养值（总值 + 每100g值）
  → 用户确认替换 → 前端重新计算营养
```

### 3. 饮食记录与营养分析

```
用户录入饮食（手动/语音）
  → 前端 POST /api/diet/add {mealType, foods[]}
  → 后端计算各餐营养汇总
  → 实时更新 BMR + BMI
  → 对比 DRI 推荐摄入量
  → 生成营养缺口分析
  → 前端 ECharts 雷达图展示蛋白质/脂肪/碳水/纤维
```

### 4. 亲友代管

```
用户添加亲友关系
  → POST /api/family/add {targetUsername}
  → 被添加方确认 → 关系建立
  → 用户可在饮食录入/健康咨询时切换为目标用户（targetUserId 参数）
  → 后端验证亲属关系后代理操作
```

### 5. 食物搜索与优先级

```
用户搜索食物
  → 后端模糊匹配 foodName + foodCategory
  → 按 4 级优先级排序：
      顶级常用(11) > 常用食物(10) > 一般(5-9) > 冷门(0)
  → 返回分页结果
```

## 七、数据库

- **SQLite** 数据库文件：`backend-health/data/health.db`
- **AI 服务辅助数据库**（`ai_service/data/`）：
  - `conversations.db` — 对话记录
  - `user_memory.db` — 用户记忆
  - `food.db` — 食物数据副本
  - `cache.db` — 缓存
  - `token_usage.db` — Token 用量统计

## 八、启动方式

### 1. 启动 AI 服务（端口 8002）
```bash
cd health/ai_service
pip install -r requirements.txt
python main.py
```

### 2. 启动后端（端口 8082）
```bash
# 设置 Java 环境 + 使用 IntelliJ IDEA 内置 Maven
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_341"
& "C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.3\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run
```

### 3. 启动前端（端口 5173）
```bash
cd health/frontend-health
npm install
npm run dev
```

## 九、测试

- **Java 单元测试**：`backend-health/src/test/`，27 个测试用例
  - `RecipeServiceTest`（20 个）— 食材匹配 5 级算法
  - `AiChatServiceTest`（7 个）— 断路器、用户校验、边界条件
- **Python AI 测试**：`ai_service/scripts/test_*.py`（20+ 测试脚本）
- **运行测试**：
```bash
# Java 测试
cd health/backend-health
mvn test

# Python 测试
cd health/ai_service
python scripts/test_all_agents.py
```
