# 个人健康助手 — 项目优化方案报告（v3.0 · 现状盘点与后续优化设计）

> 版本：v3.0 ｜ 日期：2026-08-09
> 定位：在 v2.1（十大体系 + 四阶段 28 项）基础上，**盘点已落地成果**，并对多轮讨论形成的**后续优化设计**给出明确方案与决策。
> 配套文档：[项目优化方案报告.md](项目优化方案报告.md)（v2.1） / [系统架构与功能实现详解.md](系统架构与功能实现详解.md)（v5.0） / [内网分布式多机部署指南.md](内网分布式多机部署指南.md)

---

## 一、总览

v2.1 确立的「十大致胜体系 + 四阶段落地计划」中，**阶段一（9 项）、阶段二核心项（五库隔离/路由）、阶段三多项加分项均已落地**。本项目目前已具备竞赛级稳定架构。本文档在现状之上，给出**五块后续优化设计**：

| # | 优化方向 | 一句话目标 | 优先级 |
|:-:|----------|-----------|:------:|
| 1 | 知识卡片分区字数规范 | 分区定制字数（模板库扩至 500 字），内容更充实 | 高 |
| 2 | 文献溯源链接 | 检索保留原始 URL，文章点击直达来源，答辩可溯源 | 高 |
| 3 | 检索粒度自动适配确认 | 用户不选，系统按语义自动判定粒度 | 已定稿 |
| 4 | 双机拆分部署 | 算力节点 + 业务节点，虚拟局域网/内网穿透打通 | 中 |
| 5 | 分布式分片评估 | 明确与当前五库隔离的关系与边界 | 暂缓 |

---

## 二、现有项目架构与已完成优化盘点

### 2.1 三层子系统（稳定基线）

```
浏览器 → Vue3 前端(5173) → SpringBoot 后端(8082) → FastAPI AI 服务(8002)
                                              │
                                  ChromaDB 五库 + Ollama 本地 + DeepSeek 云端
```

### 2.2 已落地成果（对照 v2.1）

| v2.1 体系/阶段 | 落地项 | 现状 |
|------|--------|:----:|
| 阶段一 1 | 后端异步化（@Async + 任务注册表） | ✅ |
| 阶段一 2 | 建表演进（SC-001~005） | ✅ |
| 阶段一 3 | Caffeine 业务缓存 | ✅ |
| 阶段一 4 | 全链路 TraceId | ✅ |
| 阶段一 5 | AI 服务异步（受控线程池） | ✅ |
| 阶段一 6 | Ollama 管理接口 | ✅ |
| 阶段一 7 | 统一混合检索（BM25+BGE+RRF） | ✅ |
| 阶段一 8 | 前端 SSE 流式 | ✅ |
| 阶段一 9 | 图表缓存 useReportCache | ✅ |
| 体系 1.1 | **五库物理隔离**（kb_food/guide/crowd/literature/templates，4625 条） | ✅ |
| 体系 1.1 扩展 | **KnowledgeRouter 路由层**（意图检测 + ACL + 多库并行 + 轮询合并） | ✅ |
| 体系 1.6 扩展 | **三级检索粒度**（document/paragraph/fact，backfill_doc_level 回填） | ✅ |
| 体系 8 | **Reranker 精排**（bge-reranker-base 本地交叉编码，失败自动降级） | ✅ |
| 体系 8 | 离线一键演示 start_demo_offline.ps1 | ✅ |
| 体系 8 | 监控大屏（五库分布图 + 粒度选择 + rerank 分展示） | ✅ |
| 阶段三 25 | 健康时序预测（线性回归 + 置信区间） | ✅ |
| — | 前端巨石组件拆分（RecipeLibrary/Admin/ArticleDetail） | ✅ |
| — | 后端 AI 服务按业务域拆分（6 服务 + 单例熔断） | ✅ |

### 2.3 关键决策回顾（避免重复踩坑）

- Reranker 只新增 `rerank_score` 字段，**不改 `similarity`**；所有相似度阈值（0.45/0.60/0.70/0.15）基于 `similarity`，**无需调整**
- 三级检索粒度由系统自动适配，**不暴露给用户手动选择**
- 向量库只存原始权威材料，AI 生成内容严禁入库

---

## 三、后续优化设计（详细方案）

### 3.1 知识卡片分区字数规范

**现状**：`kb_templates` 模板卡按 150-250 字设定；`kb_food/kb_guide/kb_crowd/kb_literature` 按实际文档段落分块，长短不一、无统一规范。

**设计**：按知识库分区定制字数规范（配置化，不写死）：

| 分区 | 内容定位 | 字数规范 | 说明 |
|------|----------|:--------:|------|
| kb_templates | AI 生成模板卡 | **500 字左右** | 整合原文+新增内容+保留原有表述，论据密度更高 |
| kb_literature | 文献知识卡 | **500 字** | 复合权威卡片（多篇同主题合并），已按体系 1.4 执行 |
| kb_guide | 指南段落 | 300-500 字 | 按语义段落保留，不强制截断 |
| kb_crowd | 人群建议卡 | 300-400 字 | 每人群一条整合建议 |
| kb_food | 食物事实卡 | 100-200 字 | 事实级尽量精简，利于精确召回 |

**实现要点**：
- 卡片生成/分块阈值改为**按集合配置**（如 `CARD_LEN_BY_COLLECTION` 字典），替换现有全局 150-250 约束
- 模板库扩充走既有"整合 + 新增 + 保留表述"逻辑，重跑模板批量生成脚本即可
- 校验器同步按集合校验字数区间

**收益**：模板卡更完整 → 本地改写（A/B 方案）信息量更大，检索召回单卡即含足够论据。

### 3.2 文献溯源链接（可溯源闭环）

**现状**：文献 metadata 已含 `source_url`/`source_channel`/`journal`/`pubdate`（采集时保存）；前端 `articleRendering.ts` 已有 `linkTermsAndRefs` 链接化能力（术语/参考文献）。但**检索结果与文章正文尚未把 `source_url` 渲染为可点击溯源链接**。

**设计**：
1. **检索链路**：`retriever` 检索结果 metadata 透传 `source_url` → 大屏/检索接口直接展示「来源：[链接]」可点击（数据已具备，仅补前端渲染）
2. **文章链路**：文章生成时，正文引用处以 markdown 链接携带 `source_url`（如 `[来源](https://pubmed.ncbi.nlm.nih.gov/xxx/)`）→ 后端落库时保留 → 前端 `linkTermsAndRefs` 渲染为可点击外链
3. **来源卡片**：文章底部「参考来源」区列出每篇文献（标题 + 期刊 + 年份 + 跳转链接），替代现有"复制走搜"的弱体验

**实现要点**：
- 检索结果层：确保 `parallel_retrieve` 合并后保留 `metadata.source_url`（检查 `_round_robin_merge` 是否透传）
- 生成层：prompt 要求正文引用带上 `[[来源URL]]` 标记，生成后由解析器转 markdown 链接
- 前端：`linkTermsAndRefs` 增加链接渲染（`<a target="_blank">`）

**收益**：答辩可直接点开文献原文，验证"每个数值有出处"，是幻觉抑制体系（体系 6）的最直观证据。

### 3.3 检索粒度自动适配（设计定稿）

**决策**：三级粒度（document/paragraph/fact）**不做用户手动选择**，由系统自动判定。

**判定逻辑**（`detect_doc_level` 强化）：
- 问题含明确数据诉求（"多少/几克/含量/数值"）→ `fact`
- 问题含段落/条文指向（"要求/规定/段落"）→ `paragraph`
- 问题宽泛需整体框架（"注意事项/全文/整体"）→ `document`
- 默认：不限定粒度，让 BM25+向量自然召回（当前默认行为）

**现状**：已实现 `DOC_LEVEL_WORDS` 关键词识别 + `doc_level` 参数过滤 + `/api/v1/retrieve` 接口透传；后续可把关键词规则升级为轻量语义判断（短问题走 fact、长问题走 document），**不做 LLM 二次判断**（成本高收益低）。

### 3.4 双机拆分部署

**背景**：本地单机受 6GB 显存（RTX 3060）限制；多轮讨论确认按功能拆分到两台设备，分摊算力与存储。

**方案（双机最小拆分）**：

| 节点 | 部署内容 | 职责 |
|------|----------|------|
| 节点 A（算力节点） | Ollama 大模型 + `ai_service`（FastAPI + 五库 ChromaDB + Reranker） | 全部 AI 推理 + 向量检索（计算密集） |
| 节点 B（业务节点） | SpringBoot 后端 + Vue3 前端 + SQLite | 业务 API + 数据存储 + 用户界面 |

**网络打通**：
- 同一局域网：直接互访（Ollama 端口 11434 + AI 服务 8002 映射到局域网）
- 跨网：**虚拟局域网**（Tailscale/ZeroTier，组网零配置）或**内网穿透**（frp，暴露 8002/8082）
- 后端 `application.yml` 的 AI 服务地址改为节点 A 局域网 IP；前端访问节点 B 地址即可

**收益**：
- 大模型与检索解耦，节点 A 可独立升级显存/模型（未来 14B）
- 节点 B 纯业务，轻负载低功耗（可用笔记本/迷你主机）
- 演示两台设备分工明确，答辩展示"分布式"加分

**与现有文档关系**：复用《内网分布式多机部署指南.md》方案 A/B；本文档明确为「算力 + 业务」最小双机。

### 3.5 分布式分片评估（暂缓）

**结论：现阶段不落地**，但明确边界：

| 维度 | 当前架构（五库物理隔离） | 分布式分片 |
|------|------------------------|-----------|
| 拆分维度 | **业务域**（食物/指南/人群/文献/模板） | **数据量**（按 ID/哈希水平分片） |
| 解决什么问题 | 跨域检索噪声、ACL 权限 | 单库容量上限、查询并行度 |
| 是否需要 | 已实现 | 当前 4625 条单机无压力，**不需要** |
| 触发条件 | — | 单库 > 数十万条，或向量检索延迟劣化明显 |

**判断**：五库物理隔离本身就是"业务域分片"，已覆盖本项目主要收益（噪声隔离 + 权限）。真正的数据分片/分布式向量库（如 Milvus/ES 向量索引）引入运维复杂度，与"单机便携、一键启动"的演示定位冲突。**仅当后续数据量或性能成为瓶颈时再评估**（优先选型：ChromaDB 独立进程 + 多集合，而非重引擎）。

### 3.6 AI 方案收敛（本地部署 A/B 为主）

**决策**：本地/演示场景收敛为 **A（纯本地）+ B（本地框架 + 云端正文）** 双链路；**C（纯云端）不作为常驻方案**，仅保留在线高性能模式入口。

| 方案 | 链路 | 适用场景 | 结论 |
|:----:|------|----------|:----:|
| A | 模板召回 → 本地 Qwen 改写 | 离线/断网/简单问答 | ✅ 主力 |
| B | 本地框架 → 云端正文 → 本地校验 | 在线正式科普文章 | ✅ 主力 |
| C | 直接 DeepSeek | 在线 `high_performance=true` 演示 | ⚠️ 仅入口保留 |

**依据**：本地部署场景（离线演示、单机演示）不需要 C；B 流水线对结构化任务较 C 省 token 25~58%；保留 C 仅为高性能演示路径，不参与日常路由。

---

## 四、落地优先级

```
第 1 步（立即、高性价比）
  ① 溯源链接（数据已具备，纯前端/透传补全，答辩直观加分）
  ② 知识卡片分区字数规范（模板库 500 字，配合内容完善）
        ▼
第 2 步（演示增强）
  ③ 双机部署验证（局域网直连 → 虚拟局域网/内网穿透）
        ▼
第 3 步（按需）
  ④ 检索粒度语义增强（关键词规则升级）
  ⑤ 分布式分片评估（触发条件达成后再启动）
```

### 最简落地组合（时间紧）

**① 溯源链接 + ② 模板库 500 字** 两项即可在答辩展示"可溯源 + 内容充实"两个加分点，工作量均集中在配置与透传，风险低。

---

## 五、决策记录

| 决策项 | 结论 |
|--------|------|
| 检索粒度用户选择 | 不做，系统自动适配 |
| 模板库卡片字数 | 扩至 500 字，按集合配置 |
| 文献溯源 | 做：保留 source_url → 检索/文章可点击溯源 |
| 双机部署 | 做：算力节点（Ollama+AI服务）+ 业务节点（后端+前端） |
| 分布式分片 | 暂缓：五库隔离已覆盖业务收益，触发条件后再评估 |
| AI 方案 | 本地 A/B 为主，C 仅保留高性能入口 |
| Redis 分布式缓存 | 不做（沿用 v2.1 决策） |
| 爬虫继续扩充 | 不做（沿用 v2.1 决策） |

---

## 六、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-09 | v3.0 | 新增：现状盘点（v2.1 落地成果）；后续设计（卡片分区字数/溯源链接/粒度自动适配定稿/双机部署/分片评估/A-B 收敛） |
