name: "Router Standard (experimental)"
description: "Task-aware routing — classify-first persona + full prompt sections; first-turn core tool surface; near-field guides in weak mode. Full Standard tools after the first tool call."
