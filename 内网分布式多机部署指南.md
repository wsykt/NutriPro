# 个人健康助手 — 内网分布式多机部署指南

> 版本：v1.0（2026-08-09）
> 定位：规划尚未落地的「内网分布式多机部署」的**实施文档**。当前单机部署已稳定运行，本文档说明如何演进到内网多机部署，以及每一步的真实工作量。

---

## 一、先纠正一个认知：单机 vs 分布式，区别到底大不大？

**结论：对「进程/计算」层面，区别很小；对「存储/状态」层面，区别很大。** 之所以感觉"区别不大"，是因为本项目本身就是三进程微服务式拆分（前端 5173 / 后端 8082 / AI 8002 通过 HTTP 通信）——这天然就是分布式的骨架。但下面这张表是真正的差异所在：

| 能力 | 单机现状 | 分布式要求 | 改造量 |
|------|----------|-----------|:------:|
| **后端数据库** | SQLite 单文件（`./data/health.db`） | SQLite 是**单写者 + 文件锁**，多机无法共享同一文件 | **大（必改）** |
| **AI 向量库** | ChromaDB 嵌入式（进程内加载 collection + 锁文件） | 0.4.x 无法跨机共享目录；多实例各自加载会数据分裂 | **大（必改）** |
| **本地大模型 Ollama** | GPU 机本地 `localhost:11434` | 只能一台 GPU 机跑，其他机**远程调用** | **零改动** |
| **嵌入模型 BGE** | 本地模型目录 `./models/bge-base-zh-v1.5` | 每台运行 AI 的机器需各自准备模型文件 | 小 |
| **AI 服务地址** | 后端硬编码 `localhost:8002` | 指向 AI 服务所在机器 IP | 小 |
| **异步任务状态** | `async_task_service` 内存字典 | 多实例间任务状态不共享 | **中（必改）** |
| **知识库热度/质量统计** | 内存队列 | 多实例各自计数 | 中 |
| **JWT 鉴权** | 无状态 token | 天然支持多实例 | **零改动** |
| **SSE 流式对话** | 单实例长连接 | 负载均衡需会话亲和 + 关闭缓冲 | 中 |
| **静态资源/前端** | Vite dev (127.0.0.1) | build 后部署到任意机/nginx | 小 |

> **一句话总结**：HTTP 进程拆分已就绪，但 **SQLite 与 ChromaDB 是单机绑定**，多机水平扩展前必须先迁移；而 Ollama、JWT、配置项早已支持远程/环境变量化，几乎不用改。

---

## 二、单机现状盘点（配置出处）

| 项 | 当前值 | 配置位置 |
|----|--------|----------|
| AI 服务监听 | `host=0.0.0.0, port=AI_SERVICE_PORT`（默认 8003，实际 .env 设 8002） | `ai_service/main.py:1578`、`config/settings.py` |
| Ollama 地址 | `OLLAMA_BASE_URL=http://localhost:11434` | `ai_service/config/settings.py`（环境变量可覆盖） |
| 向量库路径 | `CHROMA_DB_PATH=./knowledge/chroma_db_storage` | `config/settings.py` |
| 数据目录 | `DATA_DIR=./data`（6 个 SQLite：templates/food/conversations/token_usage/user_memory/cache） | `config/settings.py` |
| 嵌入模型 | `EMBEDDING_MODEL_NAME=./models/bge-base-zh-v1.5` | `config/settings.py` |
| 后端数据库 | `jdbc:sqlite:./data/health.db` | `backend-health/src/main/resources/application.yml` |
| AI 服务地址 | `health.ai-service.url=http://localhost:8002` | 同上 |
| 前端代理 | `/api → http://localhost:8082`，dev host 127.0.0.1 | `frontend-health/vite.config.ts` |
| 一键启动 | 三服务 + Ollama 双链路自检 | `start_all.ps1` |

**好消息**：`settings.py` 全部走 `os.getenv`，后端 yml 也可被命令行参数覆盖——**配置层已为分布式预留了入口**。

---

## 三、推荐目标架构（两条路径）

### 方案 A：物理分散 · 逻辑单实例（推荐，近零代码改造）

适合：内网 2~3 台机器、单实例即可满足家庭/小团队负载、不想改 SQLite/ChromaDB。

```
┌───────────────────────────────┐      ┌───────────────────────────────┐
│  GPU 机（如本机）              │      │  主服务器（内网 IP，如 192.168.1.10）│
│  ┌─────────────────────────┐  │      │  ┌─────────────────────────┐  │
│  │ Ollama (11434)          │◄─┼──OLLAMA_BASE_URL───┼─│ AI 服务 (8002)          │  │
│  │ qwen2.5-7b (GPU 专属)   │  │      │  │  ├ ChromaDB 向量库         │  │
│  └─────────────────────────┘  │      │  │  ├ data/*.db (6 SQLite)   │  │
└───────────────────────────────┘      │  │  └ BGE 模型目录            │  │
                                        │  └─────────────────────────┘  │
                                        │  ┌─────────────────────────┐  │
                                        │  │ 后端 (8082)              │  │
                                        │  │  ├ health.db (SQLite)    │  │
                                        │  │  └ ai-service.url → AI 机 │  │
                                        │  └─────────────────────────┘  │
                                        │  ┌─────────────────────────┐  │
                                        │  │ 前端 (build 静态/nginx)   │  │
                                        │  └─────────────────────────┘  │
                                        └───────────────────────────────┘
```

**特点**：AI 服务、数据库、向量库、后端全部集中在主服务器（单实例语义不变），GPU 机只贡献 Ollama 推理能力。所有"单机绑定"的存储都在同一台机器上，**SQLite 和 ChromaDB 无需迁移**。其他机器通过浏览器访问主服务器前端即可。

**改动清单（全是配置，零代码）**：
1. GPU 机：确认 Ollama 监听 `0.0.0.0:11434`（默认），并放行防火墙
2. 主服务器 `.env`：`OLLAMA_BASE_URL=http://<GPU机IP>:11434`、`AI_SERVICE_PORT=8002`
3. 后端启动参数覆盖：`--health.ai-service.url=http://<AI机IP>:8002`
4. 前端 `npm run build` → 静态文件放主服务器 nginx（`/api` 反代到 `localhost:8082`）
5. 其余零改动

### 方案 B：多机水平扩展（完整分布式）

适合：多实例并发、高可用、需要 Redis 生态。**必须先完成本文档第六节的基础设施改造。**

```
┌────────────┐   ┌────────────┐   ┌────────────┐
│ 前端(nginx) │   │ 后端实例 1  │   │ 后端实例 2  │     负载均衡 + 会话亲和(SSE)
└─────┬──────┘   └─────┬──────┘   └─────┬──────┘
      │                │                │
      ▼                ▼                ▼
┌──────────────────────────────────────────────┐
│  PostgreSQL（替代 SQLite，主从/单点）           │
│  + pgvector（替代 ChromaDB，或 Chroma 单点）    │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│  Redis（缓存 / 任务队列 / 分布式锁 / 限流计数） │
└──────────────────────────────────────────────┘
      │                │                │
      ▼                ▼                ▼
┌────────────┐   ┌────────────┐   ┌────────────┐
│ AI 实例 1   │   │ AI 实例 2   │   │  GPU 机     │
│ (8002)     │   │ (8002)     │   │  Ollama    │
└────────────┘   └────────────┘   └────────────┘
```

---

## 四、方案 A 实施步骤（详细）

1. **网络准备**：确认各机内网互通；主服务器固定 IP；防火墙放行 8002/8082/80
2. **GPU 机 Ollama 校验**：
   ```
   curl http://<GPU机IP>:11434/api/tags   # 返回模型列表即通
   ```
3. **主服务器 AI 服务**：`.env` 增加
   ```
   OLLAMA_BASE_URL=http://<GPU机IP>:11434
   AI_SERVICE_PORT=8002
   ```
   启动后访问 `/api/v1/ollama/status` 确认远程链路
4. **后端**：命令行参数覆盖
   ```
   java -jar health-backend-1.0.0.jar --health.ai-service.url=http://<AI机IP>:8002
   ```
5. **前端**：
   - 开发：`vite.config.ts` 的 `server.host` 改 `0.0.0.0`、proxy target 改后端 IP
   - 生产：`npm run build`，产物 `dist/` 放 nginx，反代 `/api` → 后端
6. **验证**：见第七节

---

## 五、方案 B 基础设施改造（对应"规划未落地"项）

方案 B 的每一项都对应之前文档第八节剔除的规划项，落地顺序即依赖顺序：

| 顺序 | 规划项 | 作用 | 依赖 |
|:---:|--------|------|------|
| 1 | **Redis 缓存** | 替换/支撑 `utils/cache_utils`（检索/会话缓存已定义未接线）、后端 Caffeine 升级为共享缓存、浏览量与热度计数集中化 | 无 |
| 2 | **接口限流** | 登录防爆破、AI 调用配额、`/article/generate` 防刷 | Redis（计数） |
| 3 | **消息队列** | 异步任务 `async_task_service` 内存字典 → Redis 队列/Stream，任务可跨实例调度、重启不丢 | Redis |
| 4 | **数据库迁移** | SQLite → PostgreSQL（后端业务库 + AI 服务 data/*.db 中需共享者）；表结构/方言改造 | 无 |
| 5 | **向量库** | ChromaDB → pgvector（复用已建索引语义），或 Chroma 单点集中 | PostgreSQL |
| 6 | **知识库快照/回滚** | 向量库 + 模板库双写一致性快照，支撑多实例发布前校验 | 迁移后 |
| 7 | **爬虫三档限速** | `crawler/compliance.py` 基础上按域名档位限速（低/中/高三档），避免内网并发采集触发外网封禁 | 可独立先行 |
| 8 | **完整营养素计算器** | `services/nutrition_calc`（此前规划未落地）→ 替代散落的 BMR/营养素计算，供多实例一致性使用 | 可独立先行 |

> **建议**：7、8 两项与分布式无强绑定，**可独立先行**；1~6 按依赖顺序推进，核心工作量集中在 4、5（数据迁移）。

---

## 六、单机 → 方案 B 的迁移步骤（总纲）

```
阶段 0  前置：完成安全加固（JWT 密钥环境化 / .env 化 / 统一配置入口）
阶段 1  引入 Redis：先接缓存（检索缓存、会话缓存、浏览计数）→ 验证
阶段 2  异步任务迁 Redis 队列（async_task_service 状态外置）→ 多实例任务调度
阶段 3  接口限流接入（登录/生成类端点）→ 验证
阶段 4  后端 SQLite → PostgreSQL（Entity 方言、ddl、时间戳格式回归）
阶段 5  AI 服务向量库 → pgvector（retrieval_service/template_store 回归）
阶段 6  知识库快照/回滚机制
阶段 7  nginx 负载均衡（含 SSE 会话亲和、keepalive 配置）→ 多实例压测
```

每阶段独立可回滚；阶段 4、5 是回归重点（SQLite 时间戳格式、JSON 字段映射此前踩过坑）。

---

## 七、验证清单

| 项 | 方法 | 预期 |
|----|------|------|
| AI 服务健康 | `GET http://<AI机>:8002/health` | 200 |
| 远程 Ollama | `GET http://<AI机>:8002/api/v1/ollama/status` | models 非空 |
| 后端→AI | 对话一次 | 正常流式返回 |
| 数据库迁移 | 执行既有 20 用例单用户测试 | 全过 |
| 负载均衡（B） | 双实例压测 / 会话保持 | 请求均衡、SSE 不中断 |
| 任务跨实例（B） | submit 任务后由另一实例 get | 状态一致 |

---

## 八、与现有一键启动的关系

- `start_all.ps1` 保持单机一键启动不变（开发机/演示用）
- 新增 `start_deploy.ps1`（或文档中的手动命令）用于方案 A/B 的生产启动
- 二者共用 `.env`，通过环境变量区分单机/分布式

---

## 九、决策记录（后续待确认）

| 问题 | 默认建议 | 理由 |
|------|----------|------|
| 先做 A 还是 B？ | 先 A（近零改造，先跑通内网多机） | 数据迁移（B 的 4/5）是大工程，应单独评估 |
| 后端库迁移目标 | PostgreSQL 15 + pgvector | 生态成熟、Hibernate 支持好 |
| Redis 是否需要 | 方案 B 才需要 | A 方案单实例无需引入 |
| 消息队列选型 | Redis Stream（不引入 Kafka/RabbitMQ） | 规模小、运维零成本 |

---

## 变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-09 | v1.0 | 初稿：差异纠正、单机现状、方案 A/B 双路径、迁移总纲与验证清单 |
