# 个人健康助手 — 项目优化方案报告（终极版）

> 版本：v2.1（终极整合版 + 迭代完善清单）
> 日期：2026-08-08
> 定位：面向软件杯/挑战杯竞赛展示的整套**顶层架构优化方案**——纯架构、纯逻辑、不含硬件
> 配套文档：[优化后项目架构说明.md](优化后项目架构说明.md) / [架构与功能说明.md](架构与功能说明.md)
> 说明：本文档整合此前全部讨论结论（外部方案逐条评估 + 语音多轮讨论 + 终极理想版方案），形成**十大致胜体系 + 四阶段落地计划**。

---

## 一、结论摘要（一句话总纲）

> **用向量相似度做粗筛、本地大模型做精判，自动合并文献、自动识别学术争议，构建多隔离知识库 + 精细化人群 BMI 策略，配合双层 AI 流水线与严格校验体系，形成远超普通 AI 项目的垂直深度与工程稳定性，达到国赛一等奖梯队架构。**

现有项目已完成 **Vue3 前端 + Spring Boot 后端 + FastAPI Python AI 服务** 三层解耦，具备双模型流水线、RAG 知识库、多 Agent 编排、离线兜底等竞赛级基础。终极版方案在此基础上收敛为 **10 大核心体系**：

| # | 体系 | 一句话目标 |
|:-:|------|-----------|
| 1 | RAG 知识库顶层优化 | 五库物理隔离 + 双层智能聚类 + 文献自动合并 + 学术争议挖掘 |
| 2 | 人群策略体系 | 6 人群 × 5 档 BMI 的垂直深度营养策略 |
| 3 | 三梯度科普生成 | 速读/深度/综述三段式结构固化（已实现，保留） |
| 4 | B 混合流水线终极架构 | 本地只做框架，云端只填正文，双阶段校验 |
| 5 | 智能模型路由 | A/B/C 自动分流 + 断网降级纯本地 |
| 6 | LLM 幻觉抑制体系 | 六道防线全链路校验，项目核心壁垒 |
| 7 | 后端架构优化 | 全异步 + TraceId + 缓存 + 三级降级 |
| 8 | 轻量加分功能 | 营养素计算器 + 知识库看板 + 离线固化 |
| 9 | ABC 测试策略 | 现阶段暂停对比，知识库成熟后再开启 |
| 10 | 最终项目定位 | 国赛级壁垒：多库隔离 RAG + 垂直策略 + 幻觉抑制 |

**明确不做项**：蓝牙手环对接、异步消息队列、爬虫继续扩充、Redis 分布式缓存（详见第八节）。

---

## 二、现有架构核心优势回顾（优化基座）

| 维度 | 现状亮点 |
|------|----------|
| 架构解耦 | 三层独立服务：Vue3（5173）→ SpringBoot（8082）→ FastAPI（8002），单一职责清晰 |
| 双模型流水线 | 本地 Qwen2.5-7B + 云端 DeepSeek 双链路，A/B/C 三方案可按场景切换 |
| 知识库闭环 | 模板批量生成 → 逐条入库 → 四层去重 → SQLite 双写镜像 → 启动对账，已具备自增长能力 |
| 业务覆盖 | 饮食记录/NLU 解析/营养分析/膳食计划/食材审核/菜谱推荐/运动建议/健康周报/科普文章/社区/亲友代理，14 个 Controller、~134 端点 |
| 容错降级 | LocalFallbackEngine 离线兜底、RestTemplate 断路器（3 次失败→60s 熔断）、Ollama 超时放宽 |
| 数据规范 | 食物库 9 大类 + 生/熟标注 + 优先级 4 级；用户表字段约束；科普文章三文稿 + 事实校验 Agent |
| 稳定性基线 | SQLite 全库统一 WAL + busy_timeout=5000 + 索引（10.9 节已落地） |

---

## 三、核心优化体系详解（十大体系）

### 体系 1：RAG 知识库顶层优化体系（核心最大加分项）

#### 1.1 五大类物理独立向量知识库

```
┌─────────────┐ ┌──────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
│ 食物营养库   │ │ 人群膳食指南库 │ │ 运动知识库  │ │ 补剂知识库  │ │ 科研文献库  │
│ food_data   │ │ diet_guideline│ │ exercise   │ │ supplement │ │ literature │
│ 9大类+生熟  │ │ 6人群+5BMI   │ │ 运动科普    │ │ 补剂资料    │ │ PMID+通道   │
└─────────────┘ └──────────────┘ └────────────┘ └────────────┘ └────────────┘
      各司检索域，彻底杜绝跨域噪声（问"吃多少"不会召回"怎么跑"）
```

> 在现有"知识库分块"（同一 ChromaDB 内分块过滤）之上**升级为物理独立 collection**，检索时按领域定向，从根上隔离噪声。

#### 1.2 文献素材双层智能聚类体系（相似度 + LLM 语义校验）

```
        候选文献素材池
              │
  ① BGE 向量相似度粗筛（召回候选集，天然相似度偏低、噪声大）
              │
  ② 本地大模型语义精判（对候选集逐条做语义终审：
     是否同主题？结论是否一致？是否属于同一论据？）
              │
        聚类结果（高置信度分组）
```

> 解决 BGE 相似度天然偏低、检索噪声大的问题——向量负责"找可能相关"，LLM 负责"确认是否真相关"。

#### 1.3 三层相似度分级体系（固定顶层逻辑，不写死阈值）

| 分级 | 判定 | 处理 |
|:----:|------|------|
| 低分区 | 绝对无关 | 直接过滤，不浪费 token |
| 灰色区间 | 相似度中等、难以判定 | **全部交给本地 LLM 二次判定**（这是主要工作量所在） |
| 高分区 | 强相关 | 直接聚类合并，不再过 LLM，节省算力 |

> 阈值不写死：不同主题/人群的相似度分布不同，阈值作为可配置参数（`KB_DEDUP_*` 既有配置扩展），顶层逻辑固定、参数可调。

#### 1.4 智能文献合并机制

```
多篇同主题、结论一致文献
   → 自动融合提炼
   → 生成 500 字复合权威文献卡片
   （内容互证、论据密度提升、来源附全部 PMID 可溯源）
```

- 消除重复素材、压缩 Token、提升单张卡片的论据密度
- 卡片保留全部原始 PMID（多源互证，可信度更高）

#### 1.5 学术争议自动识别体系

```
同主题、结论相悖的文献
   → 自动识别、归类、打标签（dispute 标签 + 双方立场摘要）
   → 注入综述文稿生成
   → 产出「学术争议 / 多元研究观点」章节
```

> 这是**综述文稿大幅拔高专业度**的关键：普通项目只会"罗列共识"，本项目能呈现"研究分歧与不同立场"，直接体现知识库的学术深度。

#### 1.6 素材统一标准化规范

- 所有知识库卡片统一结构、统一字数（150-250 字模板、文献卡片 500 字）、统一核心字段
- 彻底解决素材参差不齐、长短不一导致的向量不准、检索乱序问题
- 既有规范延续：卡片 4 类核心信息（核心共识/定量数据/适用人群/局限争议）

#### 1.7 知识库冷热分层召回

```
高频人群 + 高频主题 → 热区（优先召回）
低频内容           → 冷区（后置召回，仅按需加载）
```

- 提升生成贴合度：高频场景响应更快、命中更准
- 结合检索频次统计自动分层（配合 SQLite 冷热归档索引）

### 体系 2：人群策略体系（项目独有的垂直深度）

```
6 大人群 + 通用人群
    × 5 档 BMI（过低/偏低/正常/偏高/超高）
    = 35 个「人群 × BMI」组合，每个组合拥有：
    ├─ 独立膳食策略（热量/宏量配比/餐次结构）
    ├─ 独立禁忌清单（过敏/慢病禁忌/孕期禁忌）
    ├─ 独立食材替换规则（同等营养更低风险替代）
    └─ 独立营养目标（钙/叶酸/DHA/膳食纤维/GI 等分人群差异）
    全部策略依托「聚类合并后的权威文献」生成，有据可依
```

> 与现有模板矩阵（7 人群 × 5 BMI × 4 功能 = 1400 模板）无缝衔接，策略内容全部有文献支撑，答辩时可展示"策略→文献"溯源链。

### 体系 3：AI 三梯度科普生成体系（已实现，保留固化）

| 梯度 | 定位 | 结构 |
|:----:|------|------|
| 速读层 | 通俗、轻量化、普适 | 图文卡片式，3 分钟读完 |
| 深度层 | 原理 + 营养数据 + 标准依据 | 完整章节，引用官方指南数值 |
| 综述层 | 多文献整合 + 学术争议 + 研究对比 + 权威结论 | **融合体系 1.5 争议识别结果** |

> 严格固定三层梯度输出、全程结构化隔离，三文稿编号体系统一（一级目录中文数字、二级"（一）（二）"），已实现并作为优势保留固化。

### 体系 4：B 混合流水线终极架构优化（核心架构升级）

**铁律：本地模型只做结构、框架、大纲、素材筛选、校验——绝对不生成正文。**

```
┌─────────────────────────────────────────────────────────────┐
│ 本地模型（框架层）：                                         │
│  素材筛选 → 大纲生成 → 章节骨架 → 分块标记（不做正文）        │
│  ⇒ 从根源降低 Token、减少幻觉、压缩耗时                      │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ 云端模型（正文层）：只负责填充正文精加工                       │
│  按骨架分块直填，不梳理层级，省 token 且结构一致              │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ 本地双阶段校验：                                             │
│  ① 素材校验（生成前）：素材质量/相关性/文献可用性             │
│  ② 成文校验（生成后）：格式完整/数值合规/常识冲突             │
└─────────────────────────────────────────────────────────────┘
```

配套机制：
- **文献预检机制**：NCBI 验证 PMID 真实性，杜绝虚假文献、失效文献；网络故障自动降级（改用缓存官方指南）
- **Token 前置预算机制**：按目标字数/层级预估算 token 预算，超预算自动精简素材，防止上下文溢出

### 体系 5：智能模型路由体系（自适应调度）

| 场景 | 路由 | 说明 |
|------|:----:|------|
| 简单问答 | **A 纯本地** | 模板召回 → 本地 Qwen 改写，零云端成本 |
| 正式科普结构化文章 | **B 混合流水线** | 本地框架 + 云端正文 + 双阶段校验 |
| 超高精度、多文献综述 | **C 纯云端** | 直接 DeepSeek 高精度生成 |
| 断网 / 弱网 | **降级纯本地** | 全部走本地 + 规则引擎兜底 |
| 高性能演示 | **C 直连** | `high_performance=true` 跳过本地校验，LLM_TIMEOUT_HIGH_PERF=45 |

> 保留 A/B/C 三模式 + ModeRouter 智能分流，叠加场景/负载/定时三层动态调度。

### 体系 6：LLM 幻觉抑制体系（整套项目核心壁垒）

六道防线，全链路卡控：

```
① 向量素材前置过滤     → 低质/无关素材在生成前剔除
② 相似度+LLM 双层筛选  → 灰色区间由本地 LLM 二次判定（体系 1.2/1.3）
③ 文献真实性校验       → PMID 预检 + 来源通道标签（文献预检机制）
④ 营养数值合规校验     → 对照《中国居民膳食指南 2022》营养素标准
⑤ 三层文稿逻辑重复性校验 → 三梯度文稿内容不重复、层级不混淆
⑥ 成文本地常识终审     → 与官方文档冲突自动触发润色，减少人工排查
```

> 幻觉抑制是**项目与普通 AI 助手的核心分水岭**：普通项目"AI 说什么是什么"，本项目"每个数值有出处、每篇文献可溯源"。

### 体系 7：后端架构优化（比赛必稳、必流畅）

| 优化 | 说明 |
|------|------|
| 耗时 AI 任务全异步化 | `@Async` 线程池 + AI 服务 `asyncio.to_thread`，前端无阻塞 |
| 全链路 TraceId 日志 | 前端请求头 → 后端 → AI 服务贯穿，MDC 自动打点 |
| 业务缓存优化 | Caffeine 缓存食物搜索/菜谱查询，高频查询加速 |
| 三级降级策略 | 云端 → 混合（本地+云端）→ 本地规则兜底，逐级自动降级 |

### 体系 8：新增轻量化高性价比加分功能（极低工作量、高展示度）

| 功能 | 说明 |
|------|------|
| **内置营养素精准计算器** | 公式驱动（BMR/热量/宏量配比/微量目标），非 AI 瞎算，结果可复核 |
| **知识库可视化看板** | 数量、分类、更新、争议文献统计（dashboard 完善） |
| **完整离线能力固化** | 无外网可全功能运行（本地 Qwen + 本地向量库 + 规则引擎） |

### 体系 9：ABC 测试体系策略（顶层决策）

> **现阶段暂停 ABC 自动对比开发。**
>
> 理由：知识库仍处于建设扩充期，对比无意义（素材不稳定则结论不可比）。
>
> 后续：知识库完全成熟后，再开启 AC 性能对标测试，验证"本地 RAG + 本地模型"媲美云端效果。将对比作为**论文/报告的数据支撑**而非功能。

### 体系 10：最终项目定位（国赛级别壁垒总结）

1. **多库隔离 RAG + 文献智能聚类合并 + 学术争议挖掘**（远超普通 RAG 项目）
2. **人群 + BMI 双维度垂直营养策略**（垂直领域深度极强）
3. **三段式梯度科普生成**（结构固化）
4. **双模型智能流水线 + 严格幻觉校验体系**（工程壁垒）
5. **完整离线可用、多级降级、工业级架构**（稳定壁垒）

---

## 四、四阶段落地计划

> 阶段划分原则：**先保稳定与演示体验，再深架构，后做加分创新**。每阶段完成后可独立演示、可回滚。

### 4.1 阶段一：稳定与体验（必做）

**目标**：消除阻塞、加速检索、流式呈现、链路可观测。

| 举措 | 落地内容 | 涉及模块 |
|------|----------|----------|
| 1. 后端异步化 | 耗时 AI 调用改 `@Async`（线程池），同步接口保留兜底 | backend-health |
| 2. 建表演进 | **保持自动建表（ddl-auto: update）**，逐步补索引/唯一约束/演进脚本，冻结期切 `none` + 手动 SQL；过程记录见《SQL建表演进报告》 | backend-health |
| 3. 业务缓存 | Caffeine 缓存食物搜索/菜谱查询（TTL + 失效策略） | backend-health |
| 4. 全链路 TraceId | logback MDC 贯穿前端 → 后端 → AI 服务 | backend-health + ai_service |
| 5. AI 服务异步 | 长任务 `asyncio.to_thread` + 状态查询接口 | ai_service/main.py |
| 6. Ollama 管理接口 | 模型列表/加载状态/上下文窗口/健康检查统一接口 | ai_service/ollama_manager.py |
| 7. 统一混合检索 | 收敛为 `RetrievalService`（BM25 + BGE + RRF），所有 Agent 复用 | ai_service/services/retrieval_service.py |
| 8. 前端 SSE 流式 | AI 对话/文章生成流式逐字呈现 | frontend-health |
| 9. 图表缓存 | 看板接口防抖 + 前端缓存 + 增量更新 | frontend-health |

#### 阶段一落地进度（2026-08-09）

| 举措 | 状态 | 落地详情 |
|------|:----:|----------|
| 1. 后端异步化 | ✅ | 新增 [AsyncConfig.java](backend-health/src/main/java/com/health/config/AsyncConfig.java)（aiTaskExecutor：核心2/最大8/队列20/CallerRunsPolicy + MDC透传装饰器）；新增 [AsyncArticleTaskService.java](backend-health/src/main/java/com/health/service/AsyncArticleTaskService.java)（内存任务注册表，PENDING→RUNNING→SUCCESS/FAILED，容量上限200）；ArticleController 新增 `/api/articles/generate-async`、`/generate-hybrid-async`、`/regenerate-async/{id}`、`/task/{taskId}` 四个接口，同步接口保留兜底。已验证：提交 196ms 立即返回 taskId，异步线程 `[ai-task-1]` 执行且 traceId 透传成功 |
| 2. 建表演进 | ✅ | SC-001~005 全部落地（详见《SQL建表演进报告》）：SC-001 补 21 条索引、SC-002 补 3 条唯一索引、SC-003 生成 init-schema.sql 幂等基线；SC-004 切换 `ddl-auto: none` 为**决策暂缓**（功能迭代活跃期保持 update，冻结期再切换）；SC-005 外键评估结论为**保持逻辑外键** |
| 3. 业务缓存 | ✅ | CaffeineCacheManager（maximumSize 2000 / expireAfterWrite 10min）；FoodService 4 个缓存类 + 写操作全量 evict；RecipeService 4 个缓存类 + 写操作 evict。已验证：二次相同搜索命中缓存（34ms→1ms） |
| 4. 全链路 TraceId | ✅ | TraceIdFilter（HIGHEST_PRECEDENCE，生成/透传 X-Trace-Id 写 MDC + 响应头回传）；JwtFilter 认证后写 userId MDC；logback-spring.xml 三处 pattern 输出 `[traceId= userId=]`。已验证：带 JWT 请求日志 `[traceId=09726e814d0647e2 userId=1]` |
| 5. AI 服务异步 | ✅ | 新增 [async_task_service.py](ai_service/services/async_task_service.py)：受控线程池（max_workers=MAX_LLM_CONCURRENCY）+ 内存任务注册表（PENDING→RUNNING→SUCCESS/FAILED，容量200）；main.py 新增 `POST /api/v1/tasks/submit`、`GET /api/v1/tasks/{id}`、`GET /api/v1/tasks` 三个接口，5 种任务类型（article/diet/food_recommend/exercise/nutrition）注册表分发；全部 LLM 阻塞端点（chat/nutrition/article/diet/food_recommend/exercise/meal/voice/weekly/reflection/_call_llm）统一经 `run_in_thread` 卸载（等价 asyncio.to_thread，并发受控）。已验证：submit 立即返回 taskId，nutrition 6.2s/article 10.2s 后台完成，长任务执行期间 /health 仅 8ms 响应（事件循环不再被阻塞） |
| 6. Ollama 管理接口 | ✅ | 新增 [ollama_manager.py](ai_service/services/ollama_manager.py)：健康检查（/api/version）/ 模型列表（/api/tags + /api/ps 兼容新旧字段）/ 加载卸载（keep_alive）/ 上下文窗口运行时调整（内存覆盖，写入 settings 实时生效）/ 异步拉取模型；main.py 新增 `/api/v1/ollama/status`、`/models`、`/models/load`、`/models/unload`、`/models/pull`、`/context-window`(GET/POST) 七个接口；`ollama_pull` 注册进后台任务。已验证：加载 qwen2.5-7b-local 后 /api/ps 显示驻留（Q4_K_M/8192），卸载后 running=0，num_ctx 4096→8192 运行时切换生效 |
| 7. 统一混合检索 | ✅ | 复用 [retrieval_service.py](ai_service/services/retrieval_service.py) 统一入口：`hybrid_kb_context`（受检索判断器门控）+ 底层 [retriever.hybrid_retrieve()](ai_service/vector/retriever.py) 已实现 BM25 + BGE 向量 + RRF 融合（utils/retrieval_utils.py），全部 Agent 经此召回；`retrieve_knowledge`/`ingest_document`/`list_documents` 供 main.py 对外。已验证：/health 显示 agents_available 含 judge 检索判断器，C_direct 路线 retrieval_ms=3ms 快速召回 |
| 8. 前端 SSE 流式 | ✅ | main.py 新增 `POST /api/v1/chat/stream`：`StreamingResponse(text/event-stream)`，**双路径**——`high_performance=true` 走 [mode_router.stream_qa](ai_service/services/mode_router.py) 真流式（`orchestrator.chat_stream_setup` + `_iter_sync_stream` 桥接，逐字推送 `thinking`→`delta×N`→`done`，route=C_direct_stream）；`false` 保留完整业务链路（模板召回/模式路由/校验/存储）在线程池执行后按句子分块推送。后端 [AiConsultController.consultStream](backend-health/src/main/java/com/health/controller/AiConsultController.java) 经 HttpURLConnection 逐行透传 SSE 事件，`highPerformance` 布尔参数透传 AI 服务。已验证：curl 流式拉取 34 个 delta 分块，done 携带完整 meta |
| 9. 图表缓存 | ✅ | 新增 [useReportCache.ts](frontend-health/src/composables/useReportCache.ts) 组合函数：**防抖 300ms**（连续切换周期合并请求）+ **会话内缓存**（sessionStorage，TTL 30min）+ **按天粒度增量失效**（缓存 key 含操作身份+周期+日期，切换用户/切换周期/跨天自动重新请求）；[HealthReport.vue](frontend-health/src/views/dashboard/HealthReport.vue) 看板已接入，数据变更可调用 `invalidateReportCache` 主动失效。已验证：vue-tsc 类型检查通过、vitest 30/30 通过 |

#### 架构收敛专项（分析报告高优项，2026-08-09 补齐）

| 举措 | 状态 | 落地详情 |
|------|:----:|----------|
| main.py 拆分 | ✅ | 1578→234 行，7 个 routers 模块注册（chat/tasks/ollama/knowledge/agent/quality/dashboard） |
| llm._mode 竞态 | ✅ | [kb_dedup_service.py](ai_service/services/kb_dedup_service.py) `check_relevance_with_local_llm` 移除 `self.llm._mode` 临时改写，改为 `mode="local"` 参数局部生效 |
| AiChatService 拆分 | ✅ | 原 1480 行 10+ 职责类删除，按业务域拆为 6 个新服务（[AiChatClientService](backend-health/src/main/java/com/health/service/AiChatClientService.java) 通信域 / [AiChatContextBuilder](backend-health/src/main/java/com/health/service/AiChatContextBuilder.java) 上下文域 / [AiConsultService](backend-health/src/main/java/com/health/service/AiConsultService.java) 咨询域 / [AiNutritionService](backend-health/src/main/java/com/health/service/AiNutritionService.java) 营养域 / [AiExerciseService](backend-health/src/main/java/com/health/service/AiExerciseService.java) 运动域 / [AiContentService](backend-health/src/main/java/com/health/service/AiContentService.java) 内容域）；[CircuitBreaker](backend-health/src/main/java/com/health/util/CircuitBreaker.java) 提升为 @Component 单例共享熔断状态。已验证：Maven 编译通过、AiChatServiceTest 7/7 通过 |
| crowdType 枚举统一 | ✅ | DB CHECK 约束短名标准（普通人/健身/老年/孕妇/青少年/糖尿病），前端 CROWD_OPTIONS value 用短名，后端 buildDietReference switch 全按短名，buildPreCalculatedTargets 用 contains 兼容 |
| 巨石组件拆分 | ✅ | [RecipeLibrary.vue](frontend-health/src/views/dashboard/RecipeLibrary.vue) 1133→295 行（拆出 RecipeCard / RecipeGenerateDialog / RecipeDetailDialog + [useRecipeSubstitution.ts](frontend-health/src/composables/useRecipeSubstitution.ts)）；[Admin.vue](frontend-health/src/views/Admin.vue) 941→66 行（拆出 admin/AdminUserManager / AdminFoodManager / AdminArticleManager / AdminStats）；[ArticleDetail.vue](frontend-health/src/views/dashboard/ArticleDetail.vue) 837→644 行（拆出 ArticleSidebar + [articleRendering.ts](frontend-health/src/utils/articleRendering.ts)）。已验证：vue-tsc 类型检查通过、vitest 30/30 通过 |

### 4.2 阶段二：架构深度（必做，对应终极体系 1/2/4/5/6 核心）

**目标**：知识库五库隔离 + 双层聚类 + 文献合并/争议识别，人群策略深化，B 方案终极化。

| 举措 | 落地内容 | 对应体系 |
|------|----------|:--------:|
| 10. Agent 三层拆分 | 编排层 → 服务层 → Agent 层，按 4 大领域重组 | — |
| 11. 校验独立封装 | 事实/数值/安全校验独立成 `agent/validators/*`，结果结构化返回前端 | 体系 6 |
| 12. 知识库统一管理接口 | `KnowledgeBaseManager`：主题/人群/关键词/数量一键替换自动跑 | 体系 1 |
| 13. **五大库物理隔离** | 食物/膳食指南/运动/补剂/文献分物理 collection | 体系 1.1 |
| 14. **双层智能聚类** | BGE 粗筛 + 本地 LLM 语义精判 + 三层相似度分级 | 体系 1.2/1.3 |
| 15. **文献智能合并** | 同主题同结论 → 500 字复合权威卡片 | 体系 1.4 |
| 16. **学术争议识别** | 结论相悖文献自动归类打标，注入综述文稿 | 体系 1.5 |
| 17. **素材标准化 + 冷热分层** | 统一卡片规范 + 高频优先召回 | 体系 1.6/1.7 |
| 18. **B 方案终极化** | 本地只做框架不写正文 + 云端填正文 + 双阶段校验 + Token 前置预算 | 体系 4 |
| 19. **幻觉抑制六道防线** | 全链路校验闭环落地 | 体系 6 |
| 20. 多模型调度精细化 | 场景/负载/定时三层调度 + 高性能模式 | 体系 5 |
| 21. 双写定时对账 | `ai_templates` ↔ ChromaDB 定时/手动对账 | — |
| 22. SQLite 全库规范化 | 全部库走 `get_conn`（WAL + busy_timeout），补全索引 | — |

### 4.3 阶段三：创新加分（选做，对应体系 8）

| 举措 | 落地内容 | 说明 |
|------|----------|------|
| 23. 离线一键演示 | 一键启动 + 离线开关，无外网完整演示 | 现场无网可跑 |
| 24. 文献溯源跳转 | PMID 点击跳 NCBI + 国内镜像（PMC/期刊官网） | 提升可信度 |
| 25. 健康时序预测 | 体重/饮食/运动记录 → pandas 轻量模型预测趋势 | 本地计算 |
| 26. **营养素精准计算器** | 公式驱动计算，非 AI 瞎算 | 低工作量高展示 |
| 27. **知识库可视化看板** | 数量/分类/更新/争议文献统计大屏 | 答辩展示 |
| 28. 监控大屏 | 服务健康、模型状态、Token 消耗、模板进度 | 管理员视图 |

### 4.4 阶段四：长期规划（按需做/部分不做）

| 方向 | 决策 | 说明 |
|------|------|------|
| 语音录入三餐 | **做** | Web Speech API + 本地 Qwen 语义解析（倒装修正、多食材/一道菜拆分） |
| 多人群知识库拓展 | **做** | 妊娠期早/中/晚期、青少年长高、老年高血压、糖尿病分阶段 |
| 周报功能完善 | **做** | WeeklyReport 按模板沉淀 |
| 蓝牙手环对接 | **不做** | 小米/华为接口基本不开放，竞赛收益低 |
| 异步消息队列 | **不做** | 比赛场景文章可提前生成，无实时削峰诉求 |
| 爬虫继续扩充 | **不做** | 现有官方权威数据已足够，剔除酒精等不相关内容 |
| Redis 分布式缓存 | **不做** | 单机演示 Caffeine 足够 |

---

## 五、ABC 测试体系策略（决策记录）

| 决策 | 内容 |
|------|------|
| **现阶段** | 暂停 ABC 自动对比开发。知识库处于建设扩充期，素材不稳定，对比结论不可比、无意义 |
| **后续** | 知识库完全成熟后开启 AC 性能对标测试，验证本地 RAG + 本地模型媲美云端效果，作为报告数据支撑 |
| **依据** | 已有对比数据：B 方案 4983 字/98.9s/5004 tokens vs C 方案 5182 字/34.9s/4456 tokens（B 质量追平但耗时 2.8 倍），B 方案终极化（体系 4）将重点压缩这一差距 |

---

## 六、优先级与实施顺序

```
第 1 步（立即） 阶段一 1-9：异步化 + 缓存 + TraceId + SSE + 统一检索 + Ollama 管理
                 ├─ 前端体验立竿见影，稳定性可测
                 ▼
第 2 步（主力） 阶段二 10-22：五库隔离 + 双层聚类 + 文献合并/争议识别
                 ├─ 知识库终极体系（1/2/4/5/6）是答辩核心壁垒
                 ▼
第 3 步（冲刺） 阶段三 23-28：营养素计算器 + 知识库看板 + 离线演示 + 时序预测
                 ├─ 低工作量高展示度的加分项
                 ▼
第 4 步（持续） 阶段四：语音录入 / 多人群扩展 / 周报完善
                 ├─ 展示度高、实现成本可控
```

### 最简落地组合（时间紧时的保底）

若赛前时间紧张，建议至少完成：**① 五库物理隔离 + ② 双层智能聚类（文献合并/争议识别）+ ③ B 方案本地只做框架 + ④ SSE 流式 + ⑤ 离线一键演示 + ⑥ 营养素精准计算器**——这六项即可形成"远超普通 AI 项目"的垂直深度与演示效果。

---

## 七、优化前后预期对比

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 知识库检索 | 单库混存、有跨域噪声 | 五库隔离 + 双层聚类，噪声趋零 |
| 文献利用 | 单篇引用、无法验证 | 自动合并成 500 字复合卡片 + 争议识别 + PMID 预检 |
| 综述文稿专业度 | 共识罗列 | 含学术争议 / 多元研究观点章节 |
| AI 对话响应 | 整段等待 | SSE 流式逐字呈现，首字 <1s |
| 科普文章生成（B） | 98.9s / 5004 token | 本地只做框架后目标 <60s，token 成本 -15% |
| 人群策略 | 模板覆盖 7 人群 | 6 人群 × 5 BMI 组合策略 + 文献溯源 |
| 幻觉控制 | 基础校验 | 六道防线全链路卡控 |
| 现场演示 | 依赖外网、手动启三服务 | 离线一键演示 |
| 排障效率 | 无链路追踪 | TraceId 全链路贯穿 |

---

## 八、明确不做项与排除理由

| 项 | 排除理由 |
|----|----------|
| 蓝牙手环设备对接 | 小米/华为等主流品牌数据接口基本不开放，需厂商授权与硬件适配，演示现场难复现，竞赛收益低 |
| 异步消息队列（RabbitMQ/Kafka） | 项目为单机比赛部署，无高并发削峰诉求；科普文章可提前批量生成 |
| 爬虫继续扩充 | 现有知识库 610+ 篇文档已对照官方指南与权威文献，覆盖面足够；酒精等无关内容直接剔除 |
| Redis 分布式缓存 | 单机演示场景 Caffeine 进程内缓存已满足；引入 Redis 增加部署复杂度，偏商用 |
| 社交/电商等商业化功能 | 超出"个人健康助手"竞赛定位，不做 |

---

## 九、附录：多设备内网分布式部署（暂不落地，仅供后续决策）

> 讨论中曾提出的扩展方向：按功能拆分到多台内网设备，各司其职，形成"算力池"。

### 9.1 方案设想

| 设备角色 | 职责 |
|----------|------|
| 大模型节点（1 台或多台） | 运行 Ollama，可多设备并行跑本地模型，支持升级至 **14B 参数**模型 |
| 后端节点 | Spring Boot 业务服务 |
| 前端节点 | Vue3 静态资源 + 网关 |
| 爬虫/数据节点 | 素材搜集与知识库建设 |

### 9.2 潜在收益

- 多本地大模型负载均衡，突破单机 6GB 显存（RTX 3060）限制，可上 14B 模型
- 异步处理天然契合（长任务分发到独立节点）
- 算力冗余：单节点故障不影响整体演示

### 9.3 决策结论

**当前不落地。** 原因：① 竞赛演示以单机便携、一键启动为第一优先；② 多设备部署增加现场网络/设备依赖，反而降低演示可靠性；③ 14B 模型收益未经验证。**若后续确定为加分项再评估**，落地时优先考虑"大模型节点 + 主节点"最小双机方案。

---

## 十、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-08 | v1.0 | 基于外部方案逐条评估，形成四阶段 20 项优化方案 |
| 2026-08-08 | v2.0 | 整合终极理想版方案：升级为十大体系 + 四阶段 28 项举措；新增多设备部署附录（暂不落地） |
