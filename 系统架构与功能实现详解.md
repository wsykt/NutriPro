# 个人健康助手 · 系统架构与功能实现详解

> 版本：v5.0 ｜ 更新时间：2026-08-09
> 本文档在《优化后项目架构说明.md》v4.0 基础上扩展，补齐**各功能模块的具体实现方式**，并收录五库物理隔离、KnowledgeRouter 路由、三级检索粒度、Reranker 精排、健康时序预测、离线一键演示、监控大屏等最新能力。

---

## 1. 系统总览

### 1.1 三子系统架构

| 子系统 | 技术栈 | 目录 | 端口 |
|---|---|---|---|
| 前端 | Vue 3 + Vite + TypeScript + Element Plus + Pinia + ECharts | `frontend-health/` | 5173 |
| 后端 | Spring Boot 2.7 + Hibernate + SQLite（JSP 时代遗留已迁移） | `backend-health/` | 8082 |
| AI 服务 | FastAPI + ChromaDB + sentence-transformers + Ollama + scikit-learn | `ai_service/` | 8002 |

```
浏览器(5173)
   │  HTTP + SSE
   ▼
后端 Spring Boot (8082) ──HTTP──► AI 服务 FastAPI (8002)
   │ JWT 鉴权/业务编排            │ ChromaDB 五库 + 向量检索
   │ SQLite (health.db)          │ Ollama 本地大模型 (qwen2.5:7b)
   │                             │ DeepSeek 云端 API（在线模式）
   ▼
AI 服务内部：
   编排层 orchestrator ─► mode_router（模板/本地/云端 三路）
        │
        └─► knowledge_router（意图→集合→ACL→五库并行检索）
              └─► retriever（BM25 + 向量 双路融合）
                    └─► reranker（bge-reranker-base 交叉编码精排）
```

### 1.2 关键设计原则

- **前端只做展示**：所有业务/AI 逻辑在后端与 AI 服务，前端通过 REST + SSE 消费
- **AI 服务独立演进**：检索、生成、文章、知识库沉淀均在 AI 服务侧，后端仅做鉴权代理
- **检索按域隔离**：知识库按 食物/指南/人群/文献/模板 五库物理隔离，配合 ACL 控制可见域
- **三路生成策略**：模板召回 → 本地改写（B）、纯云端（C）、纯本地（A），按场景自动路由
- **纯本地可演示**：`LLM_MODE=local` 一键切换离线模式，全部能力不依赖外部网络

---

## 2. 后端（Spring Boot）功能与实现

### 2.1 认证与权限

**实现链路**：`JwtUtil`（生成/校验 JWT）→ `JwtFilter`（OncePerRequestFilter 拦截）→ `SecurityConfig`（无状态会话 + 放行白名单）。

- 登录/注册颁发 JWT，前端存 localStorage，请求头带 `Authorization: Bearer <token>`
- 角色 `role IN ('user','admin')`，管理员可访问 `/admin` 后台
- 家庭关系：监护人（`family_relation` 表）可查看/操作被监护人数据，Controller 层统一做本人或监护人校验
- 时间戳格式：SQLite 兼容 `yyyy-MM-dd HH:mm:ss`（空格分隔、无毫秒），避免 Hibernate `Error parsing time stamp`

### 2.2 用户健康档案

**实现要点**（`user` 表）：
- 字段强约束：`gender IN ('男','女')`、`age 0-150`、`height 0-300cm`、`weight 0-300kg`、`crowd_type` 枚举 11 类人群（普通人/健身/老年/孕妇/青少年/糖尿病/高血压/肿瘤预防/素食/更年期女性/心血管）
- **BMR 计算**（`NutritionCalculator`）：
  - 男性：`10×体重 + 6.25×身高 − 5×年龄 + 5`
  - 女性：`10×体重 + 6.25×身高 − 5×年龄 − 161`

### 2.3 饮食管理

- **食物库**（`food` 表 + `kb_food` 向量库）：9 大分类（主食/肉蛋类/水产/蔬菜/水果/豆制品/奶类/油脂类/调味品）
- **营养字段固定**：热量、蛋白质、碳水、脂肪、膳食纤维、GI值、钙、叶酸、DHA（仅此 9 项）
- **名称必须标注生/熟**（如「普通大米(生)」「鸡胸肉(熟)」），菜谱配料默认按生重
- **priority 四级分层**：顶级常用(11) > 常用(10) > 一般(5-9) > 冷门(0)，控制搜索结果排序
- **营养可见性开关**（`showGi/showFolicAcid/showDha`）：
  - GI值：主食/蔬菜/水果/豆制品/奶类/零食可见，肉蛋/水产/油脂隐藏
  - 叶酸：主食/水产/蔬菜/豆制品可见
  - DHA：仅水产可见

### 2.4 营养报告

`analyzeDiet`（`NutritionReportService`）按天聚合饮食记录，返回：
- BMR、`bmrRatio`（摄入/BMR）、`bmrStatus`（不足/适宜/超标）
- 营养合计：钙、叶酸、膳食纤维、DHA + 达标建议
- 结果落库 `nutrition_report` 表，前端图表（`useReportCache.ts` 缓存 30 分钟）

### 2.5 菜谱与替换

- 菜谱库 `recipe` 表含**健康标签**：孕妇/糖尿病/老年人/青少年/高血压/减脂/健身/低GI/高蛋白/均衡；禁止高热量不健康菜（如红烧肉）
- **食材替换**（`useRecipeSubstitution.ts` + 后端校验）：替换后营养同时展示「整份合计」与「每 100g」两套数值；替换猪五花→鸡胸时蛋白值会相应上调
- 前端拆分为 `RecipeCard/RecipeGenerateDialog/RecipeDetailDialog` + 替换组合函数

### 2.6 AI 咨询转发（SSE 代理）

**实现链路**（`AiConsultController` → `AiChatClientService` → AI 服务 `/api/v1/chat/stream`）：

```
前端 fetch(SSE) → 后端 POST /api/ai/consult → AiChatClientService
   ├─ 组装会话上下文（历史消息 + 健康档案摘要）
   ├─ RestTemplate 请求 AI 服务（连接超时 5s / 读超时 30s）
   ├─ CircuitBreaker 熔断（连续 3 次失败 → 60s 冷却，期间直接降级）
   └─ SseEmitter 流式转发（UTF-8 显式编码，防中文乱码）
```

- 支持 `targetUserId` 代理操作（监护人代问）
- AI 服务地址固定 `http://localhost:8002/api/v1/chat`（非 8003、非根路径）
- 所有菜谱/方案生成必须走 AI 编排层，禁止前端直连

### 2.7 健康时序预测（v5.0 新增）

`BodyMetricsHistoryService.predictWeightTrend(userId, days)`：

- 取历史体重序列，**最少 2 个点**才可回归（否则返回 `status=insufficient_data`）
- **最小二乘线性回归** `y = a + b·x`（x 为相对首日的天数序号，b 为日均体重变化斜率）
- **置信区间**：残差标准差 `sd` → `width = 1.96 × sd × (1 + d/3)`（d 为预测天数，区间随预测距离外扩）
- 趋势判断：周变化 `|weeklyChange| < 0.3kg` → 稳定；`<0` 下降；`>0` 上升
- 端点：`GET /api/metrics/predict/{userId}?days=7`（days 限制 1~30），本人或监护人校验

### 2.8 科普文章

- 生成走 AI 服务 `POST /api/v1/article/generate`（B 流水线，见 4.11）
- **质量分析 5 维度**：结构完整性、证据支撑、可读性、准确性、受众适配；AI 评分仅存库供管理员后台查看，不对用户展示
- 术语显示规则：中文术语首现带英文缩写（如「单不饱和脂肪酸（MUFA）」），纯英文缩写直接显示（EPA/DHA）
- 目录编号：一级中文「一、二、三」，二级「（一）（二）」，速读卡/深度文/综述文统一编号格式
- 三层导航：主题列表 → 篇幅选择（速读/深度/综述）→ 文章详情

### 2.9 数据库

- **SQLite** `data/health.db`，Hibernate 方言 `org.sqlite.hibernate.dialect.SQLiteDialect`
- 关键表：user / food / recipe / diet_record / nutrition_report / article / article_quality / family_relation / body_metrics_history
- JSON 属性名与 Java 字段同名（禁用 snake_case 转换，`property-naming-strategy` 曾引发前端绑定错误）

---

## 3. AI 服务（FastAPI）功能与实现

### 3.1 服务入口与端点

`main.py` 聚合多个 router：

| 端点 | 功能 |
|---|---|
| `POST /api/v1/chat` | 健康问答（非流式） |
| `POST /api/v1/chat/stream` | 健康问答（SSE 流式） |
| `POST /api/v1/diet/plan` | 一日饮食方案 |
| `POST /api/v1/food/recommend` | 食材菜谱推荐 |
| `POST /api/v1/exercise/advice` | 个性化运动方案 |
| `POST /api/v1/nutrition/analyze` | 饮食营养分析 |
| `POST /api/v1/meal/parse` | 饮食记录解析 |
| `POST /api/v1/retrieve` | 知识检索（含 doc_level） |
| `POST /api/v1/article/generate` | 科普文章生成 |
| `POST /api/v1/report/weekly-summary` | 周报 |
| `POST /api/v1/reflection` `/health/reflection` | 反思 |
| `POST /api/v1/food/audit` `/voice/parse` | 食物审计 / 语音解析 |
| `GET /api/v1/knowledge/stats` | 知识库统计（含五库分布 collections） |
| `GET /api/v1/agent/stats` `GET /api/v1/quality/stats` | Agent / 质量监控 |
| `GET /dashboard/` | 监控大屏（静态页） |

### 3.2 四大 AI 功能链路

统一入口为 `orchestrator`（意图分发）→ 各 Agent 子模块。四功能：

1. **健康问答**（`agent/qa.py`）：问题 + 健康快照 + 检索知识 → LLM 回答
2. **一日饮食方案**（`agent/diet_plan.py`）：健康快照 + 模板召回 → 按目标人群生成三餐+加餐方案
3. **食材菜谱推荐**（`agent/food_recommend.py`）：快照 + 食物库检索 → 菜谱推荐（ACL 无文献库权限）
4. **个性化运动方案**（`agent/exercise_advice.py`）：快照（含运动史）+ 检索 → 运动计划

> 所有推荐**必须同时考虑运动数据与身体指标**，不能只看摄入。

**健康快照（七模块）**：用户画像（姓名/性别/年龄/身高/体重/BMI/人群标签）、当前身体指标、7 天趋势、当前饮食、7 天运动数据、参考知识、会话意图识别——由 `AiChatContextBuilder`（后端）提供数据、AI 服务 `orchestrator` 组装成 prompt。

### 3.3 模式路由（mode_router）

四功能统一走 `mode_router.route()`，三路生成策略：

| 模式 | 触发条件 | 实现 |
|---|---|---|
| **B 方案**（默认） | 有模板命中 | 模板召回 → **本地 Ollama 二次改写**（知识来自模板库，不重新外呼） |
| **C 方案** | 模板未命中 | 云端 DeepSeek 生成 + 本地格式校验 |
| **A 方案** | 离线（Ollama 不可用） | 纯本地模板改写，不依赖云端 |
| **高性能模式** | 请求 `high_performance=true` | 直接 C 方案云端生成，跳过本地校验，超时放宽至 45s |

路由细节：
- 模板召回**限定 `kb_templates` 库**（`collections=["kb_templates"]`），去重查询同理
- 返回结果带 `_meta`（mode、routing path），前端展示「本地/云端/模板」来源
- 模板卡片权威镜像在 `data/templates.db`，`template_store.reconcile()` 启动时单向回补到 ChromaDB

### 3.4 五库物理隔离（v5.0 核心）

知识库拆分为 5 个独立 ChromaDB 集合（共 4625 条，旧集合 `health_knowledge` 保留只读）：

| 集合 | 数量 | 内容 | ACL 可见 Agent |
|---|---|---|---|
| `kb_food` | 1358 | 食物营养数据 | qa / diet_plan / food_recommend / exercise |
| `kb_guide` | 241 | 官方膳食指南 | qa / diet_plan |
| `kb_crowd` | 530 | 人群营养建议 | qa / diet_plan / food_recommend |
| `kb_literature` | 1076 | PubMed 文献 | qa（仅问答） |
| `kb_templates` | 1420 | AI 生成模板 | mode_router 专用 |

- 向量模型统一 `bge-base-zh-v1.5`（`models/bge-base-zh-v1.5/`）
- 每个集合独立 BM25 索引，检索前按需重建

### 3.5 KnowledgeRouter 路由层（v5.0）

`knowledge_router.py` 职责：

1. **意图检测** `detect_intent(query)` → 分类（qa/diet_plan/food_recommend/...）
2. **集合映射** `INTENT_COLLECTIONS`：意图 → 允许的集合列表
3. **ACL 校验** `AGENT_ALLOWED_COLLECTIONS`：Agent 无权集合直接剔除（如 food_recommend 不含文献库）
4. **多库并行检索** `parallel_retrieve(query, top_k, target_crowd, agent, doc_level)`：每个库按域独立 top_k 检索
5. **轮询合并** `_round_robin_merge`：跨库结果交错合并，避免单一来源垄断

冒烟验证示例：
- 「糖尿病怎么吃」→ `[kb_crowd, kb_guide, kb_food]` 三库并行
- 「菠菜含铁量」→ `[kb_food, kb_literature]`
- 带 `target_crowd=孕妇` → 人群元数据过滤生效

### 3.6 三级检索粒度（v5.0）

`doc_level` 元数据标记 + 检索过滤：

| 粒度 | 含义 | 识别词 | 覆盖集合 |
|---|---|---|---|
| `document` | 整篇全文 | 全文/整篇/完整版 | kb_guide / kb_crowd（无 chunk_index） |
| `paragraph` | 段落/条文 | 段落/片段/条文 | kb_guide / kb_crowd（有 chunk_index） |
| `fact` | 单条数据 | 数据/数值/单条 | kb_food / kb_literature / kb_templates |

- `backfill_doc_level.py` 幂等回填：guide/crowd 按 `chunk_index` 区分段/文，其余集合全标 fact
- 检索时 `where: {"doc_level": level}` 与人群条件 `$and` 组合过滤
- API `/api/v1/retrieve` 支持 `doc_level` 参数

### 3.7 BM25 + 向量混合检索

`retriever.hybrid_retrieve()`：

```
候选集 = top_k × RERANKER_CANDIDATE_MULTIPLIER(3)
   ├─ 向量路：ChromaDB 相似度检索（top_k×3）
   └─ BM25 路：每库独立 BM25（rank_bm25）按 doc_level 过滤后取 top_k×3
        ↓
   RRF 融合（recursive_rank_fusion：score = Σ weight_i / (k + rank_i + 1)，k=60）
   ↓
   去重 + 权威加权（卫健委/国标等来源权重 ×1.2）+ 内容截断
   ↓
   Reranker 精排（见 3.8）
   ↓
   取 top_k
```

- BM25 路每库独立索引 → 独立 top_k → 合并，避免跨库污染
- 向量路失败自动降级为纯 BM25，混合失败降级纯向量

### 3.8 Reranker 精排（v5.0）

`vector/reranker.py`（`LocalReranker`）：

- 本地 `bge-reranker-base` 交叉编码模型（`models/bge-reranker-base/`，~1.1GB）
- **懒加载单例**：首次检索才加载（线程安全），模型缺失/加载失败/推理异常一律**降级为原排序**
- 打分：`(query, content)` 对 → `CrossEncoder.predict`，输出 `rerank_score`
- 候选集放大 3 倍喂给 reranker，精排后取 top_k，结果保留 `rerank_score` 供前端展示
- 配置：`RERANKER_ENABLED / RERANKER_MODEL_PATH / RERANKER_CANDIDATE_MULTIPLIER`

### 3.9 知识库构建与回流

- **采集 Agent**：域名白名单 + robots 协议 + 请求限速，只存公开权威摘要
- 检索源带通道标签（PubMed / 官方指南 / 权威报告）便于溯源；单主题在线检索 ≤ 10 篇
- **知识卡标准**：150-250 字、结构化、只提取 4 类信息（核心共识/量化数据/适用人群/局限争议）
- **向量库只存原始权威材料**，AI 生成内容严禁入库（防幻觉自引用循环）
- 新网络素材生成后**立即回流入库**；`template_store.reconcile` 模板镜像单向回补

### 3.10 文章流水线（B 流水线）

```
知识库卡片提取 → 轻量提纲 → 局部增量精修 → 定向事实核查（≤3-4 轮）
   ├─ 本地模型只出"空白标签骨架/固定标题/章节标记"（不写正文）
   ├─ 云端 API 填充正文
   └─ 本地模型校验结构（标签完整性/字数/顺序）
```

- 事实核查 Agent：NCBI API 校验 PubMed ID 真实性 + 标题/期刊/年份一致性 + 声明与来源一致性
- **增量修补**替代整文重写，显著降低 token 消耗
- 质量分析 → 问题识别 → 反馈收集 → prompt 注入 → 再生成（自纠错闭环）
- 结构化结果 `_meta` 含 mode 与路由路径

### 3.11 本地大模型（Ollama）

- 模型：`qwen2.5:7b-instruct-q4_K_M`（4-bit 量化，适配 6GB RTX 3060）
- 上下文窗口 4096/6000/8000（非 2048），本地超时阈值 45s/60s/90s（防过早回退云端）
- 离线模式 `LLM_MODE=local` 强制走本地，Ollama 需代理时设置 `HTTPS_PROXY/HTTP_PROXY`

### 3.12 监控大屏（v5.0）

`dashboard/index.html`（FastAPI 静态托管，`http://localhost:8002/dashboard/`）：

- 健康检查 `/health`（12 个 Agent 状态）
- 知识库总览：总文档数 + 分类/来源统计 + **五库物理分布图**（`vector_db.collections`）
- Agent 监控 + 回答质量统计 + 检索试玩 + 聊天测试
- **检索试玩增强**：支持人群下拉 + **三级粒度选择**（全文/段落/数据事实），结果展示命中集合 tag + rerank 重排分 + 相似度
- **file:// 直开兼容**：`window.location.origin` 为 file: 时回退 `http://localhost:8002`，支持 `?api=` 参数覆盖

---

## 4. 前端（Vue 3）功能与实现

### 4.1 页面清单（路由）

- 公开：`/`、`/login`、`/register`、`/forgot-password`、`/admin`（管理员）
- 用户区 `/dashboard/*`：profile、food-input、food-add、nutrition、food-search、family-input、ai-consult、training-plan、near-gym、family-relation、metrics-history、health-history、recipe-detail、recipe-library、dietary-profile、health-report、muscle-chart、articles、article-detail、icon-gallery 等

### 4.2 API 层与状态管理

- `api/index.ts` 统一 axios 实例（baseURL `/api`，后端 dev 代理转发 8082），JWT 拦截器注入
- Pinia 管理用户态；组合函数（composables）承载业务逻辑，页面组件只做渲染

### 4.3 AI 咨询流式渲染

`api/index.ts` 的 `streamChat`（SSE 客户端实现）：

```
fetch(stream) → res.body.getReader() → TextDecoder('utf-8')
   → 按 \n\n 切分事件
   → 解析 event:/data: 行（thinking / message / done / error）
   → 增量写入气泡（思考中… → 逐字渲染）
```

配合 `AbortController` 支持停止生成。后端 SseEmitter 显式 UTF-8 编码保证中文不乱码。

### 4.4 指标历史与时序预测（v5.0）

`MetricsHistory.vue`：

- 强度折线图（ECharts）+ 下方「健康时序预测」卡片
- 调用 `GET /api/metrics/predict/{userId}?days=7`，渲染：
  - 趋势徽章（上升/下降/稳定）+ 说明文字
  - **置信区间带**（SVG path 填充 lower~upper）+ **虚线预测线**（真实数据之后）
  - `-7/+7` 天按钮切换预测周期
- 不足 2 条体重记录时显示「暂无足够数据」兜底
- `predictionPoints` computed 显式类型标注（修 TS7006）

### 4.5 图表缓存（useReportCache.ts）

- 防抖 300ms + sessionStorage TTL 30 分钟 + 按天增量失效
- 缓存 key 含「身份 + 周期 + 日期」，切换身份/跨天自动失效

### 4.6 组件化拆分（优化后）

| 巨石组件 | 行数 | 拆分结果 |
|---|---|---|
| RecipeLibrary | 1133→295 | RecipeCard / RecipeGenerateDialog / RecipeDetailDialog + useRecipeSubstitution.ts |
| Admin | 941→66 | admin/ 下 4 个管理器组件 |
| ArticleDetail | 837→644 | ArticleSidebar + utils/articleRendering.ts |

---

## 5. 数据存储总览

| 存储 | 位置 | 用途 |
|---|---|---|
| SQLite | `backend-health/data/health.db` | 业务数据（用户/饮食/菜谱/文章/指标） |
| SQLite | `ai_service/data/templates.db` | 模板卡权威镜像 |
| ChromaDB | `ai_service/vector_db/` | 五库向量（kb_food/guide/crowd/literature/templates） |
| 模型 | `ai_service/models/` | bge-base-zh-v1.5（嵌入）+ bge-reranker-base（精排） |
| Ollama | 本机 | qwen2.5:7b-instruct-q4_K_M（本地生成） |

---

## 6. 部署与启动

### 6.1 在线模式（默认）

依次启动：Ollama（可选）→ AI 服务（`python main.py`，8002）→ 后端（JDK8 + jar，8082）→ 前端（`npm run dev`，5173）。

### 6.2 离线一键演示（v5.0）

`start_demo_offline.ps1`：

- 强制 `LLM_MODE=local`（不依赖 DeepSeek 云端）
- 自动拉起 Ollama → AI 服务 → 后端 → 前端，含端口等待/自检（`Check-Offline`：LLM 链路、五库 `total_docs`+`collections`、三服务健康）
- 参数：`-SkipOllama` / `-StopAll`
- 注：PS 5.1 解析中文脚本需 UTF-8 BOM

### 6.3 内网多机部署

见《内网分布式多机部署指南.md》方案 A/B（AI 服务与业务服务可拆分部署，⑤⑥步按需跳过）。

---

## 7. 关键设计决策与约束（ADR）

| 决策 | 理由/约束 |
|---|---|
| SQLite + SQLiteDialect | 本地单文件，零运维；禁用 snake_case JSON 转换 |
| 五库物理隔离 | 域分离 + ACL，防跨域污染；旧集合只读 |
| 模板→本地改写为主 | 省云端 token（B 流水线对结构化任务省 25~58%） |
| Reranker 候选集 3 倍 | 保证精排候选充足；模型缺失自动降级不影响主流程 |
| 熔断 3 次 → 60s 冷却 | AI 服务抖动时后端快速降级，避免雪崩 |
| 向量库只存权威原文 | 防 AI 生成内容回流导致幻觉自引用 |
| 文档/段落/事实三级粒度 | 按用户提问精度分层召回，减少无关片段 |
| 时序预测最小 2 点 | 数据不足返回明确状态，不产生误导性预测 |

---

*关联文档：`优化后项目架构说明.md`（v4.0）、`项目优化方案报告.md`、`内网分布式多机部署指南.md`、`start_demo_offline.ps1`*
