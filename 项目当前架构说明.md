# 个人健康助手 — 项目当前架构说明

> 版本：v1.2（实况版）
> 日期：2026-08-10
> 说明：本文档描述**当前代码实际落地状态**的架构，与[优化后项目架构说明.md](优化后项目架构说明.md)（目标架构 v3.0，含 ⭐ 规划中 / ◎ 预埋项）互为对照：本文档只记录"已存在于代码中、可运行验证"的内容。
> 建表演进见 [SQL建表演进报告.md](backend-health/SQL建表演进报告.md)。

---

## 一、总体架构

```
┌──────────────────────────────────────────────────────────────┐
│              Vue 3 前端（frontend-health :5173）              │
│  登录/注册 · 健康档案 · 饮食记录 · 营养报告 · 食谱             │
│  科普文章（三梯度）· AI 咨询 · 运动记录 · 社区                  │
│  └─ 通过 vite proxy 转发 /api → 后端 8082（无跨域）            │
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTP REST + JWT + X-Trace-Id
┌───────────────────────────▼──────────────────────────────────┐
│          Spring Boot 后端（backend-health :8082）              │
│  ├─ config/   AsyncConfig ✅ · CacheConfig ✅ · TraceIdFilter ✅│
│  │            SecurityConfig · RestClientConfig · LogAspect    │
│  ├─ security/ JwtFilter（写入 userId MDC）✅ · JwtUtil          │
│  ├─ controller/ AiConsult · Article（含异步生成）✅ · Food ·    │
│  │            Recipe · Diet · Exercise · NutritionReport ·    │
│  │            Auth · Profile · Community · Admin · Family ·    │
│  │            BodyMetrics                                     │
│  ├─ service/  AiChatClientService（HTTP 通信）·                │
│  │            AiConsultService · AiNutritionService ·         │
│  │            AiExerciseService · AiContentService ·          │
│  │            AiChatContextBuilder · AsyncArticleTaskService ✅│
│  │            Food/Recipe（Caffeine 缓存）✅ · 其余业务服务      │
│  └─ util/     RagVectorSearchUtil · NutritionCalculator ·      │
│               ArticleSplitUtil                                │
│  数据库：SQLite health.db（ddl-auto: update + 演进脚本）✅      │
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTP REST（TraceId 透传）
┌───────────────────────────▼──────────────────────────────────┐
│            Python AI 服务（ai_service :8002）                  │
│  ├─ main.py    REST + SSE ✅ + 异步任务接口 ✅ + Ollama 管理 ✅  │
│  │             + 全局入参校验中间件                            │
│  ├─ agent/     编排层 orchestrator + 14 个 Agent              │
│  │             + validators/（fact/numeric/safety/validator） │
│  ├─ services/  async_task_service ✅ · ollama_manager ✅       │
│  │             retrieval_service（统一混合检索）✅ ·            │
│  │             mode_router（智能路由）· template_store ✅ ·    │
│  │             kb_dedup_service                               │
│  ├─ vector/    retriever（BM25+BGE+RRF 混合检索）✅ · embedder │
│  ├─ llm/       router（云端 DeepSeek / 本地 Ollama 双模式）    │
│  ├─ conversation/ 对话存储 + 记忆提取                          │
│  └─ local_fallback_engine.py · pipeline_v32.py               │
└──────────┬─────────────┬─────────────┬────────────┬──────────┘
           ▼             ▼             ▼            ▼
   ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
   │  ChromaDB  │ │  SQLite×5  │ │   Ollama   │ │  DeepSeek  │
   │ 知识库     │ │ food/conv/ │ │ qwen2.5-7b │ │ 云端 API   │
   │ (单 collection│ │ templates/ │ │  (本地)    │ │  (远程)    │
   │  + 模板卡片)│ │ token/mem  │ │ 11434      │ │            │
   └────────────┘ └────────────┘ └────────────┘ └────────────┘
```

---

## 二、技术栈与端口

| 服务 | 技术栈 | 端口 | 说明 |
|------|--------|:----:|------|
| 前端 | Vue 3 + TypeScript + Vite | 5173 | 请求经 vite proxy 转发至 8082 |
| 后端 | Spring Boot 2.7 + SQLite + JPA(Hibernate) + Caffeine | 8082 | 业务与 AI 网关，AI 转发前缀 `/api/v1` |
| AI 服务 | FastAPI + ChromaDB + sentence-transformers + loguru | 8002 | 全部 AI 能力（对话/营养/食谱/运动/文章） |
| 本地模型 | Ollama qwen2.5:7b-instruct-q4_K_M | 11434 | 离线/隐私场景与 B 流水线骨架 |
| 云端模型 | DeepSeek API | — | 高性能模式 C 直连与 B 流水线正文 |

---

## 三、分层详解

### 3.1 前端（frontend-health）

- 页面：登录注册、健康档案（Profile）、饮食记录（FoodInput/Meal）、营养报告、食谱推荐、科普文章（三梯度详情）、AI 咨询（AiConsult）、运动记录、社区、管理后台（Admin）。
- AI 接口定义于 [api/index.ts](frontend-health/src/api/index.ts)：`/ai/consult`、`/ai/nutrition/analyze`、`/ai/food/audit`、`/ai/voice/parse`、`/ai/report/weekly`、`/ai/article/generate`、`/ai/diet/plan`、`/ai/recipe/recommend`、`/ai/exercise/advice`、`/ai/meal/parse` —— 均为普通 POST（当前**尚未接入 SSE 流式**）。
- 文章列表 → 主题卡片 → 三篇幅（速读/深度/综述）三层导航已实现。

### 3.2 后端（backend-health）

| 领域 | 关键类 | 说明 |
|------|--------|------|
| 安全 | `security/JwtFilter` + `JwtUtil` + `SecurityConfig` | JWT 认证；认证后 `MDC.put("userId")`（try/finally 清理） |
| 全链路可观测 | `config/TraceIdFilter`（HIGHEST_PRECEDENCE） | 生成/透传 16 位 traceId → MDC + `X-Trace-Id` 响应头 |
| 日志 | `resources/logback-spring.xml` | 三处 pattern 均输出 `[traceId=%X{traceId:-} userId=%X{userId:-}]` |
| 异步化 | `config/AsyncConfig` + `service/AsyncArticleTaskService` | `aiTaskExecutor`（核心2/最大8/队列20/CallerRunsPolicy + MDC 透传装饰器）；文章生成提交即返回 taskId，`@Async` 后台执行 |
| 缓存 | `config/CacheConfig` + `service/FoodService`/`RecipeService` | Caffeine（max 2000 / expireAfterWrite 10min / recordStats）；foodSearch/foodCategory/foodAllApproved/foodById 与 recipe 对应键 `@Cacheable`，写操作全量 evict |
| AI 网关 | `service/AiChatClientService`（HTTP 通信）+ `service/AiChatContextBuilder`（上下文组装）+ `service/CircuitBreaker`（@Component 单例） | 按业务域拆分：`AiConsultService`/`AiNutritionService`/`AiExerciseService`/`AiContentService` 分别处理咨询/营养/运动/文章；转发 AI 服务 `/api/v1/*`；RestTemplate 5s 连接/30s 读取 + 熔断（3 失败 → 60s 冷却，全局共享） |
| 数据库 | `application.yml`（ddl-auto: update）+ `DataInitializer` | SQLite `health.db`；启动时幂等执行 `db/index_optimizations.sql` |
| 业务 | Article/Food/Recipe/Diet/Exercise/NutritionReport/Auth/Profile/Community/Admin/FamilyRelation/BodyMetrics | 常规 CRUD + 营养计算 |

### 3.3 AI 服务（ai_service）

**API 面（main.py）**：

| 分组 | 端点 |
|------|------|
| 业务 | `/api/v1/chat`（含高性价比 SSE `/chat/stream`）、`/nutrition/analyze`、`/food/audit`、`/meal/parse`、`/voice/parse`、`/report/weekly-summary`、`/diet/plan`、`/food/recommend`、`/exercise/advice`、`/article/generate`、`/health/reflection` |
| 异步任务 | `POST /api/v1/tasks/submit`、`GET /api/v1/tasks/{task_id}`、`GET /api/v1/tasks`（5 类任务：article/diet/food_recommend/exercise/nutrition/ollama_pull） |
| Ollama 管理 | `/api/v1/ollama/status`、`/models`、`/models/load`、`/models/unload`、`/models/pull`、`/context-window`(GET/POST) |
| 系统 | `/health`、`/api/v1/agent/stats`、`/knowledge/stats` 等 |

**核心机制**：

- **全局入参校验中间件**：POST 必填字段校验 + 超长文本截断（`MAX_TEXT_LENGTH=500`），非法 JSON 统一 400。
- **编排层**（`agent/orchestrator.py`）：意图识别 → 模式路由（`services/mode_router.py`：模板召回→本地改写→本地校验为正常链路；`high_performance=true` 时 C 方案云端直连跳过校验；校验失败 C 方案兜底；离线 A 方案纯本地）。
- **异步化**（`services/async_task_service.py`）：`ThreadPoolExecutor`（max_workers=MAX_LLM_CONCURRENCY）+ 锁保护任务注册表（容量 200）；`run_in_thread` 替代 `asyncio.to_thread`，全部阻塞 LLM 端点经此卸载，事件循环不被阻塞。
- **统一混合检索**（`services/retrieval_service.py` + `vector/retriever.py#hybrid_retrieve`）：BM25 + BGE 向量 + RRF 融合，全部 Agent 统一经此召回；`hybrid_kb_context` 受检索判断器（judge）门控。
- **模板权威镜像**（`services/template_store.py`）：SQLite `templates.db` 与 ChromaDB 双写，启动 reconcile 单向回补；已生成 7 人群 × 5 BMI × 4 功能 = 1400 模板。
- **Ollama 管理**（`services/ollama_manager.py`）：原生 HTTP API（`/api/version`、`/api/tags`、`/api/ps`、`/api/generate`、`/api/pull`），兼容新旧版 `num_ctx` 字段；运行时切换上下文窗口并同步 settings。
- **LLM 路由**（`llm/router.py`）：cloud/local 双模式分流、超时重试、Token 日限流；`chat_json` 支持 temperature 透传。
- **统一校验入口**（`services/validation_service.py`）：对话/结构化输出委托三组件校验链（safety→numeric→fact），科普文稿独立校验（结构完整性、引用 PMID 一致性、绝对化用语、异常数值），`kind=response|article` 统一派发。
- **双层智能聚类**（`services/kb_cluster.py`）：BGE 余弦相似度批量粗筛 → 高/低阈值三层分级 → 灰色区间 LLM 语义终审，并查集聚簇，供合并/综述复用。
- **文献智能合并**（`services/literature_merge.py`）：同主题同结论卡片融合为 500 字复合权威卡片（四段式 + 全部 PMID 溯源），LLM 融合失败自动降级本地拼接。
- **学术争议识别**（`services/dispute_detect.py`）：两两判定结论关系（一致/互补/相悖/无关），相悖文献并查集聚类打 dispute 标签，`has_major_dispute` 供综述判断注入争议章节。
- **科普文章 B 流水线**（`pipeline_v32.py`）：Stage1 本地 Ollama 出框架 → Stage2 DeepSeek 外扩正文 → Stage3 本地格式校验（15 个【#标记#】），经 `/api/v1/articles/mother-draft` 暴露，后端 `ArticleService.generateAndSave` 调用；现库内 6 人群 × 4 主题 × 3 篇幅 = 72 篇。
- **人群标签体系**：前后端统一 6 类人群（健身人群/普通人群/青少年/老年人/孕妇/糖尿病患者），文章 audience 字段与前端 key/label 严格匹配。
- **术语否定前缀保护**（前端 `utils/articleRendering.ts`）：GLOSSARY 打标前检测「不/非」+ 术语复合词并占位保护，避免「不饱和脂肪酸」误标为「饱和脂肪酸」，且不影响「多不饱和脂肪酸」等合法词条。

---

## 四、数据存储

| 库 | 位置 | 用途 |
|----|------|------|
| health.db | backend-health/data/ | 业务主库（用户/饮食/食谱/文章/营养报告/运动/社区） |
| food.db | ai_service/data/ | 食物营养数据（9 大类，生/熟标注，GI/叶酸/DHA 可见性控制） |
| templates.db | ai_service/data/ | AI 模板权威镜像（1400 模板卡片） |
| conversations.db | ai_service/data/ | 对话记录 |
| token_usage.db | ai_service/data/ | LLM Token 用量统计 |
| user_memory.db | ai_service/data/ | 用户记忆 |
| cache.db | ai_service/data/ | 缓存 |
| ChromaDB | ai_service/knowledge/chroma_db_storage/ | 向量知识库（权威指南/文献卡片/模板嵌入） |

---

## 五、阶段一优化落地现状（8/9）

| 举措 | 状态 | 落地文件 | 验证结果 |
|------|:----:|----------|----------|
| 1. 后端异步化 | ✅ | AsyncConfig / AsyncArticleTaskService / ArticleController 异步端点 | 提交 196ms 返回 taskId，异步线程 `[ai-task-1]` 执行且 traceId 透传 |
| 2. 建表演进 | ⏳ | SC-001 已落地；SC-002~005 待做 | — |
| 3. 业务缓存 | ✅ | CacheConfig + Food/RecipeService | 二次搜索 34ms→1ms |
| 4. 全链路 TraceId | ✅ | TraceIdFilter + JwtFilter + logback | 日志 `[traceId=... userId=1]` |
| 5. AI 服务异步 | ✅ | async_task_service + main.py 任务接口 | 长任务期间 /health 仅 8ms |
| 6. Ollama 管理接口 | ✅ | ollama_manager + main.py 7 端点 | 加载/卸载/num_ctx 切换验证通过 |
| 7. 统一混合检索 | ✅ | retrieval_service + retriever.hybrid_retrieve | C_direct 路线 retrieval_ms=3ms |
| 8. 前端 SSE 流式 | 🟡 | **仅 AI 服务侧**：`/api/v1/chat/stream`（thinking→delta×N→done）| 34 个 delta 分块、done 携带完整 meta |
| 9. 图表缓存 | ⏳ | — | 待做 |

> 注：举措 8 的 AI 服务侧已就绪并通过验证，前端 `AiConsult` 页仍走普通 POST，**SSE 接入前端为待办**（可在演示前接入，也可保留分段流式能力供后端直连演示）。

---

## 六、关键链路

### 6.1 AI 对话（正常链路）

```
前端 /ai/consult → 后端 AiConsultController（JWT 鉴权 + TraceId）
  → AiChatClientService 转发 AI 服务 /api/v1/chat（CircuitBreaker 熔断保护）
  → orchestrator.chat：意图识别 → 模板召回（ChromaDB+SQLite 双写库）
  → 本地 Ollama 改写 → 本地校验 → 返回（校验失败 → C 方案云端兜底）
```

### 6.2 高性能模式（演示链路）

```
前端 high_performance=true → 模式路由 C 方案云端直连（DeepSeek）
  → 跳过本地校验（validation.skipped=true）→ _meta 携带 mode/route 信息
```

### 6.3 异步文章生成

```
前端 POST /ai/article/generate-async → 后端返回 taskId（196ms）
  → @Async aiTaskExecutor 后台执行（MDC traceId 透传）
  → 前端轮询 GET /task/{taskId} 获取 PENDING→RUNNING→SUCCESS/FAILED
```

### 6.4 SSE 流式（AI 服务侧，两阶段推送）

```
POST /api/v1/chat/stream → StreamingResponse(text/event-stream)
  → event:thinking（"正在检索知识库并生成回答..."）
  → 完整 orchestrator.chat 链路在线程池执行（事件循环不阻塞）
  → event:delta ×N（按句子边界分块，打字机效果）
  → event:done（conversation_id/response/provider/mode/route/elapsed）
  失败 → event:error
```

### 6.5 Ollama 管理

```
前端/运维 → /api/v1/ollama/* → Ollama HTTP API（11434）
  → 列表/驻留状态（兼容新旧 num_ctx）/加载(keep_alive=30m)/卸载/异步拉取/上下文窗口运行时切换
```

---

## 七、剩余待办

| 优先级 | 事项 | 说明 |
|:------:|------|------|
| 高 | 前端接入 SSE 流式 | AiConsult 页改用 `/api/v1/chat/stream`，事件名区分 thinking/delta/done |
| 高 | 建表演进 SC-002~005 | 补索引/唯一约束/演进脚本（详见 SQL建表演进报告） |
| 中 | 图表缓存（举措 9） | 看板接口防抖 + 前端缓存 + 增量更新 |
| 中 | 五库物理隔离 | ChromaDB 按领域拆分 collection（目标架构 §5.1） |
| 低 | Redis 三级缓存/任务队列 | 预埋，暂不落地 |

> 已完成（2026-08-09）：双层聚类（kb_cluster.py）、文献合并（literature_merge.py）、学术争议识别（dispute_detect.py）、统一校验入口（validation_service.py），17 项单测全通过。

---

## 八、与目标架构（优化后 v3.0）的差异

| 维度 | 当前实况 | 目标架构 |
|------|----------|----------|
| 知识库 | 单 collection + 模板卡片双写 | 五大物理库隔离 |
| 检索 | BM25+BGE+RRF 已统一 + 双层聚类 ✅ | + 领域定向路由（五库） |
| 校验 | validation_service 统一入口 ✅（对话+文稿） | 同左（已落地） |
| 缓存 | Caffeine 单机 | + Redis 三级缓存（◎） |
| 爬虫 | 素材已入库，框架存在 crawler/ | 独立进程 + Redis 4 队列 + 三档速率（◎） |
| 部署 | 单机三层 | 多设备局域网预留（◎） |
| 前端 | 普通 POST 对话 | + SSE 流式 + 图表缓存 |

---

## 九、变更记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-08-09 | v1.0 | 依据当前代码实况生成：阶段一 8/9 项落地后的三层架构、数据存储、关键链路、待办与目标差异 |
| 2026-08-09 | v1.1 | 落地目标架构服务层 4 组件（kb_cluster / literature_merge / dispute_detect / validation_service），17 项单测全通过；新增聚类/合并/争议配置项；待办与差异表同步 |
| 2026-08-10 | v1.2 | 同步最新落地：AI 服务按业务域拆分（AiChatClientService 等 6 类 + CircuitBreaker 组件）、B 流水线（pipeline_v32）、6 人群标签统一、术语否定前缀保护（articleRendering.ts）；移除已删除的参考文档链接 |
