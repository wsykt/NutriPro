# 个人健康助手《功能实现详解》

> 适用范围：health/ 下三个子项目 —— `backend-health`（Spring Boot 2.7 + JPA/SQLite，端口 8082）、`frontend-health`（Vue 3 + Pinia + Vue Router hash 模式 + axios，端口 5173）、`ai_service`（FastAPI + ChromaDB + Ollama，端口 8002）。
> 本文档按"页面 → 接口 → 调用链 → 设计"逐功能拆解实现细节，包含真实文件路径、接口路径、参数名与字段名，并列出已知缺陷。

---

## 目录

- [一、系统总览](#一系统总览)
- [二、用户认证](#二用户认证)
- [三、个人资料与身体指标](#三个人资料与身体指标)
- [四、健康报表与缓存设计](#四健康报表与缓存设计)
- [五、食物库](#五食物库)
- [六、饮食记录](#六饮食记录)
- [七、营养分析](#七营养分析)
- [八、运动记录与训练计划](#八运动记录与训练计划)
- [九、社区 / 家庭关系 / 管理后台](#九社区--家庭关系--管理后台)
- [十、AI 四大功能链路](#十ai-四大功能链路)
- [十一、菜谱推荐](#十一菜谱推荐)
- [十二、科普文章（知识科普）](#十二科普文章知识科普)
- [十三、已知问题与缺陷清单](#十三已知问题与缺陷清单)
- [十四、通用设计要点](#十四通用设计要点)

---

## 一、系统总览

### 1.1 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Pinia + Tailwind CSS + ECharts | hash 路由，axios 请求封装 |
| 后端 | Spring Boot 2.7 + Spring Data JPA + SQLite + Spring Security | BCrypt 密码、HS512 JWT、方法级 @PreAuthorize |
| AI 服务 | FastAPI + ChromaDB + SQLite + Ollama（qwen2.5:7b-instruct-q4_K_M）+ 云端 LLM | 模板召回 → 本地重写 / 云端直出 双方案 |

### 1.2 目录结构

```
health/
├── backend-health/            # Spring Boot 后端（端口 8082）
│   └── src/main/java/com/health/
│       ├── controller/        # 14 个 Controller
│       ├── service/           # 业务服务（含 AI 域 6 个服务）
│       ├── security/          # JwtUtil + JwtFilter
│       ├── util/              # NutritionCalculator / CircuitBreaker / ArticleSplitUtil
│       └── config/            # SecurityConfig / RestClientConfig / GlobalExceptionHandler
├── frontend-health/           # Vue 3 前端（端口 5173）
│   └── src/
│       ├── views/             # 登录注册 + Dashboard + Admin
│       ├── views/dashboard/   # 各功能页
│       ├── api/index.ts       # axios 统一封装
│       ├── stores/            # user/diet/recipe/article 四个 Pinia store
│       ├── composables/       # useReportCache / useRecipeSubstitution / useWorkoutRecords
│       └── utils/             # articleRendering / storage
└── ai_service/                # FastAPI AI 服务（端口 8002）
    ├── routers/chat.py        # 全部业务端点
    ├── agent/orchestrator.py  # 编排器（chat / process 双入口）
    ├── services/mode_router.py# A/C 双模式路由器
    ├── pipeline_v32.py        # 科普文章 B 流水线
    └── vector/retriever.py    # ChromaDB 混合检索
```

---

## 二、用户认证

### 2.1 注册流程

**前端** `src/views/Register.vue` → `userStore.register(form)`（`src/stores/user.ts`）→ `api.auth.register` → `POST /api/auth/register`。

表单字段：`username`、`password`、`gender`（默认'男'）、`height`（默认170）、`weight`（默认65）、`age`（默认20）、`crowdType`（默认'普通人'，选项来自 `src/constants/index.ts` 的 `CROWD_OPTIONS`）。注册成功跳转 `/login`。

**后端** `controller/AuthController.java` + `service/AuthService.java`：

1. 校验顺序：用户名非空 → 密码 ≥ 6 位 → `userRepository.existsByUsername` 查重；
2. `BCryptPasswordEncoder.encode()` 加密（`config/SecurityConfig.java` 注入）；
3. 非空字段写资料（gender/height/weight/age/crowdType），角色恒为 `'user'`；
4. 注册即签发 JWT，返回 6 个字段：`access_token`、`token_type: "bearer"`、`user_id`、`username`、`crowd_type`、`role`。

### 2.2 登录流程

`POST /api/auth/login`，body `{username, password}`。`AuthService.login` 对"用户不存在"与"密码不匹配"统一抛"用户名或密码错误"，成功返回与注册相同的 6 字段。

**前端登录后处理**（`src/stores/user.ts`）：
- token 双写 `localStorage['user_token']` 与 `localStorage['token']`；
- `user_id` 写 `localStorage['currentUserId']`；
- `setActAs(null)` 清空替亲属操作标记，随后 `loadWards()` 拉取"我监护的亲属"列表；
- `Login.vue` 根据 `userStore.isAdmin`（`role === 'admin'`）跳 `/admin`，否则 `/dashboard`。

### 2.3 JWT 签发与校验

**`security/JwtUtil.java`**：
- 配置（`application.yml`）：`health.jwt.secret`（可被环境变量 `JWT_SECRET` 覆盖）、`health.jwt.expiration = 86400000` 毫秒（**24 小时，无刷新机制**）；
- `generateToken(userId, username, role)`：**HS512** 签名，`sub = userId`，claims 含 username/role；
- `validateToken` 捕获所有异常返回 false（过期/篡改均视为无效，过期需重新登录）。

**`security/JwtFilter.java`（OncePerRequestFilter）**：
1. `OPTIONS` 预检放行；
2. 读 `Authorization: Bearer <token>` → `validateToken` → 按 `sub` 加载完整 `User` 实体 → 构造 `UsernamePasswordAuthenticationToken`（principal 是 **JPA 实体本身**，authority 为 `ROLE_` + role 大写）；
3. 写入 `SecurityContextHolder`，`MDC.put("userId")` 便于日志；
4. 解析异常时 `clearContext()` 但不阻断请求 → 后续 Controller 的 `extractUser(Authentication)` 拿到 null 或主逻辑因缺用户抛 NPE，由 `GlobalExceptionHandler` 转 **401**（隐式鉴权兜底，见缺陷清单）。

### 2.4 安全配置

**`config/SecurityConfig.java`**：
- `@EnableGlobalMethodSecurity(prePostEnabled = true)` → 仅 `AdminController` 使用 `@PreAuthorize("hasRole('ADMIN')")`；
- STATELESS、CSRF 关闭、CORS 允许 `http://localhost:*` 与 `http://127.0.0.1:*`；
- permitAll：`/api/auth/**`、`/api/food/search`、`/api/food/list`、`/api/recipes`、`/api/recipes/**`、GET `/api/articles/**`；`/api/articles/**` 的 POST/PUT/DELETE 要求登录；其余 `anyRequest().authenticated()`。

### 2.5 统一响应与异常

- `dto/ApiResponse.java`：`code(200)/message/data`，`success(data)` / `error(message)` / `error(code, message)`；
- `config/GlobalExceptionHandler.java`：`RuntimeException` → HTTP 400；`IllegalArgumentException` → 400；`NullPointerException` → **401** "未检测到有效登录信息，请重新登录"；`Exception` → 500。

### 2.6 前端 401/403 与路由守卫

- `src/api/index.ts` 响应拦截器：成功自动剥离 `body.data`；失败时 `status === 401 || 403`（排除 `/profile/info` 探活请求）→ 清除登录态并 `window.location.href = '/login'`；
- 路由守卫（`src/router/index.ts`）：`/dashboard` 与 `/admin` 带 `meta.requiresAuth`；`requiresAuth && !token → /login`；`'/' && token → /dashboard`；开发模式自动注入 `dev-mock-token` 与 `currentUserId=1`；
- 刷新页面时 `userStore.init()` 用 localStorage token 调 `/profile/info` 重建登录态。

---

## 三、个人资料与身体指标

### 3.1 资料接口（`controller/ProfileController.java`，前缀 `/api/profile`）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/profile/info` | query `targetUserId?` | 资料 Map + `operateAsUserId` |
| PUT | `/api/profile/update` | body `RegisterRequest` | 更新资料并写当日快照 |
| PUT | `/api/profile/dietary` | body `{allergicFoods, dietaryRestrictions, tastePreference, elderlyMode}` | 饮食档案 |
| POST | `/api/profile/snapshot` | query `targetUserId?`, `date?` | 手动快照（默认今天） |

### 3.2 用户表字段与约束（`entity/User.java` + `resources/db/init-schema.sql`）

| 字段 | 约束 |
|---|---|
| `user_id` | INTEGER PK 自增 |
| `username` | NOT NULL + 唯一（`uq_user_username`） |
| `password` | NOT NULL，BCrypt 哈希，`@JsonIgnore` |
| `gender` | DEFAULT '男' CHECK IN ('男','女') |
| `height` | REAL DEFAULT 165 CHECK 0–300 |
| `weight` | REAL DEFAULT 65 CHECK 0–300 |
| `age` | INTEGER DEFAULT 18 CHECK 0–150 |
| `crowd_type` | DEFAULT '普通人' CHECK IN ('普通人','健身','老年','孕妇','青少年','糖尿病') |
| `role` | DEFAULT 'user' CHECK IN ('user','admin') |
| `allergic_foods` / `dietary_restrictions` | 逗号分隔字符串 |
| `taste_preference` | DEFAULT '清淡' CHECK IN ('清淡','适中','重口味','微辣','辣') |
| `elderly_mode` | INTEGER DEFAULT 0 CHECK IN (0,1) |

### 3.3 更新与快照

`ProfileService.updateProfileWithSnapshot`：`@Transactional`；仅非空字段覆盖（null 跳过）；保存后把 **身高/体重/年龄/crowdType + BMR 快照进 `body_metrics_history`**（record_date=今天，同日 upsert）→ 保证趋势线有初始点。

`getUserInfo` 返回基础资料并实时计算 `bmr`（Mifflin-St Jeor）、`bmi`、`bmiStatus`（偏瘦/正常/超重/肥胖，阈值 18.5/24/28，见 `util/NutritionCalculator.java`）。

### 3.4 前端 Profile.vue

`loadInfo()` → `api.profile.getInfo()` 回填表单；保存 → `api.profile.update(...)` → 提示"已保存，并写入今日身体指标快照" → `userStore.loadUserProfile()` 刷新；页面实时计算 BMI 与档位。独立"快照"按钮已移除（保存即自动快照）。

### 3.5 身体指标历史与预测（前缀 `/api/metrics`）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/metrics/history/{userId}` | — | 全部记录按日期倒序 |
| GET | `/api/metrics/history/{userId}/range` | `startDate`,`endDate` | 区间记录 |
| GET | `/api/metrics/predict/{userId}` | `days`（默认7，1–30 夹取） | 体重趋势预测 |
| POST | `/api/metrics/save` | body `{recordDate, height, weight, age, crowdType, targetUserId?}` | 手动增/改某日记录 |
| DELETE | `/api/metrics/delete` | `recordDate`, `targetUserId?` | 删除某日记录 |

- 权限：本人或已确认监护人（`targetUserId` 校验）；
- `body_metrics_history` 表：`user_id` + `record_date` 唯一（`uq_bmh_user_date`）；
- **体重预测算法**（`BodyMetricsHistoryService.predictWeightTrend`）：需 ≥2 条带体重记录，否则 `status:"insufficient_data"`；一元线性回归 `y=a+b*x`（x 为相对首日天数）；置信带 `±1.96*sd*(1 + d/3)` 随预测距离放宽；输出 `points[]`（每点 date/predictedWeight/lower/upper）、`trend:"stable|down|up"`（周变化 <0.3kg 为 stable）。

### 3.6 前端图表（`views/dashboard/MetricsHistory.vue`）

- `loadData()` 优先 `api.metrics.range(uid, start, end)`，否则 `history(uid)`；uid 取 `userStore.activeUserId`（替亲属操作时为目标用户）；
- 手写 SVG 折线图（体重/身高），点可点击弹编辑框（增删改）；
- 预测区：`api.metrics.predict` → 虚线预测线 + 置信带；前端 `calcBmr` 与后端公式一致。

> 注：`HealthHistory.vue` 为占位页（硬编码假记录），无后端调用。

---

## 四、健康报表与缓存设计

### 4.1 营养报表接口（`controller/NutritionReportController.java`，前缀 `/api/report`）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| POST | `/api/report/save` | body：analyzeDiet 结果 Map + `reportDate` | 保存/更新某日报告 |
| GET | `/api/report/date/{date}` | — | 某日报告 |
| GET | `/api/report/list` | — | 全部报告（倒序） |
| GET | `/api/report/range` | `startDate`,`endDate` | 区间报告 |
| DELETE | `/api/report/{reportId}` | — | 删除（校验 owner） |

`NutritionReportService.saveReport`：按 `(user_id, report_date)` upsert；写入八项 total（`totalCalorie/Protein/Fat/Carb/DietFiber/Calcium/Dha/FolicAcid`，**DHA 读的是 `total.dna`**）、`bmr/intakeBmrRatio`、七项 status；保存后顺带 `metricsService.snapshotFromUser` 补当日身体指标快照。

### 4.2 前端缓存（`composables/useReportCache.ts`）

使用方：仅 `HealthReport.vue`（周报/月报页），`useReportCache<Partial<ReportData>>('health-report')`。三项能力：

1. **防抖 300ms**：连续切换周/月合并为最后一次请求；
2. **sessionStorage + TTL 30min**：key = `report_cache_health-report|k1=v1|k2=v2`（参数按 key 排序）；
3. **按天增量失效**：key 内带 `date: today`，跨天 key 自然变化；数据变更后 `invalidate()` 按前缀批量清。

`HealthReport.vue` 渲染 6 区块：checkinDays/checkinTrend、avgCalories/caloriesTrend、healthyDays/healthyPercent、exerciseDays/exerciseMinutes、targetWeight/weightDiff、bloodPressure/bloodSugar + weightHistory[]、nutritionGoals[]，带滚动渐显动画。

> 同类前端缓存：`stores/diet.ts`（storage TTL 5 分钟）：`diet_meals_${date}`、`diet_nutrition_${date}`、`diet_search_${keyword}`。

---

## 五、食物库

### 5.1 分类体系

数据库字段 `food.food_category`。种子数据分类：`主食`、`肉蛋类`、`水产`、`蔬菜`、`水果`、`豆制品`、`奶类`、`油脂类`、`调味料`。前端两套常量：`FOOD_CATEGORIES`（11 项，管理端下拉）、`FOOD_CATEGORY_ORDER`（录入页分类 tab 排序，8 项）。录入页动态生成 tab：`['全部', ...FOOD_CATEGORY_ORDER 中存在的分类, ...其余动态分类]`。

### 5.2 priority 排序

`FoodRepository` 三个查询统一 `ORDER BY f.priority DESC, f.foodId ASC`（`searchByName` / `findByCategory` / `findAllApproved`）。优先级高的排前，同优先级按入库顺序。

### 5.3 可见性标志（showGi / showFolicAcid / showDha）

规则硬编码在 `DataInitializer.CATEGORY_VISIBILITY`：

| 分类 | showGi | showFolicAcid | showDha |
|---|---|---|---|
| 主食 | 1 | 1 | 0 |
| 肉蛋类 | 0 | 0 | 0 |
| 水产 | 0 | 1 | **1（DHA 仅水产可见）** |
| 蔬菜 | 1 | 1 | 0 |
| 水果 | 1 | 0 | 0 |
| 豆制品 | 1 | 1 | 0 |
| 奶类 | 1 | 0 | 0 |
| 油脂类 | 0 | 0 | 0 |
| 零食 | 1 | 0 | 0 |

> 缺陷：`FoodDTO` 虽声明这三个字段，`FoodService.toDTO()` 未透传；前端也无消费点——标志实际只写入种子数据，录入页对所有食物无条件显示 GI 值。GI 的真实业务用途是"糖尿病用户按 GI 升序排序"。

### 5.4 food 表字段

`food_id`（PK）、`food_name`（非空）、`food_category`（非空）、`calorie/protein/fat/carb/diet_fiber/gi_value/calcium/dha/folic_acid`（numeric(19,2)，均为 **每 100g 含量**：热量 kcal、其余 g / GI / mg / mg / μg）、`show_gi/show_folic_acid/show_dha`（INTEGER）、`priority`（INTEGER）、`status`（DEFAULT 'approved'）。无独立单位字段，录入时输入克数。

### 5.5 生/熟标注

**无独立字段，通过名称文本标注**：`米饭(熟) 130kcal` vs `米饭(生) 350kcal`、`鸡蛋(全,煮)` vs `鸡蛋(全,生)`、`鸡胸肉(生)`、`红薯(蒸熟)` 等，同一食材多条记录。

---

## 六、饮食记录

### 6.1 页面与流程（`views/dashboard/FoodInput.vue`）

1. **加载食物库**：`api.food.list()`（`GET /api/food/list`）全量加载到内存；
2. **分类筛选 + 关键词搜索（纯前端）**：`foodCategory === activeCategory` + `foodName.indexOf(keyword)`；`isDiabetes`（`crowdType` 含"糖尿"）时按 `Number(giValue)||9999` 升序（页面提示"⤴ 糖尿病用户：优先按低 GI 排序"）；
3. **选中食物**：列表项展示 `食物名(分类) · 热量 kcal/100g · GI · 蛋白/脂肪/碳水/钙/叶酸/DHA`；另有语音识别（Web Speech API）入口；
4. **输入份量**：`form.amount`（克），实时预览预计热量 `calorie * amount / 100`；
5. **保存**：`dietStore.addMeal({ eatDate, mealType: currentMeal, remark, items: [{foodId, eatWeight}] })` → `POST /api/diet/add`；
6. **当日列表**：`GET /api/diet/date/{date}`，按 `mealType`（早餐/午餐/晚餐/加餐，存中文）分组，每项按 `营养值 × eatWeight / 100` 折算展示；条目可删除（`DELETE /api/diet/meal/{mealId}`，级联删 items）。

### 6.2 后端保存与查询

- `POST /api/diet/add`，body = `AddMealRequest{eatDate, mealType, remark, items:[{foodId, eatWeight}]}`，可选 query `targetUserId`；`DietController.addMeal` 用 `resolveOperateUserId` 校验（本人或已确认监护人，否则 403）；
- `DietService.addMeal`：先存 `diet_meal`，再逐条存 `diet_item`；
- `getMealsByDate`：**批量加载**（`findByMealIdIn` + `findAllById`）避免 N+1；item 层冗余食物营养素（前端直接算）。

### 6.3 表结构

- `diet_meal`：`meal_id`/`user_id`/`eat_date`（varchar '2026-08-11'）/`meal_type`/`remark`/`created_at`，索引 `idx_diet_meal_user_date`；
- `diet_item`：`item_id`/`meal_id`/`food_id`/`eat_weight`（克），索引 `idx_diet_item_meal`。
- **注意：diet_item 不存营养素快照**，营养素实时 join food 表计算。

---

## 七、营养分析

### 7.1 入口

`views/dashboard/Nutrition.vue`：`loadAnalysis()` 先 `dietStore.fetchTodayMeals(date)` 再 `api.diet.analyze(date)` → `GET /api/diet/analyze/{date}` → `DietService.analyzeDiet(userId, date)`。

### 7.2 计算逻辑（返回 `{user, total, recommendations, status, warnings, meals}`）

1. **营养素汇总**：每 item `factor = eatWeight/100`，累加 8 项：calorie/protein/fat/carb/dietFiber/calcium/dha/folicAcid。**字段坑：DHA 的 key 是 `dna`**（前端与 NutritionReportService 都按 `dna` 读）；
2. **BMR**（`util/NutritionCalculator.java`，Mifflin-St Jeor）：男 `10×体重+6.25×身高−5×年龄+5`，女 `…−161`；缺省兜底 weight 65 / height 165 / age 18；
3. **intakeBmrRatio**：`totalCalorie / bmr` → `user.intakeBmrRatio`；
4. **推荐热量**：`bmr × getActivityMultiplier(crowdType)[0]/[1]`：健身 [1.6,1.8]、青少年 [1.5,1.7]、孕妇 [1.3,1.5]、老年 [1.2,1.4]、糖尿病 [1.2,1.4]、默认 [1.2,1.5]；
5. **三大营养素推荐（每 kg 体重）**：

| 人群 | 蛋白 | 脂肪 | 碳水 |
|---|---|---|---|
| 健身 | 1.6-2.2 | 0.8-1.0 | 3.5-5.0 |
| 青少年 | 1.2-1.5 | 0.9-1.1 | 4.0-5.0 |
| 老年 | 1.2-1.4 | 0.7-0.9 | 2.0-3.0 |
| 孕妇 | 1.3-1.6 | 0.9-1.1 | 3.0-4.0 |
| 糖尿病 | 1.1-1.3 | 0.7-0.9 | 1.5-2.5 |
| 默认 | 1.0-1.2 | 0.8-1.0 | 3.0-4.0 |

6. **微量元素推荐（绝对值）**：孕妇钙 1000-1500、叶酸 400-800、纤维 25-35、DHA 200-600；默认钙 800-1200、叶酸 200-400、纤维 20-35、DHA 100-300；其余人群见 `DietService.getMicroNutrientRecommendations`；
7. **status 判定**：`intake < min×0.8 → "low"`；`> max×1.2 → "high"`；否则 `"normal"`。七项：protein/fat/carb/calcium/folicAcid/dietFiber/dha；
8. **warnings**：对 low/high 生成中文建议文案。

### 7.3 前端展示细节

- 顶部卡片：今日摄入热量、BMR、推荐热量范围、身体概况；
- 三大营养素：`proteinKcal=protein×4、fatKcal=fat×9、carbKcal=carb×4`，ECharts 玫瑰饼图；列表按"每kg摄入 vs 推荐"；
- 微量元素 `microRows` 读 `total.dietFiber/calcium/dna/folicAcid`；
- 前端**不直接用 intakeBmrRatio**，自算 `calorieRatioPct = totalCalorie / ((recMin+recMax)/2) × 100`，80/120 阈值判定。

---

## 八、运动记录与训练计划

### 8.1 后端运动记录（前缀 `/api/exercise`）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| POST | `/api/exercise/record` | `{exerciseType, durationMin, note, targetUserId?}` | 新增记录 |
| POST | `/api/exercise/record-with-date` | 同上 + `recordDate` | 指定日期新增 |
| GET | `/api/exercise/records` | `targetUserId?` | 我的记录（**仅 approved**，倒序） |
| GET | `/api/exercise/records/{date}` | — | 某日记录 |
| GET | `/api/exercise/stats/today` | — | 今日汇总 `{duration, calories, recordCount}` |
| GET | `/api/exercise/stats/week` | — | 近 7 天汇总 |
| DELETE | `/api/exercise/records/{id}` | — | 删除本人记录 |

- **热量公式**：`MET × 3.5 × 体重(kg) × 时长(min) / 200`；`EXERCISE_MET_MAP` 内置 15 项（跑步 8.0、跳绳 10.0、游泳 8.0、骑行 6.0、快走 3.5、太极 2.5 等），未知默认 MET 5.0；体重缺省 65kg；
- **审核机制**：`status` 默认 `'pending'`，用户查询过滤 `approved` —— 新记录在管理员审核通过前不出现在自己的列表/统计里（设计矛盾点，见缺陷清单）；管理员走 `/api/admin/exercise-records/approve/{recordId}` 等审核。

### 8.2 前端训练记录（`views/dashboard/MuscleChart.vue`）—— 纯本地存储，不走后端

- `body-muscles` 库 `BodyChart` 人体肌肉图，19 个训练部位映射中文名；
- 动作库 `src/data/exercises.ts`（name/met/category/difficulty/equipment/steps/tips）；
- `saveRecord()` 按重量/组数/次数推断强度，`useWorkoutRecords().add(...)` 存 localStorage（键 `health_workout_records`，TTL 24h）；
- **两套并行的运动数据**：后端 `exercise_record`（待审核）与前端本地训练记录互不相通。

### 8.3 训练计划（`views/dashboard/TrainingPlan.vue`）

- 数据来自 `useWorkoutRecords()`：`weekStats`（近7天统计 + 按部位聚合）、`streakDays`（连续训练天数，间隔 ≤2 天计入）、`totalRecordExercises`；
- **AI 教练对话**：`buildPrompt(question)` = 用户画像 + 近 7 日训练快照文本 + 教练指令（部位平衡、人群适配：老人避大重量/孕妇避核心压迫/糖尿病防低血糖）→ `api.ai.consult(prompt)`（`POST /api/ai/consult`，非流式）；**后端不可用时降级 `localGenerate()` 本地规则回答**；
- 提供动作步骤弹窗与 5 个快捷提问。

---

## 九、社区 / 家庭关系 / 管理后台

### 9.1 社区（前缀 `/api/community`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/community/posts` | `tag?` 过滤或全部（仅 approved） |
| POST | `/api/community/post/create` | body `{content, tag}` |
| GET | `/api/community/post/{id}` | 详情（404 处理） |
| DELETE | `/api/community/post/{id}` | 仅作者可删（403） |
| GET/POST | `/api/community/post/{id}/comments` | 评论列表（仅 approved）/ 新增评论 |
| DELETE | `/api/community/comment/{id}` | 仅作者可删，评论数 -1 |
| POST | `/api/community/post/{id}/like` | 点赞/取消 toggle，返回 `{liked, likesCount}` |
| GET | `/api/community/tags` | 固定 6 标签：饮食分享/运动打卡/健康知识/心得感悟/求助提问/食谱推荐 |

- 实体：`Post`（`content TEXT / image_url / tag / likes_count / comments_count / status 默认 pending`，级联删 comments/likes）、`Comment`、`PostLike`；
- **审核设计**：用户列表只展示 approved 帖子/评论，但发帖/评论不自动 approved → **"发帖后自己与别人都看不到，直到管理员通过"**；
- `toggleLike` 对历史 `likes_count` 为 NULL 做 `Math.max(0, base-1)` 兼容。
- **前端无社区页面**：路由已重定向到 `/dashboard/profile`，社区功能仅后端保留。

### 9.2 家庭关系 / 监护（前缀 `/api/relation`）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/relation/add` | body `{wardUsername}`，发邀请 |
| POST | `/api/relation/confirm/{relationId}` | 仅被监护人可确认 |
| POST | `/api/relation/reject/{relationId}` | 仅被监护人可拒绝 |
| GET | `/api/relation/my-wards` / `my-guardians` | 我监护的 / 监护我的（仅 confirmed） |
| GET | `/api/relation/pending-invitations` | 待处理的邀请 |
| DELETE | `/api/relation/{relationId}` | 双方任一方可解除 |

- `addRelation` 规则：不能添加自己；**禁止双向监护**（对方已添加你 → 抛"不允许建立双向监护关系"）；重复添加时 rejected → 恢复 pending；`(guardian_id, ward_id)` 唯一约束；
- **`isConfirmedGuardian(guardianId, wardId)` 是"替亲属操作"的唯一授权依据**，被 Profile/BodyMetricsHistory/NutritionReport/Diet 控制器复用；
- 前端 `FamilyRelation.vue`：并行拉 myWards/myGuardians/pendingInvitations，字段兼容驼峰/下划线。

### 9.3 管理后台（`controller/AdminController.java`，类级 `@PreAuthorize("hasRole('ADMIN')")`，前缀 `/api/admin`）

| 分类 | 接口 |
|---|---|
| 用户 | `GET /users`（脱敏）、`/users-with-relations`（附 wards/guardians）、`/users/{userId}`（含 bmr/bmi）、`DELETE /users/{userId}`（管理员不可删） |
| 统计 | `GET /stats/crowd-type`（按人群分组计数，排除 admin） |
| 食物 | `GET /food/list`、`PUT /food/update/{id}`、`DELETE /food/{id}`、`POST /food/approve|reject/{id}` |
| 运动 | `GET /exercise-records`、`/status/{status}`、`POST /approve|reject/{recordId}`、`DELETE /{recordId}` |
| 社区 | `GET /posts`、`/posts/status/{status}`、`POST /posts/approve|reject/{postId}`、评论同理 |
| 食谱 | `GET /recipes`、`POST /recipes`、`PUT /recipes/{id}`、`DELETE /recipes/{id}` |
| 文章 | `GET /articles`、`PUT/DELETE /articles/{id}`、`POST /articles/{id}/analyze`、`/analyze-all`、`GET /analysis`、`/low-quality?threshold=60`、`/analysis-stats`、`POST /analysis/{id}/apply`（AI 质量分析闭环） |

前端 `views/Admin.vue` 四个 Tab：`admin/AdminUserManager.vue`（用户+亲属关系列、详情弹窗、删除）、`AdminFoodManager.vue`（食物审核/编辑）、`AdminArticleManager.vue`（**复用公开文章接口** `/api/articles` 而非 `/api/admin/articles`）、`AdminStats.vue`（echarts 环形饼图人群分布）。

---

## 十、AI 四大功能链路

### 10.1 前端请求封装（`src/api/index.ts`）

| 函数 | 方法/路径 | 说明 |
|---|---|---|
| `consultStream(question, handlers)` | POST `/ai/consult/stream` | 原生 fetch + AbortController + ReadableStream，SSE 接收 |
| `consult(question)` | POST `/ai/consult` | 同步咨询（运动教练用） |
| `dietPlan(goal)` | POST `/ai/diet/plan` | 一日饮食方案 |
| `generateRecipe(prompt)` | POST `/ai/generate-recipe` | AI 生成菜谱（返回纯 JSON 字符串） |
| `recipeRecommend(...)` | POST `/ai/recipe/recommend` | 食材推荐（已实现但无页面调用） |
| `exerciseAdvice(...)` | POST `/ai/exercise/advice` | 运动建议（已实现但无页面调用） |

### 10.2 ① AI 健康问答（SSE 流式）

**调用链**：`AiConsult.vue sendMessage()`（只发当前问题文本，**不发历史、不发 highPerformance**）→ `consultStream`（原生 fetch 读流；替亲属操作时 URL 追加 `?targetUserId=`）→ 后端 `POST /api/ai/consult/stream` 返回 `SseEmitter(120s)` → `AiConsultService.consultStream`（`HttpURLConnection` connect 5s / read 60s POST `{aiBaseUrl}/chat/stream`，body `{message, user_id, health_snapshot, high_performance}`；逐行读 SSE 事件**原样透传** `SseEmitter.event().name(eventType).data(data)`）→ AI 服务 `POST /api/v1/chat/stream`。

**SSE 协议**（AI 服务 `routers/chat.py`）：
- 单条事件：`event: {type}\ndata: {json(ensure_ascii=False)}\n\n`；
- 事件序列：`thinking` → 多个 `delta`（`{content}`）→ `done`（完整结果）；异常 → `error`；
- 正常模式（当前实际路径）：完整链路跑完后 `_chunk_sse_text` 按句号/换行切成 ≤30 字块逐个 delta（`asyncio.sleep(0.01)` 打字机节奏）；`done` 携带 orchestrator.chat 完整 dict；
- `done` payload：`conversation_id / response / provider / mode / route / high_performance / validation / elapsed_seconds / retrieve_info / timing_breakdown`；
- 响应头 `StreamingResponse(media_type="text/event-stream", headers={"Cache-Control":"no-cache","X-Accel-Buffering":"no"})`。

**前端接收**（`consultStream`）：`fetch` + `body.getReader()` + `TextDecoder('utf-8'){stream:true}`；buffer 按 `\n\n` 切事件；分发 `onThinking / onDelta(content) / onDone(payload) / onError(message)`；`AbortController` 支持中断。

**orchestrator.chat 7 个 Stage**（`agent/orchestrator.py`）：① 长期记忆提取 → ② 构建上下文（记忆+今日快照）→ ③ `retrieve_judge` 判定 + `knowledge_router.parallel_retrieve` 多库并行检索 → ④ `_stage_nlu_parse`（命中"吃了/早餐/碗/克"自动解析饮食）→ ⑤ 会话创建 + 取最近 4 条历史拼进 question → ⑥ `mode_router.route(func_type="qa")` → ⑦ 免责声明去重 + 安全拦截（`_guard_qa_response`），`store.add_message` 写回。

### 10.3 ② 一日饮食方案

- **入口在 AiConsult.vue 快捷按钮**（无独立页面；`Nutrition.vue` 是本地营养分析，不属于 AI 链路）；
- `runAgent('dietPlan')` → `api.ai.dietPlan('均衡饮食')` → 后端 `POST /api/ai/diet/plan`（`AiNutritionService.generateDietPlan`：熔断检查 → 快照 → body `{user_profile, goal, today_diet, today_diet_total, recent_exercise, today_body_metrics}`）→ AI 服务 `POST /api/v1/diet/plan` → `orchestrator.process("diet", ...)` → `mode_router.route(func_type="diet_plan")`；
- **`_meta` 注入**：`orchestrator.process` 在结果 dict 注入 `result["_meta"] = {high_performance, mode, route, timing_ms, validation}`，随 result 一并返回前后端；
- 缺陷：前端 `formatAgentResult('dietPlan')` 期望 `data.summary / data.plan`，但 mode_router 结果字段是 `goal / total_calories / daily_plan / nutrition_breakdown / tips` → 字段不匹配，当前实际只渲染标题空壳。

### 10.4 ③ 食材菜谱推荐（AI 生成）

- `RecipeGenerateDialog.vue`：`validatePrompt` 三道校验（长度≥4、纯数字/符号拒绝、`foodKeywords` 正则命中）→ `api.ai.generateRecipe(prompt)` → 返回**纯 JSON 字符串**，前端先剥 ` ```json ` 代码块再 `JSON.parse` → 展示食材/步骤/5 格营养简表；`saveGeneratedRecipe()` → `POST /api/recipe/save`（写 `saved_recipe` 表）；
- 后端 `POST /api/ai/generate-recipe`：`AiConsultService.generateRecipe` → `buildSystemPrompt(snapshot, ragResult)`（含"严格按 JSON 输出"指令）→ `AiChatClientService.callRecipeApi`（body `{message: systemPrompt + "\n\n" + userQuestion, user_id: 0}`）→ `POST {aiBaseUrl}/chat`（user_id=0 走通用链路）；
- 独立链路 `/ai/recipe/recommend` → AI 服务 `/api/v1/food/recommend`（`orchestrator.process("food_recommend")`），**已实现但当前无页面调用**。

### 10.5 ④ 个性化运动方案

- `TrainingPlan.vue` 教练对话走 `api.ai.consult(prompt)` **同步接口**（非 SSE）；API 异常降级本地规则；
- 后端 `POST /api/ai/consult`：`AiConsultService.consult` → `buildSystemPrompt(snapshot, ragResult)`（**sync 链路才组装 RAG + 7 模块**）→ `AiChatClientService.callAiService`（body `{message, user_id, health_snapshot, system_prompt}`）→ `POST {aiBaseUrl}/chat`；
- **注意**：AI 服务 `/chat` 端点**不读取 `system_prompt` 字段**，实际由 AI 服务用 `CLOUD_PROMPTS["qa"]` 自行组装 → Java 侧 7 模块 systemPrompt 在该链路实际未进入大模型；
- 独立链路 `/ai/exercise/advice` → `/api/v1/exercise/advice`（`orchestrator.process("exercise")`），已实现但无页面调用。

### 10.6 高性能模式与正常模式（`services/mode_router.py`）

**开关现状**：`highPerformance` 字段贯穿链路（前端 body → 后端转 `high_performance` → AI 服务），但**当前前端 4 个页面均不发送该字段**，线上恒为正常模式。

**高性能模式**（`route="C_direct"`）：
- 直接 `_run_cloud(timeout=LLM_TIMEOUT_HIGH_PERF=180)`，`validation={skipped: true}`；
- 云端异常 → 本地规则兜底（`route="C_direct_fallback_local"`）；
- 成功后**后台线程**执行 C 结果入库 + 本地大模型相关性校验，不阻塞主流程。

**正常模式三段式**：
1. **Stage 1 模板召回** `_retrieve_template`：`retriever.search(query, top_k=5, target_crowd=kb_crowd, collections=["kb_templates"])`；相似度 < 0.72 视为未命中；**≥ 0.95 时 `skip_llm=True` 直接返回模板原文（0 token）**；
2. **Stage 2 A 方案本地改写** `_run_local_rewrite`：模板命中 → `REWRITE_PROMPTS[func_type]` 本地 Ollama 改写；未命中 → 本地 LLM 直接生成；本地失败 → 本地规则兜底（`_local_fallback`）；qa 走 `llm.chat`，结构化功能走 `llm.chat_json`；
3. **Stage 3 A 校验** `VALIDATORS[func_type]`（如 diet_plan 必填 daily_plan/total_calories/nutrition_breakdown、≥3 餐、热量 800-4000；exercise 周总时长 ≤300 分钟 WHO 上限）；A 通过 → `route="A_template_local"`；**A 不通过才触发 C 云端** → C 再校验，C 不过沿用 A；C 调用异常沿用 A（`route="A_C_fallback_failed"`，passed=true 保证前端正常解析）。

### 10.7 上下文组装（`service/AiChatContextBuilder.java`）

**`buildHealthSnapshot(user, today)`**（供 SSE 与各业务转发）→ 8 块：`date / profile / today_body_metrics / body_metrics_trend（近7日极值均值）/ today_diet（三餐明细）/ today_diet_total / recent_exercise / diet_reference`。

**`buildSystemPrompt(snapshot, ragKnowledge)`**（仅 sync consult 使用）→ 依次拼接：
1. 固定系统指令（健康助手角色定位）；2. 三类意图定义（营养咨询/数据分析/慢病管理）；3. 一、用户基本资料；4. 二、今日身体指标；5. 三、近 7 日趋势；6. 四、今日饮食记录；7. 五、近 7 日运动数据；8. 六、后端预计算目标值（BMR/TDEE）；9. 【RAG 权威参考知识库】可选段；10. 通用营养参考标准。

**AI 服务侧补充**（`_compute_user_derived_context`）：自动推导 BMI 分类（18.5/20/24/28）、Mifflin-St Jeor BMR×活动系数（1.2/1.375/1.55/1.725 按近 7 日运动分级）得 TDEE、饮食快照行、运动汇总行 → 注入 REWRITE_PROMPTS/CLOUD_PROMPTS 的 `{user_derived_context}`。

### 10.8 熔断与超时

**`util/CircuitBreaker.java`**：三态 CLOSED/OPEN/HALF_OPEN；阈值 3 次失败 → OPEN；冷却 60s 后 HALF_OPEN 放行试探，成功回 CLOSED、失败重新 OPEN；`buildBreakerMessage()` 生成用户文案。接入点：`AiChatClientService.callAiService/callRecipeApi`、`AiNutritionService` 各方法、`AiExerciseService`、`AiContentService`、`AiConsultService`。**SSE 路径不经过熔断**。

**HTTP 超时**：`RestClientConfig` 两个 RestTemplate —— `aiRestTemplateShort`（connect 5s / socket 30s）、`aiRestTemplateLong`（connect 5s / socket 300s）；SSE 路径 HttpURLConnection connect 5s / read 60s + SseEmitter 120s；AI 服务侧 `LLM_TIMEOUT=120`（正常）、`LLM_TIMEOUT_HIGH_PERF=180`，本地重试 1 次、云端重试 2 次，`chat_json` 内部做 JSON 提取修复。

---

## 十一、菜谱推荐

### 11.1 页面结构

`views/dashboard/RecipeLibrary.vue`（主组件）+ `RecipeCard.vue` / `RecipeGenerateDialog.vue` / `RecipeDetailDialog.vue` + `composables/useRecipeSubstitution.ts`。路由 `/dashboard/recipe-library`。

- 状态：`currentTab: 'all' | 'saved'`（全部/我的收藏）、`searchKeyword`、`selectedTags: string[]`；
- 数据转换：`normalizeRecipe`（`recipeId→id`、`recipeName→name`、tags 字符串→数组）、`normalizeSavedRecipe`（JSON 解析 nutritionSummary/ingredients/steps）；
- 过滤（前端）：标签 `r.tags.some(t => selectedTags.includes(t))` 或关键词（name/description）；
- 标签常量 `RECIPE_TAGS`（10 个营养标签：孕妇/糖尿病/老年人/青少年/高血压/减脂/健身/低GI/高蛋白/均衡）。

### 11.2 数据库检索

`RecipeRepository`：`findByRecipeNameContaining`（名称模糊）、`findByTagsContaining`（**tags 逗号分隔 LIKE %tag%，不能多标签交集**）、`findByRecipeNameContainingOrTagsContaining`。食材存于 `recipe_ingredients` 关联表，**无后端"按食材检索"查询**；真正的食材检索只在 AI 推荐链路（向 AI 服务传 `ingredients` 数组）。

### 11.3 食材替换（useRecipeSubstitution）

`RecipeDetailDialog` 打开 → `api.recipe.getDetail(id)` → `GET /api/recipe/detail/{id}`（`RecipeService.getRecipeWithSubstitution`），返回：`recipe / ingredients / substitutions（规则基：过敏/口味/饮食限制，含 reason/alternatives/isNotSuitable）/ foodDbSubstitutions（高脂高GI高热量食材 → 同类别更优食材，含 concerns/alternatives）/ ingredientNutrition（每食材营养估算，`findBestFoodMatch` 按用量折算）/ aggregatedNutrition（DHA、叶酸总和 + GI 加权平均）/ nutritionChange`。

**营养双展示（关键设计）**：
- `originalNutritionSum`：对 `ingredientNutrition` 逐项累加 calories/protein/fat/carbs（**以食材估算值为基数，不依赖 `recipe.calories`**）；
- `modifiedNutrition`：基数 = 原始总和，对每个替换"减原 + 加替代"；替代品营养按 `per100g × 用量`（`ratio = amount/100`，仅 g 单位）；
- `originalPer100g / modifiedPer100g`：`总和 / estimatedTotalWeight × 100`（estimatedTotalWeight 只累加 g/ml 单位，保底 1 防除零）；
- 模板展示：无替换显示"每100g 营养值"单值；有替换显示"替换后营养（总值/每100g）"双值 + "替换前后营养对比"表格（4 行含变化差值）；
- **容错**：替代品无营养数据时沿用原食材营养（避免错误归零）。

### 11.4 AI 生成菜谱完整链路

`RecipeGenerateDialog.generateRecipe()` → `api.ai.generateRecipe(prompt)` → 后端 `POST /api/ai/generate-recipe`（取用户档案/身体数据构建上下文 → `callRecipeApi` → `POST {aiBaseUrl}/chat`）→ AI 服务 `orchestrator.process("chat", ...)` 输出含 `ingredients/name/steps/calories/protein/fat/carbs` 的 JSON → 前端剥壳 parse 展示 → `saveGeneratedRecipe()` → `POST /api/recipe/save`（写 `saved_recipe` 表：id/created_at/ingredients/nutrition_summary/persona_tag/source/steps/title/user_id）→ `RecipeLibrary` 重新 `loadRecipes()` 合并展示。

### 11.5 菜谱接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/recipes`、`/api/recipes/search`、`/api/recipes/tags`、`/api/recipes/{id}` | 系统食谱（permitAll） |
| GET | `/api/recipe/list`、`/search`、`/tag/{tag}`、`/tags`、`/{id}` | 用户侧食谱 |
| GET | `/api/recipe/detail/{id}` | **详情含替换建议**（getRecipeWithSubstitution） |
| GET | `/api/recipe/my-saved` | 我的收藏 |
| POST | `/api/recipe/save` | 保存 AI 生成菜谱 |
| DELETE | `/api/recipe/my-saved/{id}` | 取消收藏 |
| POST | `/api/ai/generate-recipe` | AI 生成菜谱 |
| POST | `/api/ai/recipe/recommend` | 食材推荐（无页面调用） |

---

## 十二、科普文章（知识科普）

### 12.1 三层导航

- 路由：`/dashboard/articles`（Articles.vue）→ 主题列表卡片；`/dashboard/article-detail/:id`（ArticleDetail.vue）。
- **第一层 Articles.vue**：`articleStore.fetchArticles()` 后按 `article.topic` 分组为 `TopicMeta`（label/category/summary/audience 集合，图标按 category 映射）；适配人群筛选（全部 / 我的人群 `crowdType → CROWD_TO_AUDIENCE`）；空库时管理员可见"一键初始化 Demo 数据"。
- **第二层**（点击主题后同页切换）：三个篇幅卡片 —— `short` 速读卡（约300字）/ `medium` 深度文（约1500字，推荐首选）/ `long` 综述文（约2500字）。
- **第三层 ArticleDetail.vue**：`displayContent` 按 `currentLen` 选 `contentShort/contentMedium/contentLong`；`displayTitle` 剥掉 `【速读卡】` 等后缀；篇幅切换 `switchLen(len)` **优先在同主题找到对应 lengthType 的独立文章跳转**（三篇幅 = 3 条独立记录，共享 `topicGroupId`）；PC 端右侧 `ArticleSidebar.vue`（竖排篇幅切换 + 跟随式目录，IntersectionObserver `rootMargin:'-80px 0px -70% 0px'` 滚动高亮）；交互含点赞、分享、参考文献折叠区（`parseSources` + 角标点击 scrollIntoView）、同主题其它篇幅卡片、免责声明。

### 12.2 B 流水线（三版生成链路）

入口：`POST /api/articles/generate`（body `{topic, persona}`）→ `ArticleService.generateAndSave` → `AiChatClientService.generateArticleMotherDraftB`（`POST {aiBaseUrl}/articles/mother-draft`，120s 长超时 + 熔断）→ AI 服务 `pipeline_v32.run_pipeline`：

1. **Stage 0 知识库检索**：ChromaDB `hybrid_retrieve(query, top_n=14)` + 按 `sub_group` 元数据精确补充细分人群卡片；
2. **Stage 0.5 联网搜索**：PubMed → Europe-PMC → Semantic Scholar → Crossref 多源回退，汇总 `verified_pmids` 白名单；
3. **Stage 1 本地 Ollama 生成 15 标签母稿骨架**（素材限定 14 张卡片内适配 4096 上下文）；`gate4` 截断检测自动重生成（max_retries=2）；
4. **Stage 2 云端 DeepSeek 外扩**：只允许引用 PMID 白名单/DOI 标注文献，15 个标签必须完整保留，正文引用统一 `[序号]` 上角标；
5. **Stage 2.5** `verify_pmids_in_article`（白名单 + Europe-PMC 复核剔除虚假 PMID）→ `clean_placeholders` → `standardize_references` → `ensure_official_guides`；
6. **Stage 3 本地校验**：15 标签完整性 + 顺序 + 结构检查（模板残留检测、DEEP_PLUS 内禁用一级编号、全篇一级编号重复检测）；
7. **闸门5 参考文献审计**：正文角标 ↔ 文末参考文献一一对应、按 指南>立场声明>RCT>综述 排序。

**母稿 15 标签协议**（前后端/AI 服务三方共享契约）：`【#META#】【#ALL_INTRO#】【#SUMMARY_FAST/DEEP/ALL#】【#COMMON_BEGIN/END#】【#DEEP_PLUS_BEGIN/END#】【#DEBATE_ZONE_BEGIN/END#】【#CONCLUDE_FAST/DEEP/ALL#】【#REF_LIST#】`。

**后端拆分**（`util/ArticleSplitUtil.splitMotherDraft`）：速读卡 = ALL_INTRO + COMMON + CONCLUDE_FAST（cleanShortContent 删大表格与机制深度段）；深度文 = ALL_INTRO + COMMON + DEEP_PLUS + CONCLUDE_DEEP；综述文 = ALL_INTRO + COMMON + DEEP_PLUS + DEBATE_ZONE + CONCLUDE_ALL。

**五道校验**（`ArticleSplitUtil.validate`，score=100 扣分制）：① 篇幅区间（速读 50-900 / 深度 300-2500 / 综述 500-4000，扣 15）；② 参考文献 ≥3 条（扣 15）；③ 营养数值异常（mg>4000 / g≥1000 / kcal>99999 / IU>200000 / 微克>5000，扣 10）；④ 敏感话术（治愈/根治/包治/特效药/偏方/秘方/绝对安全，扣 25）；⑤ 查重预留。`passed = errors.isEmpty() && score >= 60`。

**落库**：`topicGroupId = "tg-" + System.currentTimeMillis()`；3 条文章（`lengthType: short/medium/long`、标题带 `【速读卡】` 等后缀、`contentShort/Medium/Long`、`sourcesJson`、`qualityScore`、`category/audience` 取自 META）。

配套接口：`/api/articles/import-mother`、`/generate-hybrid`、`/generate-smart`（统一委托 B 方案）、`/generate-async` + `GET /task/{taskId}`（异步轮询）、`/regenerate-with-correction/{id}`（自纠错）、`/reset-demo`（重建 15 篇 Demo）。

### 12.3 目录编号系统

- 模板规范：COMMON 板块一级标题"一、二、三"、二级"（一）（二）（三）"；DEEP_PLUS 板块标题单独一行"深度拓展"（不编号），其下仅二级标题，**禁止一级编号避免与共识板块冲突**；
- 前端 `utils/articleRendering.ts` `renderMarkdown`：模板残留清理（`TEMPLATE_LABEL_PATTERNS`、`SECTION_HEADING_RULES`）→ `^（[一二三...]+）(.+)$` → `<h3 id>`、`^([一二三...]+)、(.+)$` → `<h2 id>`；`###` 中"一、"开头提升 h2，`####` 降 h3；
- `extractToc()`：按 `contentCards` 顺序，每个 h2 为一级、card 内 h3 为二级 → `ArticleSidebar` 渲染（二级缩进）；
- 卡片化：`contentCards` 用 `<h2 id>` 正则切块，首个 h2 前内容为"导读"卡，前 2 张默认展开。

### 12.4 术语渲染（GLOSSARY）

- 词典约 30 条：**中文术语带 alias**（`'单不饱和脂肪酸': {def, alias: 'MUFA'}`、`'血糖生成指数': alias 'GI'`、`'体质量指数': alias 'BMI'`）；**纯英文缩写直接作 key**（`'EPA'`、`'DHA'`、`'Omega-3'`）；
- `GLOSSARY_KEYS` 长度降序（避免短词先匹配）；
- `linkTermsAndRefs`：`[n]` → `<sup class="cite-badge">` 可点击角标；DOM 遍历只处理 TEXT_NODE（跳过 script/style/code/pre/sup/a）；**每篇仅标记术语首次出现**；带 alias 的术语匹配"术语（alias）"或纯"术语"两种形式；**否定前缀保护**（`不/非` 紧邻不打标，如"不饱和脂肪酸"不命中"饱和脂肪酸"）；
- 交互：`.term-mark` hover/click → Tooltip 标题 `术语（alias）`、正文 `def`。

### 12.5 文章接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/articles` | `category?`、`audience?` 过滤 |
| GET | `/api/articles/{id}` | 详情（阅读量 +1） |
| GET | `/api/articles/topic/{topic}`、`/category/{category}`、`/search`、`/top/views`、`/top/likes` | 检索/排行 |
| GET | `/api/articles/categories`、`/topics` | **主题列表来源**（`findAllTopics()`，无独立主题表） |
| GET | `/api/articles/related/{topicGroupId}`、`/topic-group/{topicGroupId}` | 同主题其它篇幅 |
| POST | `/api/articles/generate` | B 流水线生成三版（body `{topic, persona}`） |
| POST | `/api/articles/generate-async` + `GET /task/{taskId}` | 异步生成轮询 |
| POST | `/api/articles/{id}/like` | 点赞 |
| POST | `/api/articles/reset-demo` | ADMIN 重建 Demo |
| POST | `{ai}/api/v1/articles/mother-draft` | 母稿生成（pipeline_v32） |

---

## 十三、已知问题与缺陷清单

1. **找回密码不可用**：`ForgotPassword.vue` 只传 `username + newPassword`，后端 `resetPassword` 强制校验 `oldPassword` → 必报"旧密码不正确"。
2. **新记录不可见**：运动记录/帖子/评论创建时 `status=pending`，用户端列表过滤 approved → 用户"保存后看不到自己的内容"，需管理员审核后才可见（有审核设计意图，但 UX 矛盾）。
3. **字段命名坑 `dna`**：`analyzeDiet` 输出 `total` 中 DHA 的 key 是 `dna` 而非 `dha`（前端 Nutrition.vue 与 NutritionReportService 均按 `dna` 读）。
4. **`NutritionReportVO.bmrStatus` 未赋值**：`fromEntity` 未给 bmrStatus/recommendations/createdAt 赋值（遗留缺口）。
5. **`FoodDTO.showGi/showFolicAcid/showDha` 未透传**：`FoodService.toDTO()` 未复制这三字段，前端无消费点 → 可见性标志形同虚设（仅种子数据写入）。
6. **前端不传 `high_performance`**：4 个 AI 页面均不发送该字段 → 高性能模式代码路径当前实际不生效。
7. **前端不传 `conversation_id`**：每次提问都是新会话（历史仅存于 AI 服务端会话内）。
8. **dietPlan 字段不匹配**：前端 `formatAgentResult` 期望 `data.summary/data.plan`，AI 结果实际为 `goal/total_calories/daily_plan/nutrition_breakdown/tips` → 页面只渲染标题空壳。
9. **Java 侧 `system_prompt` 被忽略**：AI 服务 `/chat` 端点不读取该字段 → sync consult 的"7 模块 systemPrompt"实际未进入大模型（仅 callRecipeApi 拼进 message 才生效）。
10. **SSE 截断风险**：后端 HttpURLConnection readTimeout 60s < 正常模式本地 Ollama 生成耗时，长回答可能被截断；SSE 链路不经熔断。
11. **鉴权依赖 NPE→401 兜底**：未登录访问受保护接口靠 `GlobalExceptionHandler` 把 NPE 转 401，而非显式拒绝（较脆弱）。
12. **generateRecipe 格式漂移**：返回体是字符串化 JSON（可能包 ```` ```json ```` 代码块），前端需剥壳 + parse 兜底，AI 输出格式漂移风险高。
13. **健康社区无前端页面**：路由已重定向 `/dashboard/profile`，社区功能仅后端保留。
14. **两套并行运动数据**：后端 `exercise_record`（待审核）与前端 localStorage `health_workout_records` 互不相通，统计口径不一致（后端 `MET×3.5×体重×时长/200` vs 前端 `MET×体重×时长/60`）。

---

## 十四、通用设计要点

1. **代录模式（targetUserId）贯穿全站**：前端 `localStorage['actAsUserId']` + axios 拦截器自动附加 `targetUserId`（GET 进 params，其余拼 query）；后端各控制器 `resolveOperateUserId` 统一以 `FamilyRelationService.isConfirmedGuardian`（confirmed 状态）为授权依据；store getter `activeUserId/activeUserLabel` 支撑"替 XX 操作"显示。
2. **数据单位约定**：食物库营养素一律"每 100g"；用户输入 `eatWeight`（克）；折算统一 `factor = eatWeight/100`（DietService / AiChatContextBuilder / 前端三处算法一致）。
3. **三层审核机制**：帖子/评论/运动记录/食物均 status 流转 pending→approved/rejected，admin 审核后公开。
4. **BMR/BMI 公式集中**：`util/NutritionCalculator.java`（Mifflin-St Jeor；BMI 阈值 18.5/24/28），后端多处复用，前端各有同公式副本。
5. **前端缓存分层**：`useReportCache`（sessionStorage + 30min TTL + 300ms 防抖 + key 含日期跨天失效）→ 报表；`stores/diet.ts`（localStorage + 5min）→ 当日饮食；`utils/storage.ts`（localStorage + TTL）→ 训练记录。
6. **AI 模板召回阈值**：0.72 起召回、0.95 以上跳过 LLM 直接返回模板原文（0 token）；C 方案结果后台异步入库形成知识库自增长闭环（KBDedupService 四层去重）。
7. **性能优化**：`getMealsByDate` 批量查询避免 N+1；`FoodService` 用 `@Cacheable` + 写操作 `@Caching(evict allEntries)`；高性能模式把入库与校验丢后台线程池。
8. **SQLite 注意**：`application.yml` 用自定义 `HealthSQLiteDialect`（org.sqlite.hibernate.dialect.SQLiteDialect 子类）；JPA `ddl-auto: update`；`DataInitializer` 启动时幂等执行 `index_optimizations.sql` + `unique_constraints.sql` 并初始化种子数据（admin/admin123、user001~006、食物库、食谱）。
