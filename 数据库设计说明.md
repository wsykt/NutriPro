# 个人健康助手 — 数据库设计说明

## 一、数据库概览

| 项目 | 详情 |
|------|------|
| 数据库类型 | SQLite 3（文件型数据库） |
| 数据库文件 | `backend-health/data/health.db` |
| JDBC URL | `jdbc:sqlite:./data/health.db` |
| ORM 框架 | Spring Data JPA（Hibernate 5） |
| 表生成策略 | `ddl-auto: update`（JPA 实体自动建表/更新） |
| 方言适配 | `org.sqlite.hibernate.dialect.SQLiteDialect` |
| 主键策略 | 全部使用 `GenerationType.IDENTITY`（自增） |
| SQL Schema 文件 | 无（完全由 JPA 实体驱动） |

## 二、数据表全景（17 张）

```
┌─────────────────────────────────────────────────────────────────────┐
│                          user（用户表）                               │
│  ┌─────────────┐     ┌───────────────────┐                          │
│  │food（食物库）│     │body_metrics_history│                          │
│  └──────┬──────┘     │（身体指标历史）      │                          │
│         │            └───────────────────┘                          │
│  ┌──────▼──────┐     ┌───────────────────┐                          │
│  │diet_item    │◄────│diet_meal（饮食餐食）│                          │
│  │（饮食条目）   │     └───────────────────┘                          │
│  └─────────────┘     ┌───────────────────┐                          │
│                      │exercise_record    │                          │
│  ┌─────────────┐     │（运动记录）         │                          │
│  │nutrition_   │     └───────────────────┘                          │
│  │report       │                                                    │
│  │（营养报告）   │     ┌───────────────────┐                          │
│  └─────────────┘     │family_relation    │                          │
│                      │（亲属关系）         │                          │
│  ┌─────────────┐     └───────────────────┘                          │
│  │recipes      │                                                    │
│  │（菜谱）      │     ┌───────────────────┐                          │
│  │  │          │     │ai_conversation_   │                          │
│  │  ▼          │     │record（AI对话记录） │                          │
│  │recipe_      │     └───────────────────┘                          │
│  │ingredients  │                                                    │
│  │（菜谱食材）   │     ┌───────────────────┐                          │
│  └─────────────┘     │saved_recipe       │                          │
│                      │（用户收藏菜谱）      │                          │
│  ┌─────────────┐     └───────────────────┘                          │
│  │article      │                                                    │
│  │（科普文章）   │     ┌───────────────────────────────────────┐     │
│  │  │          │     │          社区模块（3 表）                  │     │
│  │  ▼          │     │  post ──┬── comment（帖子评论）          │     │
│  │article_     │     │         └── post_like（帖子点赞）         │     │
│  │analysis     │     └───────────────────────────────────────┘     │
│  │（文章分析）   │                                                    │
│  └─────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────┘
```

### 数据表清单

| # | 表名 | 实体类 | 功能分类 | 记录数级别 |
|---|------|--------|----------|:---------:|
| 1 | `user` | User | 用户核心 | 百-千 |
| 2 | `food` | Food | 食物库（静态数据） | 1500+ |
| 3 | `diet_meal` | DietMeal | 饮食记录 | 千-万 |
| 4 | `diet_item` | DietItem | 饮食条目 | 万-十万 |
| 5 | `exercise_record` | ExerciseRecord | 运动记录 | 千-万 |
| 6 | `nutrition_report` | NutritionReport | 营养报告 | 百-千 |
| 7 | `body_metrics_history` | BodyMetricsHistory | 身体指标历史 | 百-千 |
| 8 | `family_relation` | FamilyRelation | 亲属关系 | 十-百 |
| 9 | `recipes` | Recipe | 菜谱库 | 百 |
| 10 | `recipe_ingredients` | RecipeIngredient | 菜谱食材 | 千 |
| 11 | `saved_recipe` | SavedRecipe | 用户收藏菜谱 | 百-千 |
| 12 | `article` | Article | 科普文章 | 百-千 |
| 13 | `article_analysis` | ArticleAnalysis | 文章质量分析 | 百-千 |
| 14 | `post` | Post | 社区帖子 | 百-千 |
| 15 | `comment` | Comment | 帖子评论 | 千-万 |
| 16 | `post_like` | PostLike | 帖子点赞 | 千-万 |
| 17 | `ai_conversation_record` | AiConversationRecord | AI 对话记录 | 千-万 |

## 三、表结构详细定义

### 3.1 user — 用户表

用户身份认证与健康档案核心表。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| userId | `user_id` | INTEGER | PK, AUTOINCREMENT | — | 用户唯一标识 |
| username | `username` | TEXT | NOT NULL, UNIQUE | — | 登录用户名 |
| password | `password` | TEXT | NOT NULL | — | BCrypt 加密密码 |
| gender | `gender` | TEXT | CHECK(IN('男','女')) | `'男'` | 性别 |
| height | `height` | REAL | CHECK(0~300) | `165.0` | 身高(cm) |
| weight | `weight` | REAL | CHECK(0~300) | `65.0` | 体重(kg) |
| age | `age` | INTEGER | CHECK(0~150) | `18` | 年龄 |
| crowdType | `crowd_type` | TEXT | CHECK(IN('普通人','健身','老年','孕妇','青少年','糖尿病')) | `'普通人'` | 人群分类 |
| role | `role` | TEXT | CHECK(IN('user','admin')) | `'user'` | 用户角色 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 注册时间 |
| allergicFoods | `allergic_foods` | TEXT | NULLABLE | NULL | 过敏食物（逗号分隔） |
| dietaryRestrictions | `dietary_restrictions` | TEXT | NULLABLE | NULL | 饮食限制（逗号分隔） |
| tastePreference | `taste_preference` | TEXT | CHECK(IN('清淡','适中','重口味','微辣','辣')) | `'清淡'` | 口味偏好 |
| elderlyMode | `elderly_mode` | INTEGER | CHECK(0/1) | `0` | 老年模式开关 |

**索引**: `username` 字段级 UNIQUE 约束。

### 3.2 food — 食物营养数据库

存储食物营养成分，为用户饮食计算提供基础数据。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| foodId | `food_id` | INTEGER | PK | — | 食物唯一标识 |
| foodName | `food_name` | TEXT | NOT NULL | — | 食物名称（含生/熟标记） |
| foodCategory | `food_category` | TEXT | NOT NULL | — | 分类（主食/肉蛋类/水产/蔬菜/水果/豆制品/奶类/油脂类） |
| calorie | `calorie` | DECIMAL | NULLABLE | NULL | 热量(kcal/100g) |
| protein | `protein` | DECIMAL | NULLABLE | NULL | 蛋白质(g/100g) |
| fat | `fat` | DECIMAL | NULLABLE | NULL | 脂肪(g/100g) |
| carb | `carb` | DECIMAL | NULLABLE | NULL | 碳水化合物(g/100g) |
| dietFiber | `diet_fiber` | DECIMAL | NULLABLE | NULL | 膳食纤维(g/100g) |
| giValue | `gi_value` | DECIMAL | NULLABLE | NULL | 血糖生成指数(GI值) |
| calcium | `calcium` | DECIMAL | NULLABLE | NULL | 钙(mg/100g) |
| dha | `dha` | DECIMAL | NULLABLE | NULL | DHA(mg/100g) |
| folicAcid | `folic_acid` | DECIMAL | NULLABLE | NULL | 叶酸(μg/100g) |
| showGi | `show_gi` | INTEGER | — | `0` | GI值显示标记(1=显示) |
| showFolicAcid | `show_folic_acid` | INTEGER | — | `0` | 叶酸显示标记(1=显示) |
| showDha | `show_dha` | INTEGER | — | `0` | DHA显示标记(1=显示) |
| status | `status` | TEXT | — | `'approved'` | 审核状态(approved/pending/rejected) |
| priority | `priority` | INTEGER | — | `0` | 搜索优先级(11=顶级常用,10=常用,5-9=一般,0=冷门) |

**设计规则**:
- GI 值可见分类: 主食、蔬菜、水果、豆制品、奶类、零食
- 叶酸可见分类: 主食、水产、蔬菜、豆制品
- DHA 可见分类: 仅水产
- 营养成分均为每 100g 可食部含量

### 3.3 diet_meal — 饮食餐食记录

记录用户每餐的元数据，与 `diet_item` 一对多关联。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| mealId | `meal_id` | INTEGER | PK | — | 餐食唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID（隐式外键→user） |
| eatDate | `eat_date` | TEXT | NOT NULL | — | 用餐日期(格式:yyyy-MM-dd) |
| mealType | `meal_type` | TEXT | NOT NULL | — | 餐次(breakfast/lunch/dinner/snack) |
| remark | `remark` | TEXT | NULLABLE | NULL | 备注 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 记录时间 |

> ⚠️ **问题**: `eatDate` 使用 String 而非 DATE 类型，无法利用数据库日期函数。

### 3.4 diet_item — 饮食条目

每餐中具体食物及其摄入量。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| itemId | `item_id` | INTEGER | PK | — | 条目唯一标识 |
| mealId | `meal_id` | INTEGER | NOT NULL | — | 所属餐食ID（隐式外键→diet_meal） |
| foodId | `food_id` | INTEGER | NOT NULL | — | 食物ID（隐式外键→food） |
| eatWeight | `eat_weight` | DECIMAL | NOT NULL | — | 食用重量(g) |

### 3.5 exercise_record — 运动记录

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 记录唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID（隐式外键→user） |
| exerciseType | `exercise_type` | TEXT | NOT NULL | — | 运动类型（跑步/游泳/健身等） |
| durationMin | `duration_min` | INTEGER | NULLABLE | NULL | 运动时长(分钟) |
| caloriesBurned | `calories_burned` | REAL | NULLABLE | NULL | 消耗热量(kcal) |
| recordDate | `record_date` | DATE | NULLABLE | NULL | 运动日期 |
| note | `note` | TEXT | NULLABLE | NULL | 备注 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 记录时间 |
| status | `status` | TEXT | — | `'pending'` | 审核状态 |

### 3.6 nutrition_report — 营养评估报告

每日营养综合分析报告，包含用户当日快照以实现历史回溯。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| reportId | `report_id` | INTEGER | PK | — | 报告唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID |
| reportDate | `report_date` | DATE | NOT NULL | — | 报告日期 |
| totalCalorie | `total_calorie` | REAL | NULLABLE | NULL | 总热量摄入 |
| totalProtein | `total_protein` | REAL | NULLABLE | NULL | 总蛋白质 |
| totalFat | `total_fat` | REAL | NULLABLE | NULL | 总脂肪 |
| totalCarb | `total_carb` | REAL | NULLABLE | NULL | 总碳水 |
| totalDietFiber | `total_diet_fiber` | REAL | NULLABLE | NULL | 总膳食纤维 |
| totalCalcium | `total_calcium` | REAL | NULLABLE | NULL | 总钙 |
| totalDha | `total_dha` | REAL | NULLABLE | NULL | 总 DHA |
| totalFolicAcid | `total_folic_acid` | REAL | NULLABLE | NULL | 总叶酸 |
| bmr | `bmr` | REAL | NULLABLE | NULL | 基础代谢率 |
| intakeBmrRatio | `intake_bmr_ratio` | REAL | NULLABLE | NULL | 摄入/BMR 比值 |
| proteinStatus | `protein_status` | TEXT | NULLABLE | NULL | 蛋白质评估 |
| fatStatus | `fat_status` | TEXT | NULLABLE | NULL | 脂肪评估 |
| carbStatus | `carb_status` | TEXT | NULLABLE | NULL | 碳水评估 |
| calciumStatus | `calcium_status` | TEXT | NULLABLE | NULL | 钙评估 |
| folicAcidStatus | `folic_acid_status` | TEXT | NULLABLE | NULL | 叶酸评估 |
| dietFiberStatus | `diet_fiber_status` | TEXT | NULLABLE | NULL | 纤维评估 |
| dhaStatus | `dha_status` | TEXT | NULLABLE | NULL | DHA 评估 |
| crowdType | `crowd_type` | TEXT | NULLABLE | NULL | 人群分类 |
| *userHeight* | `user_height` | REAL | NULLABLE | NULL | 用户身高快照 |
| *userWeight* | `user_weight` | REAL | NULLABLE | NULL | 用户体重快照 |
| *userAge* | `user_age` | INTEGER | NULLABLE | NULL | 用户年龄快照 |
| *userBmr* | `user_bmr` | REAL | NULLABLE | NULL | 用户 BMR 快照 |
| *userCrowdType* | `user_crowd_type` | TEXT | NULLABLE | NULL | 用户人群分类快照 |

> `userHeight`/`userWeight`/`userAge`/`userBmr`/`userCrowdType` 为报告生成时的用户数据快照，确保历史报告可独立回溯。

### 3.7 body_metrics_history — 身体指标历史

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| historyId | `history_id` | INTEGER | PK | — | 记录唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID |
| recordDate | `record_date` | TEXT | NOT NULL | — | 记录日期 |
| height | `height` | REAL | NULLABLE | NULL | 身高(cm) |
| weight | `weight` | REAL | NULLABLE | NULL | 体重(kg) |
| age | `age` | INTEGER | NULLABLE | NULL | 年龄 |
| bmr | `bmr` | REAL | NULLABLE | NULL | 基础代谢率 |
| crowdType | `crowd_type` | TEXT | NULLABLE | NULL | 人群分类 |

**唯一约束**: `UNIQUE(user_id, record_date)` — 同一用户每天仅一条记录。

> ⚠️ **问题**: `recordDate` 使用 String 而非 DATE 类型。

### 3.8 family_relation — 亲属关系

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| relationId | `relation_id` | INTEGER | PK | — | 关系唯一标识 |
| guardianId | `guardian_id` | INTEGER | NOT NULL | — | 监护人ID（隐式外键→user） |
| wardId | `ward_id` | INTEGER | NOT NULL | — | 被监护人ID（隐式外键→user） |
| status | `status` | TEXT | CHECK(IN('pending','confirmed','rejected')) | `'pending'` | 关系状态 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 创建时间 |

**唯一约束**: `UNIQUE(guardian_id, ward_id)` — 防止重复关系。

另有 3 个 `@Transient` 字段（不存库仅序列化）：`guardianUsername`、`wardUsername`、`isGuardianOfCurrentUser`。

### 3.9 recipes — 菜谱库

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| recipeId | `recipe_id` | INTEGER | PK | — | 菜谱唯一标识 |
| recipeName | `recipe_name` | TEXT | NOT NULL | — | 菜谱名称 |
| description | `description` | TEXT | NULLABLE | NULL | 菜谱描述 |
| coverImageUrl | `cover_image_url` | TEXT | NULLABLE | NULL | 封面图片URL |
| calories | `calories` | DECIMAL | NULLABLE | NULL | 总热量(kcal) |
| protein | `protein` | DECIMAL | NULLABLE | NULL | 总蛋白质(g) |
| fat | `fat` | DECIMAL | NULLABLE | NULL | 总脂肪(g) |
| carbs | `carbs` | DECIMAL | NULLABLE | NULL | 总碳水(g) |
| fiber | `fiber` | DECIMAL | NULLABLE | NULL | 总膳食纤维(g) |
| tags | `tags` | TEXT | NULLABLE | NULL | 健康标签（逗号分隔:孕妇/糖尿病/老年人/青少年/高血压/减脂/健身/低GI/高蛋白/均衡） |
| source | `source` | TEXT | — | `'system'` | 来源 |
| createdBy | `created_by` | INTEGER | NULLABLE | NULL | 创建者ID |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 创建时间 |

### 3.10 recipe_ingredients — 菜谱食材

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| ingredientId | `ingredient_id` | INTEGER | PK | — | 食材唯一标识 |
| recipeId | `recipe_id` | INTEGER | NOT NULL | — | 所属菜谱ID（隐式外键→recipes） |
| ingredientName | `ingredient_name` | TEXT | NOT NULL | — | 食材名称 |
| amount | `amount` | DECIMAL | NOT NULL | — | 用量 |
| unit | `unit` | TEXT | — | `'g'` | 单位(g/ml/个/勺等) |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 创建时间 |

> 食材用量默认为生重(raw weight)。

### 3.11 saved_recipe — 用户收藏菜谱

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 收藏唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID |
| title | `title` | TEXT | NOT NULL | — | 菜谱标题 |
| ingredients | `ingredients` | TEXT | NULLABLE | NULL | 食材清单(JSON文本) |
| steps | `steps` | TEXT | NULLABLE | NULL | 烹饪步骤(JSON文本) |
| nutritionSummary | `nutrition_summary` | TEXT | NULLABLE | NULL | 营养摘要(JSON文本) |
| personaTag | `persona_tag` | TEXT | NULLABLE | NULL | 人群标签 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 收藏时间 |

### 3.12 article — 科普文章

文章生成系统的核心表，支持三版文章（速读/深度/综述）共存储在同一记录中，也可分开存储。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 文章唯一标识 |
| title | `title` | TEXT | NOT NULL | — | 文章标题 |
| topic | `topic` | TEXT | NULLABLE | NULL | 文章主题 |
| topicGroupId | `topic_group_id` | TEXT | NULLABLE | NULL | 主题分组ID（关联三版文章） |
| lengthType | `length_type` | TEXT | — | `'medium'` | 篇幅类型(short/medium/long) |
| content | `content` | TEXT | NOT NULL | — | 文章正文 |
| contentShort | `content_short` | TEXT | NULLABLE | NULL | 速读版正文 |
| contentMedium | `content_medium` | TEXT | NULLABLE | NULL | 深度版正文 |
| contentLong | `content_long` | TEXT | NULLABLE | NULL | 综述版正文 |
| summary | `summary` | TEXT | NULLABLE | NULL | 摘要 |
| summaryShort | `summary_short` | TEXT | NULLABLE | NULL | 速读版摘要 |
| summaryMedium | `summary_medium` | TEXT | NULLABLE | NULL | 深度版摘要 |
| summaryLong | `summary_long` | TEXT | NULLABLE | NULL | 综述版摘要 |
| tags | `tags` | TEXT | NULLABLE | NULL | 文章标签(JSON数组) |
| category | `category` | TEXT | NULLABLE | NULL | 文章分类 |
| audience | `audience` | TEXT | NULLABLE | NULL | 目标人群 |
| wordCount | `word_count` | INTEGER | NULLABLE | NULL | 字数统计 |
| sourcesJson | `sources_json` | TEXT | NULLABLE | NULL | 参考文献(JSON) |
| viewsCount | `views_count` | INTEGER | — | `0` | 阅读量 |
| likesCount | `likes_count` | INTEGER | — | `0` | 点赞数 |
| createdAt | `created_at` | DATE | — | `NOW` | 创建日期 |
| updatedAt | `updated_at` | DATE | — | `NOW` | 更新日期 |
| status | `status` | TEXT | — | `'published'` | 状态 |
| source | `source` | TEXT | — | `'ai'` | 来源(ai/manual/import) |
| qualityScore | `quality_score` | INTEGER | NULLABLE | NULL | AI质量评分(仅管理员可见) |
| hasErrorsReported | `has_errors_reported` | BOOLEAN | — | `false` | 是否有事实错误标记 |

### 3.13 article_analysis — 文章质量分析记录

用于文章质量分析闭环：AI 分析 → 识别问题 → 生成建议 → 管理员审核 → 应用优化。

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 分析记录ID |
| articleId | `article_id` | INTEGER | NOT NULL | — | 文章ID（隐式外键→article） |
| qualityScore | `quality_score` | INTEGER | NULLABLE | NULL | 质量综合评分 |
| issuesJson | `issues_json` | TEXT | NULLABLE | NULL | 问题清单(JSON) |
| suggestions | `suggestions` | TEXT | NULLABLE | NULL | 优化建议 |
| optimizedContent | `optimized_content` | TEXT | NULLABLE | NULL | 优化后内容 |
| status | `status` | TEXT | — | `'pending'` | 状态(pending/applied/rejected) |
| promptVersion | `prompt_version` | TEXT | NULLABLE | NULL | 生成提示词版本号 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 分析时间 |
| updatedAt | `updated_at` | TIMESTAMP | — | `NOW` | 更新时间 |

### 3.14 post — 社区帖子

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 帖子唯一标识 |
| userId | `user_id` | INTEGER | NOT NULL | — | 发布者ID |
| username | `username` | TEXT | NULLABLE | NULL | 发布者用户名（冗余） |
| content | `content` | TEXT | NOT NULL | — | 帖子内容 |
| imageUrl | `image_url` | TEXT | NULLABLE | NULL | 图片URL |
| tag | `tag` | TEXT | NULLABLE | NULL | 帖子标签 |
| likesCount | `likes_count` | INTEGER | — | `0` | 点赞数（冗余计数） |
| commentsCount | `comments_count` | INTEGER | — | `0` | 评论数（冗余计数） |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 发布时间 |
| status | `status` | TEXT | — | `'pending'` | 状态 |

**JPA 关系**:
- `@OneToMany(mappedBy="post", cascade=ALL, orphanRemoval=true)` → comments
- `@OneToMany(mappedBy="post", cascade=ALL, orphanRemoval=true)` → likes

> ⚠️ **问题**: `likesCount` 和 `commentsCount` 为冗余字段，需应用层维护一致性。
> ⚠️ **问题**: `username` 冗余存储，用户改名后帖子中的用户名不会同步。

### 3.15 comment — 帖子评论

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 评论唯一标识 |
| post | `post_id` | INTEGER | FK→post | — | 所属帖子（JPA ManyToOne） |
| userId | `user_id` | INTEGER | NOT NULL | — | 评论者ID |
| username | `username` | TEXT | NULLABLE | NULL | 评论者用户名（冗余） |
| content | `content` | TEXT | NOT NULL | — | 评论内容 |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 评论时间 |
| status | `status` | TEXT | — | `'pending'` | 状态 |

**JPA 关系**:
- `@ManyToOne(fetch=LAZY)` + `@JoinColumn(name="post_id")` → Post

### 3.16 post_like — 帖子点赞

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 点赞唯一标识 |
| post | `post_id` | INTEGER | FK→post | — | 所属帖子（JPA ManyToOne） |
| userId | `user_id` | INTEGER | NOT NULL | — | 点赞用户ID |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 点赞时间 |

**JPA 关系**:
- `@ManyToOne(fetch=LAZY)` + `@JoinColumn(name="post_id")` → Post

> ⚠️ **严重缺陷**: 无 `UNIQUE(post_id, user_id)` 唯一约束，同一用户可并发重复点赞。

### 3.17 ai_conversation_record — AI 对话记录

| 字段名 | 列名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | `id` | INTEGER | PK | — | 对话记录ID |
| userId | `user_id` | INTEGER | NOT NULL | — | 用户ID |
| model | `model` | TEXT | NULLABLE | NULL | 使用的 LLM 模型 |
| question | `question` | TEXT | NULLABLE | NULL | 用户问题 |
| reply | `reply` | TEXT | NULLABLE | NULL | AI 回复 |
| healthSnapshotJson | `health_snapshot_json` | TEXT | NULLABLE | NULL | 健康快照(JSON) |
| createdAt | `created_at` | TIMESTAMP | — | `NOW` | 对话时间 |

## 四、实体关系图（ER）

```
user ──────1:N────── diet_meal
  │                     │
  │                    1:N
  │                     │
  │                  diet_item ────N:1──── food
  │
  ├────1:N──── exercise_record
  ├────1:N──── nutrition_report
  ├────1:N──── body_metrics_history
  ├────1:N (guardian) ──── family_relation ──── 1:N (ward) ──── user
  ├────1:N──── ai_conversation_record
  ├────1:N──── saved_recipe
  │
recipes ────1:N──── recipe_ingredients
  │
  │ (前端收藏关联，非数据库外键)
  ▼
saved_recipe

article ────1:N──── article_analysis

post ──────1:N──── comment
  │
  └────1:N──── post_like
```

### 外键情况总结

| 关系类型 | 数量 | 方式 |
|----------|:----:|------|
| JPA `@ManyToOne` + `@JoinColumn` | 2 | Comment→Post, PostLike→Post |
| JPA `@OneToMany` (mappedBy) | 2 | Post→Comment, Post→Like |
| 裸 Integer 隐式外键 | 16 | 其余全部关联（user_id, food_id, meal_id, recipe_id, article_id 等） |

## 五、索引设计

### 现有约束（3 个）

| 表 | 约束类型 | 列 |
|----|----------|-----|
| `user` | UNIQUE（字段级） | `username` |
| `family_relation` | UNIQUE（表级） | `(guardian_id, ward_id)` |
| `body_metrics_history` | UNIQUE（表级） | `(user_id, record_date)` |

> ⚠️ **所有表均未使用 `@Index` 注解**，17 张表仅 3 个唯一约束作为索引。

### 建议补充的业务索引（30+）

| 表 | 建议索引列 | 理由 |
|----|-----------|------|
| `user` | `role` | 管理员用户筛选 |
| `food` | `food_category` | 分类查询高频 |
| `food` | `(food_category, priority DESC)` | 分类+优先级排序 |
| `food` | `status` | 审核状态筛选 |
| `diet_meal` | `(user_id, eat_date)` | 用户按日期查询饮食 |
| `diet_item` | `meal_id` | 通过餐食查条目 |
| `diet_item` | `food_id` | 反向查食物使用情况 |
| `exercise_record` | `(user_id, record_date)` | 用户按日期查运动 |
| `exercise_record` | `status` | 审核筛选 |
| `nutrition_report` | `(user_id, report_date)` | 用户按日期查报告 |
| `body_metrics_history` | `user_id` | 用户历史列表 |
| `recipe_ingredients` | `recipe_id` | 通过菜谱查食材 |
| `saved_recipe` | `user_id` | 用户收藏列表 |
| `article` | `topic` | 按主题查询 |
| `article` | `topic_group_id` | 三版文章关联查询 |
| `article` | `(topic, length_type)` | 主题+篇幅查询 |
| `article` | `category` | 分类浏览 |
| `article` | `status` | 发布状态筛选 |
| `article_analysis` | `article_id` | 按文章查分析 |
| `post` | `tag` | 按标签筛选 |
| `post` | `user_id` | 用户帖子列表 |
| `post` | `status` | 审核状态 |
| `post` | `created_at DESC` | 时间排序 |
| `comment` | `post_id` | 按帖子查评论 |
| `comment` | `user_id` | 用户评论列表 |
| `comment` | `status` | 审核状态 |
| `post_like` | `(post_id, user_id)` UNIQUE | 防重复+快速查点赞状态 |
| `post_like` | `user_id` | 用户点赞列表 |
| `ai_conversation_record` | `(user_id, created_at DESC)` | 用户对话历史 |

## 六、已知问题与改进建议

### P0 — 严重（影响数据一致性）

| 问题 | 描述 | 建议 |
|------|------|------|
| PostLike 无唯一约束 | 同一用户可并发多次点赞同一帖子 | 添加 `UNIQUE(post_id, user_id)` |
| SQLite 外键未启用 | SQLite 默认不强制外键约束 | 配置 `spring.jpa.properties.hibernate.dialect` 或连接时 `PRAGMA foreign_keys=ON` |
| 16 个裸 Integer 隐式外键 | 无数据库级引用完整性保障 | 非必须修复（JPA 层保证），但建议在文档中明确说明 |

### P1 — 中等（影响查询性能）

| 问题 | 描述 | 建议 |
|------|------|------|
| 缺失 30+ 业务索引 | 仅 3 个唯一约束，高频查询无索引 | 按上节建议表逐步添加 |
| 日期字段混用 | `diet_meal.eat_date` 和 `body_metrics_history.recordDate` 用 String；`article.created_at` 用 LocalDate | 统一使用 DATE 类型 |

### P2 — 轻微（影响可维护性）

| 问题 | 描述 | 建议 |
|------|------|------|
| Post/Comment 冗余 username | 用户改名后历史数据不同步 | 考虑 JOIN 查询替代冗余，或添加用户改名同步逻辑 |
| Post 冗余 likesCount/commentsCount | 应用层维护，可能不一致 | 添加定期修复脚本或使用数据库 COUNT 查询 |
| article.content 与 contentShort/Medium/Long 共存 | 冗余设计，三版文章既可共存储也可分开存 | 明确存储策略：分开存（推荐）或共存储择一 |

## 七、AI 服务辅助数据库

AI 服务（Python/FastAPI）在 `ai_service/data/` 目录维护另外 4 个独立 SQLite 数据库：

| 数据库文件 | 用途 |
|-----------|------|
| `conversations.db` | AI 对话记录与上下文管理 |
| `user_memory.db` | 用户长期记忆（偏好/习惯） |
| `food.db` | 食物数据副本（独立于主库） |
| `cache.db` | 向量检索与 LLM 响应缓存 |

这些数据库由 Python 端独立管理，不与后端 Spring Boot 共享连接。
