# 个人健康助手 — 项目优化与清理报告

> 日期：2026-08-05 | 版本：v1.0

---

## 一、功能审计与清理

### 1.1 已移除功能

| 序号 | 功能 | 移除内容 | 移除原因 |
|------|------|---------|----------|
| 1 | 文章浏览量统计 | `Article.viewsCount` 字段、`getArticleByIdWithView()` 方法、`/api/articles/top/views` 端点 | 功能无实际用户需求，浏览量+1逻辑增加不必要的数据库写操作 |
| 2 | 文章点赞统计 | `Article.likesCount` 字段、`likeArticle()` 方法、`/api/articles/top/likes` 端点、`/api/articles/{id}/like` 端点 | 文章点赞功能缺乏后端持久化机制（社区帖子点赞独立保留），统计数据无用 |
| 3 | 文章评论功能 | 无需移除（审计发现 Comment 实体仅关联 Post，不关联 Article，评论功能模块不存在于文章中） | 不存在，无需操作 |

**涉及文件（10个）**：
- 后端：`Article.java`、`ArticleController.java`、`ArticleRepository.java`、`ArticleService.java`
- 前端：`api/index.ts`、`api/articleMock.ts`、`Admin.vue`、`ArticleDetail.vue`

### 1.2 保留功能评估

| 模块 | 评估结果 | 建议 |
|------|---------|------|
| 社区帖子（Post） | `Post.likesCount` 属于独立社区模块，评论区仅关联Post | **保留**，后端社区功能完整可用 |
| AI 11个Agent | 全部由 `AgentOrchestrator` 注册调度，无孤立Agent，集成度良好 | **保留**，11个Agent各司其职形成完整流水线 |
| 管理后台（Admin.vue） | 活跃使用中，含文章管理、食物管理、用户管理 | **保留**，移除浏览量/点赞数列后功能正常 |

---

## 二、AI / Agent 集成评估

### 2.1 Agent 模块清单

| Agent | 文件 | 职责 | 必要性 |
|-------|------|------|--------|
| NLUParser | `agent/nlu_parser.py` | 意图识别 | 核心 |
| RetrieveJudge | `agent/retrieve_judge.py` | RAG检索判定 | 核心 |
| NutritionAnalysis | `agent/nutrition_analysis.py` | 营养分析 | 核心 |
| FoodAudit | `agent/food_audit.py` | 食物审查 | 核心 |
| DietPlan | `agent/diet_plan.py` | 膳食计划 | 核心 |
| WeeklyReport | `agent/weekly_report.py` | 周报生成 | 核心 |
| ArticleGenerate | `agent/article_generate.py` | 文章生成 | 核心 |
| HealthReflection | `agent/health_reflection.py` | 健康反思 | 核心 |
| FoodRecommend | `agent/food_recommend.py` | 食物推荐 | 核心 |
| ExerciseAdvice | `agent/exercise_advice.py` | 运动建议 | 核心 |
| VoiceTextParse | `agent/voice_text_parse.py` | 语音解析 | 核心 |

**结论**：全部11个Agent均被 `AgentOrchestrator` 统一调度，形成完整的知识检索→分析→生成→验证流水线，建议全部保留。

### 2.2 清理内容

| 类别 | 数量 | 清理详情 |
|------|------|---------|
| 幽灵.pyc文件 | 1 | 删除 `question_answer.cpython-312.pyc` |
| scripts/测试脚本 | 18 | 删除所有 test_*.py + comprehensive_test.py + detailed_test.py |
| scripts/验证脚本 | 15 | 删除 check_*.py、verify_*.py、inspect_*.py 等 |
| scripts/旧版CSV生成 | 6 | 删除 v1/v4-v8，保留 v9 最新版 |
| scripts/一次性导入 | 8 | 删除 import_*.py、batch_import_pdfs.py 等 |
| scripts/一次性修复 | 5 | 删除 fix_*.py、final_cleanup.py 等 |
| scripts/__pycache__ | 1 | 删除整个目录 |
| ai_service根目录测试 | 20 | 删除 test_*.py、debug_*.py、test_*.md 等 |
| test_results/ | 1 | 删除整个目录及其内容 |

**合计清理**：**75个文件 + 2个目录**

---

## 三、前端清理

### 3.1 已删除孤独组件（13个Vue文件）

| 类别 | 文件 | 原因 |
|------|------|------|
| views/ 孤立 | `Profile.vue`、`Analysis.vue`、`DietRecord.vue`、`FoodSearch.vue`、`Landing.vue` | 路由中无任何引用 |
| dashboard/ 重定向 | `AiChat.vue`、`FeatureHub.vue`、`HealthEducation.vue`、`Community.vue`、`RecipeSearch.vue` | 路由已redirect到其他页面 |
| dashboard/ 无路由 | `Exercise.vue` | 路由中不存在 |
| components/ 无引用 | `ContainerScrollAnimation.vue`、`MonthlyHeatmap.vue` | 无任何import引用 |

### 3.2 已清理API模块

- `api.community`（12个方法）：仅被已删除的 `Community.vue` 调用，现无活跃页面使用

### 3.3 保留的重定向路由（5条，仅路径跳转不做组件加载）

| 旧路径 | 跳转到 |
|--------|--------|
| `/dashboard/ai-chat` | `/dashboard/ai-consult` |
| `/dashboard/feature-hub` | `/dashboard/profile` |
| `/dashboard/health-education` | `/dashboard/articles` |
| `/dashboard/recipe-search` | `/dashboard/recipe-library` |
| `/dashboard/community` | `/dashboard/profile` |

---

## 四、代码清理汇总

| 类别 | 操作 | 详情 |
|------|------|------|
| TODO注释 | 移除 | `ArticleSplitUtil.java:251` 的 "TODO: 接入向量检索比对已有文章" → 已标注RAG已实现 |
| 测试文件 | 保留 | 7个 Maven 标准测试（src/test/java/）完整可用 |
| 临时/调试脚本 | 删除 | 75个文件 + 2个目录（详见AI清理章节） |
| 前端死代码 | 删除 | 13个Vue文件 + 1个API模块 |

---

## 五、新增架构文档

| 文档 | 路径 | 内容 |
|------|------|------|
| AI模块架构设计文档 | `health/AI模块架构设计文档.md` | 6章10图：Agent清单、RAG算法、Pipeline架构、数据流 |
| 后端系统架构设计文档 | `health/后端系统架构设计文档.md` | 10章：分层架构、142+ API、安全策略、性能优化、数据库ER图 |
| 前端应用架构设计文档 | `health/前端应用架构设计文档.md` | 9章7图：SPA架构、状态管理、路由设计、通信方案、界面设计 |

---

## 六、当前项目状态

### 后端（health/backend-health/）
- 框架：Spring Boot 2.7.18 + JPA + SQLite
- Controller：14个 REST 控制器
- Service：13个业务服务
- Entity：17个数据实体
- 安全：JWT + Spring Security + BCrypt
- 端口：8082

### 前端（health/frontend-health/）
- 框架：Vue 3 + TypeScript + Vite
- 页面：24个活跃路由页面
- 状态：Pinia userStore
- 通信：Axios + SSE流式
- 样式：Tailwind CSS + Morandi黑白设计
- 端口：5173

### AI服务（health/ai_service/）
- 框架：FastAPI（Python）
- Agent：11个专业Agent
- 检索：BGE向量 + BM25混合RAG
- 生成：自纠错进化流水线
- 端口：8002
