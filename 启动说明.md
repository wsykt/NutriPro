# 项目启动说明

> 所有启动/编译命令已固化到脚本 `start_all.ps1`，无需每次手写。

## 服务清单

| 服务 | 端口 | 技术栈 | 目录 |
|------|------|--------|------|
| AI服务 | 8002 | Python + FastAPI + ChromaDB + BGE | `ai_service/` |
| 后端服务 | 8082 | Java Spring Boot + SQLite | `backend-health/` |
| 前端服务 | 5173 | Vue3 + Vite + TypeScript | `frontend-health/` |
| Ollama | 11434 | 本地大模型（qwen2.5-7b-q4km） | D:\ollama\models |

## 一键启动（推荐）

```powershell
# 在项目根目录执行（带BOM的UTF-8编码，PS 5.1可正常解析中文）
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1"
```

脚本支持参数（可单独调用）：

| 参数 | 作用 |
|------|------|
| `-Build` | 先编译后端 jar 包（JDK 1.8 + IntelliJ Maven），再启动全部服务 |
| `-OllamaOnly` | 仅启动 Ollama（自动设置 OLLAMA_MODELS 指向 D 盘） |
| `-BackendOnly` | 仅启动后端（jar 包方式，JDK 1.8） |
| `-FrontendOnly` | 仅启动前端 |
| `-AIServiceOnly` | 仅启动 AI 服务 |
| `-StopAll` | 停止全部服务进程 |

示例：

```powershell
# 只编译后端（改代码后重新打包）
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1" -Build

# 只重启后端
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1" -BackendOnly

# 只启动 Ollama
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1" -OllamaOnly

# 停止所有服务
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1" -StopAll
```

## 单独启动各服务（手动命令）

### 0. Ollama（端口11434，可选）

```powershell
# 首次需设置模型目录指向 D 盘（已永久写入用户环境变量，一般无需重复）
[Environment]::SetEnvironmentVariable("OLLAMA_MODELS", "D:\ollama\models", "User")
$env:OLLAMA_MODELS = "D:\ollama\models"
ollama serve
```

**注意**：Ollama 的模型目录在 `D:\ollama\models`，必须设置 `OLLAMA_MODELS` 环境变量，否则 `ollama list` 看不到 qwen2.5-7b-q4km 模型。

### 1. AI服务（端口8002）

```powershell
# 工作目录：ai_service
python main.py
```

- 向量数据库：ChromaDB（路径 `ai_service/knowledge/chroma_db_storage`）
- 嵌入模型：BGE-base-zh-v1.5（路径 `ai_service/models/bge-base-zh-v1.5`）
- 当前知识库：1751条记录（含533张结构化知识卡片）
- LLM模式：cloud（DeepSeek API）

### 2. 后端服务（端口8082）

#### 方式一：jar包启动（推荐，速度快）
```powershell
# 工作目录：backend-health
& "C:\Program Files\Java\jdk1.8.0_341\bin\java.exe" -jar "target\health-backend-1.0.0.jar"
```

#### 方式二：Maven编译并启动（改代码后使用）
```powershell
# 工作目录：backend-health
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_341"
& "C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.3\plugins\maven\lib\maven3\bin\mvn.cmd" clean package "-Dmaven.test.skip=true"
# 打包成功后用方式一启动
```

#### 方式三：Maven直接运行
```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_341"
& "C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.3\plugins\maven\lib\maven3\bin\mvn.cmd" spring-boot:run
```

**注意**：
- **项目已统一改为 Java 1.8**（`pom.xml` 的 `<java.version>` 已改为 1.8），编译和运行都用 JDK 1.8.0_341
- 系统未安装独立Maven，使用IntelliJ IDEA内置Maven
- 编译时若后端正在运行，需先停止进程释放 jar 文件锁（`-Build` 参数会自动处理）

### 3. 前端服务（端口5173）

```powershell
# 工作目录：frontend-health
npm run dev
```

## 启动顺序

```
Ollama(11434) → AI服务(8002) → 后端(8082) → 前端(5173)
```

后端依赖AI服务（调用AI接口），前端依赖后端（调用API接口），建议按顺序启动。一键脚本已按此顺序处理。

## 环境依赖

| 依赖 | 版本/路径 |
|------|----------|
| Python | 3.10+ |
| Java JDK | jdk1.8.0_341（编译 + 运行，已统一） |
| Node.js | 18+ |
| Maven | IntelliJ IDEA 2025.3.3 内置 |
| Ollama | 可选（未安装时AI服务自动降级为云端API） |

## 停止服务

```powershell
# 推荐：用脚本停止
powershell -ExecutionPolicy Bypass -File "c:\Users\13425\Desktop\个人健康助手\health\start_all.ps1" -StopAll

# 或手动停止
Get-Process -Name java,python,node -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 常见问题

### Q: AI服务端口是8003还是8002？
A: 实际监听8002端口（见 `ai_service/config/settings.py` 中 `AI_SERVICE_PORT=8002`）。

### Q: mvn命令未找到？
A: 系统未安装独立Maven，使用IntelliJ IDEA内置Maven：
`C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.3\plugins\maven\lib\maven3\bin\mvn.cmd`

### Q: 后端启动报"No compiler is provided"？
A: 需设置JAVA_HOME指向JDK 1.8：
`$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_341"`

### Q: 编译报"无效的目标发行版: 21"？
A: 说明 pom.xml 的 java.version 仍是21，需改为 1.8，或确认 JAVA_HOME 指向 JDK 1.8。

### Q: 编译报"Failed to clean project / 无法删除jar"？
A: 后端进程正在运行占用了 jar 文件。先停止后端进程再编译（`-Build` 参数会自动处理）。

### Q: 编译报测试类构造器错误？
A: 测试代码与主代码不同步，编译时加 `-Dmaven.test.skip=true` 跳过测试编译即可。

### Q: Ollama 里看不到 qwen2.5-7b-q4km 模型？
A: 需要设置 `OLLAMA_MODELS=D:\ollama\models` 环境变量并重启 Ollama。若 Ollama 服务已在运行，先停止再启动。
